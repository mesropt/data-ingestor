---
phase: 07-canonical-schema-vendor-alias-crosswalk
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - src/assayingest/service.py
  - src/assayingest/api/wire.py
  - src/assayingest/api/routes/confirm.py
  - tests/test_confirm_records_aliases.py
  - tests/api/test_confirm_aliases_route.py
autonomous: true
requirements: [ALIAS-04, ALIAS-01, ALIAS-02, ALIAS-03]
must_haves:
  truths:
    - "Confirming a reviewed mapping against a target Schema + vendor records each resolved source column as an alias on the matching canonical field, with manual provenance stamped to the signed-in user's email."
    - "A confirm with no target Schema / no vendor (the CLI path and the current UI default) writes no alias — existing confirm behavior is unchanged."
    - "A recorded alias's provenance actor is the server-resolved user.email, never a client body field."
    - "Re-confirming the same source column keeps the alias's first-seen provenance (idempotent)."
  artifacts:
    - src/assayingest/service.py
    - src/assayingest/api/routes/confirm.py
    - src/assayingest/api/wire.py
  key_links:
    - "service.confirm gains keyword-only schema_store + target_schema_name + vendor; absent = nothing written (purely additive, no CLI call-site change)."
    - "confirm route resolves schema_store via deps.get_schema_store and passes user.email as the manual alias actor."
---

<objective>
Make the existing confirm/learning path also accrete the crosswalk: extend `service.confirm` and `POST /api/confirm` additively so that, when a target Schema + vendor are supplied, each resolved `(canonical field ← source column)` is upserted as an `Alias` with `provenance_kind="manual"` and `provenance_actor` = the authenticated confirming user's email. Implements D-07-05 (confirm records aliases, ALIAS-04) and D-07-06 (vendor is a free-text label). This is what makes the crosswalk grow from real review work rather than a parallel write.

Purpose: ALIAS-04 is the milestone's "accrete from real work" mechanism — the same confirm the curator already runs now also stamps provenance-carrying aliases into the governed Schema. It MUST stay additive: the CLI path and any confirm without a schema/vendor record nothing, so every existing confirm test stays green.

Output: additive keyword args on `service.confirm`; additive optional fields on `ConfirmRequest`; the confirm route threading the schema store + vendor + server-resolved actor; service-level and TestClient tests proving aliases are recorded with correct provenance, that the no-schema path writes nothing, and that existing confirm tests are untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-canonical-schema-vendor-alias-crosswalk/07-CONTEXT.md

# The exact confirm path this plan extends additively:
@src/assayingest/service.py
@src/assayingest/api/routes/confirm.py
@src/assayingest/api/wire.py
@src/assayingest/export/writers.py
@src/assayingest/api/deps.py
# Prior plan interfaces (Schema store + get_schema_store):
@.planning/phases/07-canonical-schema-vendor-alias-crosswalk/07-01-PLAN.md
@.planning/phases/07-canonical-schema-vendor-alias-crosswalk/07-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: additive service.confirm — record aliases with provenance (D-07-05, ALIAS-04)</name>
  <files>src/assayingest/service.py, tests/test_confirm_records_aliases.py</files>
  <behavior>
    Write tests/test_confirm_records_aliases.py FIRST (RED, tmp-path SqliteSchemaStore pre-seeded with a promoted Schema), then implement (GREEN):
    - confirm(..., schema_store=store, target_schema_name="assay-potency", vendor="NovaScreen", confirmed_by="a@b.com") on a fully-clear mapping records, for each field whose resolved source_column is not None, an Alias on that Schema's matching canonical field: vendor="NovaScreen", provenance_kind="manual", provenance_actor="a@b.com", plus a created_at timestamp (ALIAS-01/02/03/04).
    - A field whose resolved source_column is None (an inferred-only field) records NO alias.
    - confirm(...) with no schema_store / no target_schema_name / no vendor writes nothing — assert the store is untouched; the returned ConfirmResult is byte-identical in shape to today's (existing behavior unchanged).
    - Idempotency: confirming the same mapping twice keeps a single alias per (field, vendor, source_column) with its first-seen provenance (relies on the 07-01 store INSERT-OR-IGNORE).
    - Alias recording happens ONLY after the gate passes (never on a NotReadyError / FieldCoverageError path).
  </behavior>
  <action>Extend service.confirm's signature with keyword-only additive params: schema_store: SchemaStore | None = None, target_schema_name: str | None = None, vendor: str | None = None. Leave all existing params and behavior identical. After the existing gate (coverage check, validate, is_ready) passes and tidy/manifest are built — and only then — if schema_store and target_schema_name and vendor are ALL present, look up the Schema by name and, for each proposal.field_mapping whose source_column is not None, call schema_store.add_alias for the matching canonical field with provenance_kind="manual", provenance_actor=confirmed_by, vendor=vendor. When any of the three is absent, skip entirely (the CLI path passes none, so no CLI call site changes — mirrors how confirmed_by was threaded additively in Phase 06). Do NOT overwrite an existing alias (rely on the store's idempotent upsert). Import SchemaStore from .learning.schema_store. Keep this a single well-named private helper (e.g. _record_aliases) called from confirm — single level of abstraction per function. The actor is always the caller-supplied confirmed_by (a server-resolved email on the API path), never re-derived.</action>
  <verify>
    <automated>. ~/.nvm/nvm.sh 2>/dev/null; cd /home/mesrop_custom_user/PycharmProjectsWSL_Linux_native/data-ingestor && python -m pytest tests/test_confirm_records_aliases.py tests/test_service.py -x -q</automated>
  </verify>
  <done>service.confirm records manual+actor aliases when schema/vendor supplied, records nothing otherwise, is idempotent, and the pre-existing tests/test_service.py confirm suite still passes unchanged (additive extension proven).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConfirmRequest additive fields + confirm route records aliases (D-07-05/06, ALIAS-04)</name>
  <files>src/assayingest/api/wire.py, src/assayingest/api/routes/confirm.py, tests/api/test_confirm_aliases_route.py</files>
  <behavior>
    Write tests/api/test_confirm_aliases_route.py FIRST (RED, TestClient with dependency_overrides for get_profile_store, get_schema_store, require_verified_user), then implement (GREEN):
    - POST /api/confirm with schema_name + vendor in the body, by a verified user, records aliases into the named Schema's crosswalk with provenance_kind="manual" and provenance_actor = the server-resolved user.email (NOT any client body field) — ALIAS-04.
    - POST /api/confirm WITHOUT schema_name/vendor behaves exactly as today (no alias written, existing confirm-route tests unaffected).
    - The existing confirm-route contract (401 signed-out, 403 unverified, 404 no upload token, 422 gate rejection, export URLs) is unchanged.
  </behavior>
  <action>Add two additive optional fields to ConfirmRequest in api/wire.py: schema_name: str | None = None and vendor: str | None = None (defaults None — fully backward compatible; no existing field removed or reshaped). In routes/confirm.py, add a get_schema_store dependency (Depends, alongside the existing get_profile_store + require_verified_user), and pass schema_store=schema_store, target_schema_name=body.schema_name, vendor=body.vendor into the service.confirm call — the alias actor flows from the already-present user.email (AUTH-04 confirmed_by), never a body field (D-07-06: vendor is a free-text label the client supplies; the actor is server-resolved). Do not change any existing branch (registry lookup, field-set signature check, provenance retention, export). Import get_schema_store from ..deps. The vendor default-to-file-stem convenience is a client concern (Plan 07-04); the server records only when schema_name AND vendor are both present, matching Task 1's "absent = nothing written".</action>
  <verify>
    <automated>. ~/.nvm/nvm.sh 2>/dev/null; cd /home/mesrop_custom_user/PycharmProjectsWSL_Linux_native/data-ingestor && python -m pytest tests/api/test_confirm_aliases_route.py tests/api -x -q</automated>
  </verify>
  <done>ConfirmRequest carries optional schema_name/vendor; the confirm route records manual aliases into the named Schema with server-resolved provenance when both are present, and is otherwise byte-identical to today's behavior; all tests/api pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/confirm | schema_name + vendor are untrusted client input; the confirming identity must be server-resolved |
| confirm gate → crosswalk write | aliases must be recorded only after the P1 gate passes, never on a rejected mapping |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-10 | Spoofing | alias provenance_actor on confirm | high | mitigate | provenance_actor = confirmed_by = server-resolved user.email (require_verified_user); vendor is the only client-supplied label, and it carries no authority |
| T-07-11 | Tampering | alias write on a rejected mapping | high | mitigate | _record_aliases runs only after the coverage + validate + is_ready gate passes; a NotReadyError/FieldCoverageError path writes nothing |
| T-07-12 | Repudiation | duplicate/overwritten alias provenance | medium | mitigate | Recording relies on the 07-01 store's idempotent INSERT-OR-IGNORE — re-confirm keeps first-seen provenance (ALIAS-03) |
| T-07-13 | Elevation of privilege | signed-out/unverified confirm records alias | high | mitigate | Route already depends on require_verified_user; the alias-recording path inherits the same gate, no new unauthenticated surface |
| T-07-SC | Tampering | dependency supply chain | low | accept | No new package installs — additive edits to existing modules only |
</threat_model>

<verification>
- `python -m pytest tests/test_confirm_records_aliases.py tests/api/test_confirm_aliases_route.py -q` passes.
- `python -m pytest tests/test_service.py tests/api -q` — the entire existing confirm surface (service + routes) stays green, proving the extension is additive.
- `grep -n "schema_name" src/assayingest/api/wire.py` shows the additive optional field (default None).
</verification>

<success_criteria>
- ALIAS-04: confirming a reviewed mapping with a target Schema + vendor records each resolved source column as a manual, user-attributed alias on the matching canonical field.
- The CLI path and any confirm without schema/vendor record nothing; every pre-existing confirm test remains green.
- Alias provenance actor is server-resolved; recording is idempotent and gated behind confirm success.
</success_criteria>

<output>
Create `.planning/phases/07-canonical-schema-vendor-alias-crosswalk/07-03-SUMMARY.md` when done.
</output>
