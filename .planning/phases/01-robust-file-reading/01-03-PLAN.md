---
phase: 01-robust-file-reading
plan: 03
type: tdd
wave: 3
depends_on: [01-02]
files_modified:
  - scripts/gen_synthetic_pk.py
  - pyproject.toml
  - src/assayingest/parsing/structure/grid.py
  - src/assayingest/parsing/structure/sheets.py
  - src/assayingest/parsing/table.py
  - src/assayingest/cli.py
  - data/synthetic/README.md
  - tests/test_structure_sheets.py
  - tests/test_structure_drawings.py
  - tests/test_parse_entry_sheets.py
autonomous: true
requirements: [PARSE-04, PARSE-06]
user_setup: []
must_haves:
  truths:
    - "orion_pk_report.xlsx (Summary and Raw timepoints both tidy tables) makes the parser HESITATE and return a StructureQuestion with both sheets as ranked candidates — picking Summary silently would be wrong (D-09)."
    - "meridian_cro_codes.xlsx picks DATA over the narrow LEGEND sheet (PARSE-04)."
    - "A chartsheet is dropped from ranking and never crashes the reader; an embedded chart on a data sheet does NOT disqualify that sheet (D-16, D-17)."
    - "An image-only sheet returns a StructureQuestion, NOT the silent 'sheet has no data rows' skip (D-18)."
    - "defusedxml is installed so openpyxl.DEFUSEDXML is True — the XXE gap is closed (Pitfall 6)."
  artifacts:
    - src/assayingest/parsing/structure/sheets.py
    - "data/synthetic/ structural fixtures (embedded chart, chartsheet, image-only, two-tables-on-one-sheet)"
  key_links:
    - "table.parse() Excel branch selects the data sheet via sheets.py before header detection, returning a StructureQuestion when no clear winner (D-09)."
    - "grid.is_drawing_only_sheet routes image-only sheets to a StructureQuestion instead of the row_count==0 skip (D-18)."
---

<objective>
Deliver the multi-sheet + drawings vertical slice: a workbook's data sheet is selected structurally (meridian → DATA), a genuinely tied workbook makes the parser hesitate and ask (orion → Summary vs Raw timepoints, D-09), a chartsheet is dropped without crashing, an embedded chart never disqualifies its data sheet (D-16/D-17), and an image-only sheet surfaces a StructureQuestion instead of the false "no data rows" skip (D-18). Also close the pre-existing XXE gap (defusedxml) since this is the first phase to harden the parser against adversarial input.

The corpus has ZERO drawings today, so D-16..D-18 are untestable until this plan generates the fixtures — that is real work (a task), not a footnote.

Purpose: PARSE-04 plus the drawing edge cases are where a naive reader silently picks the wrong sheet or silently skips real data. Both are structural errors the downstream gate cannot see (D-02 rationale).
Output: 4 new structural fixtures via `gen_synthetic_pk.py` (Pillow dev-only), `structure/sheets.py` ranking, `grid.is_drawing_only_sheet`, the sheet-selection branch of `parse()`, the CLI fix for the drawing-only skip, and `defusedxml` added.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-robust-file-reading/01-CONTEXT.md
@.planning/phases/01-robust-file-reading/01-RESEARCH.md
@.planning/phases/01-robust-file-reading/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@scripts/gen_synthetic_pk.py
@src/assayingest/parsing/structure/grid.py
@src/assayingest/cli.py
@.planning/phases/01-robust-file-reading/01-02-SUMMARY.md
@data/synthetic/orion_pk_report.xlsx
@data/synthetic/meridian_cro_codes.xlsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: generate drawing fixtures (Pillow dev-only) + close the XXE gap (defusedxml)</name>
  <files>scripts/gen_synthetic_pk.py, pyproject.toml, data/synthetic/README.md, data/synthetic/ (generated .xlsx fixtures)</files>
  <read_first>
    - scripts/gen_synthetic_pk.py (gen_zephyr / gen_orion / save / main — 01-PATTERNS.md lines 320-340; function-per-fixture + shared save() pattern; RNG is seeded for determinism)
    - .planning/phases/01-robust-file-reading/01-RESEARCH.md §Pattern 7 (a/b/c/e — the throwaway fixture shape: Data / DataWithChart / ChartOnly chartsheet / ImageOnly), §Package Legitimacy Audit (Pillow OK, defusedxml OK — both registry-verified), §Pitfall 6 (defusedxml closes the XXE gap; openpyxl auto-detects it)
    - .planning/phases/01-robust-file-reading/01-CONTEXT.md §decisions D-16, D-17, D-18, D-19
  </read_first>
  <action>
    Add `pillow` to `[dependency-groups] dev` in pyproject.toml (NEVER to `[project] dependencies` — D-19: the parser must keep working with Pillow absent, which is itself the D-18 test case). Add `defusedxml` to `[project] dependencies` (runtime — Pitfall 6: openpyxl auto-detects it and flips openpyxl.DEFUSEDXML to True with no code change, closing the pre-existing XXE/billion-laughs gap on untrusted .xlsx). Run `uv add --dev pillow` and `uv add defusedxml`. Both packages were registry-verified in RESEARCH.md's Package Legitimacy Audit (Pillow 12.3.0, defusedxml 0.7.1 — decade-plus PyPI staples, verdict OK), so no legitimacy checkpoint is required.
    Extend `gen_synthetic_pk.py` with four deterministic fixtures appended to main()'s call list (update the trailing count message): (1) `gen_chart_embedded()` — a normal data sheet with a `BarChart` added via `ws.add_chart(chart, "E2")` (must still parse as a normal table); (2) `gen_chartsheet()` — a data sheet plus `wb.create_chartsheet(...)` (the chartsheet must be dropped from ranking, no crash); (3) `gen_image_only_sheet()` — a workbook whose one sheet holds only an `openpyxl.drawing.image.Image` (needs Pillow, dev-only) and no cells; (4) `gen_multiple_tables()` — one sheet holding two independent data blocks with different headers, separated by at least two fully-blank rows, so `shape.classify` has a real fixture for the `multiple_tables` branch (D-10) instead of only a defensive `unknown` fallback. Import Pillow-requiring symbols only inside the generator script, never from `src/assayingest/`.
    Update data/synthetic/README.md with a row per new fixture and note that DEMO-02's hazard list now includes drawings (per CONTEXT.md Specific Ideas).
  </action>
  <verify>
    <automated>uv run python scripts/gen_synthetic_pk.py && uv run python -c "import openpyxl; print('DEFUSEDXML', openpyxl.DEFUSEDXML)" | grep -v '^#' | grep -q 'DEFUSEDXML True'</automated>
  </verify>
  <acceptance_criteria>
    - The generator runs clean and writes the four new fixtures into data/synthetic/ (an embedded-chart workbook, a chartsheet-bearing workbook, an image-only-sheet workbook, a two-tables-on-one-sheet workbook).
    - `openpyxl.DEFUSEDXML` is True after `uv add defusedxml` (XXE gap closed — Pitfall 6).
    - `pillow` appears under `[dependency-groups] dev` and NOT under `[project] dependencies`; `defusedxml` appears under `[project] dependencies`.
    - `grep -rn "import PIL\|from PIL\|import pillow" src/` returns nothing (the parser never imports Pillow — D-19).
  </acceptance_criteria>
  <done>The corpus can now exercise D-16..D-18, and untrusted .xlsx parsing is protected against XML entity expansion.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: sheet ranking + drawing/chartsheet/image guards (sheets.py + grid.py)</name>
  <files>src/assayingest/parsing/structure/sheets.py, src/assayingest/parsing/structure/grid.py, tests/test_structure_sheets.py, tests/test_structure_drawings.py</files>
  <read_first>
    - .planning/phases/01-robust-file-reading/01-RESEARCH.md §Pattern 4 (sheet-ranking signals: fill density, row/col count, first-column uniqueness ratio; the VERIFIED orion result where Summary vs Raw timepoints are indistinguishable by shape — hesitation is correct) and §Pattern 7 (b: wb.worksheets excludes chartsheets; e: image-only sheet reports max_row=1 not 0, use ws._rels drawing relationship — the is_drawing_only_sheet predicate is quoted verbatim) and §Pitfall 4/5
    - .planning/phases/01-robust-file-reading/01-CONTEXT.md §decisions D-09, D-16, D-17, D-18, D-20, D-21
    - src/assayingest/parsing/structure/grid.py (list_worksheets from plan 02 — extend, do not duplicate)
  </read_first>
  <behavior>
    - rank_sheets(orion_pk_report.xlsx) returns Summary and Raw timepoints as tied top candidates (no confident single winner — D-09); the ranking function reports "no clear winner" for this file.
    - rank_sheets(meridian_cro_codes.xlsx) returns DATA as the confident winner over the narrow LEGEND sheet (PARSE-04).
    - The chartsheet-bearing fixture: rank_sheets never returns the chartsheet and does not raise when the workbook is opened (D-17).
    - The embedded-chart fixture: its data sheet ranks as a normal data sheet (the chart does not reduce its fill/shape score — D-16).
    - is_drawing_only_sheet(image-only sheet) == True; is_drawing_only_sheet(a normal data sheet) == False (D-18 — uses ws._rels drawing relationship, NOT ws._images which is empty without Pillow — Pitfall 4).
  </behavior>
  <action>
    Create `structure/sheets.py`. Implement `rank_sheets(path) -> SheetRanking` (a small frozen result: ranked list of (sheet_name, score), confident: bool) using `grid.list_worksheets` and the Pattern 4 structural signals (fill density, row count, column count, first-column uniqueness ratio as a weak input among several — never load-bearing alone). Return confident=False when the top two scores are within a named tie margin (the orion case — D-09). D-21's chart-reference-as-signal is OPTIONAL and off the critical path: include it only if it costs almost nothing; never make it load-bearing (no corpus file exercises it). Do NOT read numbers out of any chart cache (D-20).
    In `grid.py` add `is_drawing_only_sheet(ws) -> bool` exactly per RESEARCH.md Pattern 7e: has_content via `any(cell is not None and str(cell).strip() for row in ws.iter_rows(values_only=True) for cell in row)`; has_drawing_rel via `any(rel.Type.endswith("/relationships/drawing") for rel in (ws._rels or []))`; return `not has_content and has_drawing_rel`. Requires normal mode (already the default from plan 02).
  </action>
  <verify>
    <automated>uv run pytest tests/test_structure_sheets.py tests/test_structure_drawings.py -x</automated>
  </verify>
  <acceptance_criteria>
    - rank_sheets(orion) reports confident == False with Summary and Raw timepoints as the two top-ranked candidates (D-09 hesitation verified on the real file).
    - rank_sheets(meridian) reports confident == True with DATA as the winner.
    - Opening and ranking the chartsheet fixture raises no exception and never yields the chartsheet (D-17).
    - is_drawing_only_sheet is True for the image-only fixture sheet and False for a populated sheet (D-18).
  </acceptance_criteria>
  <done>Sheet selection ranks structurally, hesitates honestly when meaning (not shape) separates two sheets, and drawings/chartsheets/images are handled per their three distinct verified behaviors.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: wire sheet selection into parse()/CLI end-to-end (fix the drawing-only skip)</name>
  <files>src/assayingest/parsing/table.py, src/assayingest/cli.py, tests/test_parse_entry_sheets.py</files>
  <read_first>
    - src/assayingest/cli.py (_map_and_report — the `table.row_count == 0` skip branch is exactly the D-18 bug to fix; 01-PATTERNS.md lines 281-298)
    - src/assayingest/parsing/table.py (parse() from plans 01/02 — add sheet selection ahead of header detection)
    - .planning/phases/01-robust-file-reading/01-CONTEXT.md §decisions D-08, D-09, D-18
  </read_first>
  <behavior>
    - parse(orion_pk_report.xlsx) with no sheet given returns a StructureQuestion whose ranked alternatives include Summary and Raw timepoints (D-09), not a silently-picked RawTable.
    - parse(orion_pk_report.xlsx, sheet="Summary") proceeds to header detection and returns a RawTable (the human's/explicit sheet choice is honored — PARSE-06 proceed-on-hint).
    - parse(meridian_cro_codes.xlsx) with no sheet given auto-selects DATA and returns a RawTable.
    - parse(image-only fixture) returns a StructureQuestion explaining the sheet holds an unreadable drawing (D-18) — it does NOT return an empty RawTable and the CLI does NOT print "sheet has no data rows".
  </behavior>
  <action>
    In parse()'s Excel branch: when `sheet` is None and the workbook has more than one worksheet, run `structure.sheets.rank_sheets`; if confident, use the winner; if not, return a StructureQuestion with the ranked sheets as alternatives (D-09). Before treating any selected sheet as empty, call `grid.is_drawing_only_sheet` and, if true, return a StructureQuestion (D-18) rather than an empty RawTable. Honor an explicit `sheet=` or `hint.sheet_name` by skipping ranking (proceed-on-hint).
    In cli.py: replace the `table.row_count == 0` silent-skip path (01-PATTERNS.md line 291) so a StructureQuestion returned by parse() routes to the plan-01 `_ask_and_report` branch (D-08). The CLI asks; it never auto-picks a sheet.
  </action>
  <verify>
    <automated>uv run pytest tests/test_parse_entry_sheets.py -x</automated>
  </verify>
  <acceptance_criteria>
    - parse(orion) returns a StructureQuestion listing Summary and Raw timepoints; parse(orion, sheet="Summary") returns a RawTable.
    - parse(meridian) returns a RawTable built from the DATA sheet.
    - parse(image-only fixture) returns a StructureQuestion; the CLI no longer emits "sheet has no data rows" for it.
    - Full suite green: `uv run pytest -q`.
  </acceptance_criteria>
  <done>Multi-sheet workbooks select their data sheet or ask when genuinely ambiguous, and an image-only sheet asks instead of being silently skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted vendor workbook → openpyxl XML parser | An uploaded .xlsx is a zip of XML; a crafted archive can carry entity-expansion payloads or hostile drawing relationships. |
| PyPI → build environment | New dependencies (pillow dev, defusedxml runtime) are pulled from the package registry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Denial of Service | openpyxl XML parse of untrusted .xlsx (XXE / billion-laughs) | high | mitigate | Install defusedxml (runtime dep); openpyxl auto-detects it and switches to the entity-safe parser (openpyxl.DEFUSEDXML True). Verified by the Task 1 automated check. |
| T-01-07 | Denial of Service | image-only / chartsheet sheet crashing or silently skipping | medium | mitigate | is_drawing_only_sheet (via ws._rels, Pillow-independent) returns a StructureQuestion instead of skipping (D-18); wb.worksheets excludes chartsheets so the reader never crashes (D-17). |
| T-01-SC | Tampering | pip installs (pillow, defusedxml) | high | accept | RESEARCH.md Package Legitimacy Audit registry-verified both as decade-plus PyPI staples (Pillow 12.3.0, defusedxml 0.7.1, verdict OK) — not [ASSUMED]/[SUS], so no blocking legitimacy checkpoint required. |
</threat_model>

<verification>
- `uv run pytest tests/test_structure_sheets.py tests/test_structure_drawings.py tests/test_parse_entry_sheets.py -x` green.
- `uv run python scripts/gen_synthetic_pk.py` regenerates the corpus including the 3 drawing fixtures and the multiple-tables fixture.
- `uv run python -c "import openpyxl; print(openpyxl.DEFUSEDXML)"` prints True.
- Full suite green: `uv run pytest -q`.
</verification>

<success_criteria>
- meridian selects DATA; orion hesitates and asks with both sheets ranked (PARSE-04, D-09).
- Chartsheet dropped without crash; embedded chart does not disqualify its sheet; image-only sheet asks (D-16, D-17, D-18).
- XXE gap closed via defusedxml (Pitfall 6); Pillow stays dev-only (D-19).
</success_criteria>

## Artifacts this phase produces (this plan)

- `scripts/gen_synthetic_pk.py`: `gen_chart_embedded()`, `gen_chartsheet()`, `gen_image_only_sheet()`, `gen_multiple_tables()`; new fixtures in `data/synthetic/`.
- `pyproject.toml`: `pillow` (dev), `defusedxml` (runtime).
- `parsing/structure/sheets.py`: `rank_sheets(path) -> SheetRanking`.
- `parsing/structure/grid.py`: `is_drawing_only_sheet(ws) -> bool`.
- `parsing/table.py`: parse() Excel branch now selects the sheet / asks / detects drawing-only.
- `cli.py`: drawing-only skip bug removed; StructureQuestion routed to the ask-branch.

<output>
Create `.planning/phases/01-robust-file-reading/01-03-SUMMARY.md` when done.
</output>
