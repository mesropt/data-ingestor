---
phase: 01-robust-file-reading
plan: 04
type: tdd
wave: 4
depends_on: [01-02, 01-03]
files_modified:
  - src/assayingest/parsing/structure/shape.py
  - src/assayingest/parsing/table.py
  - tests/test_structure_shape.py
  - tests/test_parse_entry_shape.py
autonomous: true
requirements: [PARSE-05, PARSE-06]
must_haves:
  truths:
    - "apex_labs_wide_matrix.xlsx is classified wide_matrix and yields a StructureQuestion, NOT a RawTable (D-10, PARSE-05)."
    - "bionexus_transposed.xlsx is classified transposed (row_type_homogeneity > col_type_homogeneity inversion) and yields a StructureQuestion, NOT a RawTable (D-10)."
    - "A normal row_per_record table (zephyr Week 1) classifies row_per_record and DOES produce a RawTable (control)."
    - "There is no 'parse anyway with a warning' path — any non-row_per_record shape returns a question, never a caveated RawTable (D-11)."
  artifacts:
    - src/assayingest/parsing/structure/shape.py
  key_links:
    - "table.parse() runs shape classification after header/region resolution and gates RawTable construction on row_per_record (D-10)."
---

<objective>
Deliver the unsupported-shape vertical slice: a wide-matrix layout (apex_labs_wide_matrix.xlsx) and a transposed layout (bionexus_transposed.xlsx) are detected structurally and returned as a StructureQuestion explaining the shape is unsupported in v1 — never mapped to a silently-wrong structured result (D-10/D-11, PARSE-05). A normal table still parses (control).

This is the must-ship deterministic guard: if the phase runs out of time before plan 05 (Claude assist), the tool still refuses to emit a shifted/transposed garbage table, which is the exact failure PARSE-05 exists to prevent.

Purpose: A wide_matrix or transposed table read as row_per_record produces a clean-looking table that maps confidently and passes the export gate — the corruption PARSE-05 guards against. There is no un-pivot in v1 (PARSE-V2-01 deferred); v1 detects and asks.
Output: `structure/shape.py` classifier and the shape gate in `parse()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-robust-file-reading/01-CONTEXT.md
@.planning/phases/01-robust-file-reading/01-RESEARCH.md
@.planning/phases/01-robust-file-reading/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@src/assayingest/parsing/structure/grid.py
@src/assayingest/parsing/table.py
@.planning/phases/01-robust-file-reading/01-03-SUMMARY.md
@data/synthetic/apex_labs_wide_matrix.xlsx
@data/synthetic/bionexus_transposed.xlsx
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: shape classification (shape.py)</name>
  <files>src/assayingest/parsing/structure/shape.py, tests/test_structure_shape.py</files>
  <read_first>
    - .planning/phases/01-robust-file-reading/01-RESEARCH.md §Pattern 5 (the VERIFIED numbers: zephyr col_homogeneity 1.0 / row 0.333 = row_per_record; apex 0.875 / 0.333 = wide_matrix; bionexus 0.426 / 0.9 = transposed; the transposed signal is the row>col inversion; the wide_matrix signal is >=3 columns sharing a numeric dtype with pairwise-overlapping ranges and no distinguishing category column) and §Assumptions A3/A4 (thresholds are first-guesses; multiple_tables has NO fixture — implement defensively, mark low-confidence)
    - .planning/phases/01-robust-file-reading/01-CONTEXT.md §decisions D-10, D-11
    - .planning/phases/01-robust-file-reading/01-PATTERNS.md §"No Analog Found" (multiple_tables branch has no fixture — treat as unknown/low-confidence)
  </read_first>
  <behavior>
    - classify_shape(apex native-typed grid) == TableShape.wide_matrix (>=3 columns sharing a numeric dtype with overlapping ranges, no distinguishing category/unit column — Pattern 5's verified fingerprint).
    - classify_shape(bionexus native-typed grid) == TableShape.transposed (row_type_homogeneity 0.9 > col_type_homogeneity 0.426 — the verified inversion; confirmed by first-column being all-unique field-label strings).
    - classify_shape(zephyr Week 1 data region) == TableShape.row_per_record (control: 1.0 col / 0.333 row, no inversion).
    - A grid with a fully-blank separator row between two populated blocks classifies multiple_tables OR unknown (defensive — no fixture exists; must not misclassify as row_per_record).
  </behavior>
  <action>
    Create `structure/shape.py`. Implement `classify_shape(rows) -> TableShape` from native-typed rows (Pattern 6 types, from grid.read_grid). Compute `col_type_homogeneity` and `row_type_homogeneity` (excluding column 0) per Pattern 5; the row>col inversion (with a named margin constant) plus an all-unique-string first column confirms `transposed`. For `wide_matrix`: detect >=3 columns (and >=40-50% of columns, a named constant) sharing a numeric dtype with pairwise-overlapping value ranges and no single distinguishing category/unit column. Everything else with a genuine irregularity (blank separator block) → `multiple_tables` if the blank-block predicate fires, else `unknown`; a clean regular table → `row_per_record`. Mark the multiple_tables branch's uncertainty in a docstring (Assumptions A4: zero fixtures). Pure module — no I/O, no network (D-04). Single level of abstraction: one named helper per signal.
  </action>
  <verify>
    <automated>uv run pytest tests/test_structure_shape.py -x</automated>
  </verify>
  <acceptance_criteria>
    - classify_shape returns wide_matrix for apex, transposed for bionexus, row_per_record for zephyr Week 1 — matching Pattern 5's empirically verified fixtures.
    - The transposed classification relies on the row>col homogeneity inversion (test asserts the inversion is what fires, via the intermediate ratios or a documented named constant).
    - shape.py imports no anthropic symbol and does no file I/O.
    - A blank-separator-block grid does NOT classify as row_per_record.
  </acceptance_criteria>
  <done>Wide-matrix and transposed layouts are distinguished from a normal record table by structure, grounded in the verified corpus measurements.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: gate RawTable construction on row_per_record in parse()</name>
  <files>src/assayingest/parsing/table.py, tests/test_parse_entry_shape.py</files>
  <read_first>
    - src/assayingest/parsing/table.py (parse() from plans 01-03 — add the shape gate after header/region resolution, before building a RawTable)
    - .planning/phases/01-robust-file-reading/01-CONTEXT.md §decisions D-10, D-11 (no parse-anyway-with-warning path)
    - .planning/phases/01-robust-file-reading/01-01-SUMMARY.md (StructureQuestion contract)
  </read_first>
  <behavior>
    - parse(apex_labs_wide_matrix.xlsx) returns a StructureQuestion whose reason names the detected wide_matrix shape and states it is unsupported in v1 — NOT a RawTable (D-10).
    - parse(bionexus_transposed.xlsx) returns a StructureQuestion naming transposed — NOT a RawTable (D-10).
    - parse(zephyr, sheet="Week 1") still returns a RawTable (row_per_record control — the shape gate does not regress plan 02).
    - No code path attaches a shape warning to a RawTable and returns it anyway (D-11).
  </behavior>
  <action>
    In parse()'s Excel branch, after the header row and data region are resolved, run `structure.shape.classify_shape` on the native-typed data region. If the result is anything other than TableShape.row_per_record, return a StructureQuestion (proposal carries the detected TableShape, reason describes the consequence: the shape cannot be mapped to one-row-per-record without un-pivoting, which is v2) and do NOT build a RawTable (D-10/D-11). Only row_per_record proceeds to RawTable construction. Keep the message consequence-shaped per conventions.
  </action>
  <verify>
    <automated>uv run pytest tests/test_parse_entry_shape.py -x</automated>
  </verify>
  <acceptance_criteria>
    - parse(apex) and parse(bionexus) each return a StructureQuestion (no RawTable, no exception).
    - The returned question's proposal.table_shape is wide_matrix / transposed respectively.
    - parse(zephyr, sheet="Week 1") still returns a RawTable.
    - Full suite green: `uv run pytest -q`.
  </acceptance_criteria>
  <done>Any non-row-per-record shape is refused with an explaining question; only clean record tables become RawTables.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted vendor workbook → shape classifier | An uploaded workbook's table shape is untrusted; a wide/transposed/multi-table layout must not be coerced into a false row-per-record structure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Tampering (of output) | shape misclassification emitting a silently-wrong table | high | mitigate | classify_shape gates RawTable construction on row_per_record (D-10); any other shape returns a StructureQuestion, and there is no caveated-RawTable path (D-11) — the exact silent-corruption failure PARSE-05 exists to prevent. |
| T-01-09 | Denial of Service | classifier on an unrecognised layout | low | mitigate | Unrecognised irregular layouts fall to unknown/multiple_tables → StructureQuestion, never a crash (D-05). |
</threat_model>

<verification>
- `uv run pytest tests/test_structure_shape.py tests/test_parse_entry_shape.py -x` green.
- Full suite green: `uv run pytest -q`.
- Manual smoke: `uv run assayingest data/synthetic/apex_labs_wide_matrix.xlsx` prints a structural question (unsupported shape), not a mapped table.
</verification>

<success_criteria>
- apex → wide_matrix question; bionexus → transposed question; zephyr → row_per_record RawTable (PARSE-05, D-10).
- No parse-anyway-with-warning path exists (D-11).
</success_criteria>

## Artifacts this phase produces (this plan)

- `parsing/structure/shape.py`: `classify_shape(rows) -> TableShape`, with named margin/threshold constants for the transposed inversion and wide_matrix column-cluster predicates.
- `parsing/table.py`: parse() shape gate — RawTable only for row_per_record.

<output>
Create `.planning/phases/01-robust-file-reading/01-04-SUMMARY.md` when done.
</output>
