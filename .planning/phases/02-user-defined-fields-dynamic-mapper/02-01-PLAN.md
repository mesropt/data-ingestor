---
phase: 02-user-defined-fields-dynamic-mapper
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/assayingest/fields/__init__.py
  - src/assayingest/fields/models.py
  - src/assayingest/fields/loader.py
  - src/assayingest/mapping/schema.py
  - src/assayingest/mapping/mapper.py
  - src/assayingest/domain/models.py
  - src/assayingest/domain/reference.py
  - src/assayingest/cli.py
  - presets/assay-potency.yaml
  - pyproject.toml
  - uv.lock
  - tests/test_field_set.py
  - tests/test_schema_dynamic.py
  - tests/test_max_tokens_live.py
  - tests/test_mapper_boundary.py
  - tests/test_domain.py
  - tests/test_cli.py
  - tests/test_cli_run.py
  - tests/test_reference.py
autonomous: false
requirements: [FIELD-01, FIELD-02, FIELD-03, FIELD-04, MAP-01, MAP-02]
user_setup: []

must_haves:
  truths:
    - "A user-declared field set loaded from a YAML or JSON file flows through a runtime-built Pydantic schema into a real MappingProposal — with no field name, unit, or assay term hardcoded anywhere in the mapper."
    - "The mapper's system prompt and JSON schema contain exactly the field names the user declared and nothing about IC50/EC50/Ki/Kd/%inhibition/nM/µM."
    - "A field set larger than 50 fields is rejected with a named error before any schema is built."
    - "A field-set YAML file is loaded only via yaml.safe_load — yaml.load appears nowhere in src/."
    - "The CLI exits 5 (not 0) when a mapping is proposed but has any yellow field; 0 only when fully clear."
    - "The prompt tells Claude what locale the parser read for each column, as evidence — a decimal_comma column is named, an ambiguous one is not, and nothing in the prompt tells Claude the numbers are already trustworthy."
    # CORRECTED after 02-VERIFICATION.md (2026-07-10). This must_have originally ended:
    #   "pinnacle_labs_export.csv's Value column stops being re-flagged for comma ambiguity."
    # That clause was withdrawn, not quietly dropped. It promised a *model* outcome that the
    # code must not deliver: `needs_confirmation` is Claude's own judgment, and forcing it to
    # false whenever the parser resolved a locale would be the silent trust this project
    # exists to prevent (principle 1). On a live run `value` still returns yellow at 0.95,
    # with reasoning that visibly credits the parser's evidence — the mechanism works.
    # Making a repeat file go green is the Phase 3 learning loop's job (a human-confirmed
    # profile), not the prompt's. Tracked against LEARN-01..06.
    - "An ambiguous field arrives with ranked alternatives; a field whose value was inferred rather than read from a column always arrives with needs_confirmation=true."
    - "A ~50-field field set produces a response whose stop_reason is not 'max_tokens' — the raised _MAX_TOKENS is confirmed against a real call, not only against an estimate."
  artifacts:
    - src/assayingest/fields/models.py
    - src/assayingest/fields/loader.py
    - src/assayingest/mapping/schema.py
    - presets/assay-potency.yaml
    - tests/test_field_set.py
    - tests/test_schema_dynamic.py
    - tests/test_max_tokens_live.py
  key_links:
    - "loader.load(path) -> FieldSet -> build_wire_models(field_set.field_names) -> messages.parse(output_format=...) -> _to_domain -> MappingProposal"
    - "cli --fields <path> parsed into FieldSet and threaded through run() -> _map_one -> propose_mapping(table, field_set)"
    - "domain/models.py FieldMapping.target_field: str (TargetField enum removed) consumed by cli.py's three former .value sites"
---

## Phase Goal

**As a** data curator, **I want to** declare my own target fields in a file and have Claude map any messy CRO export onto them, **so that** the tool works for my domain with no code change.

<objective>
Replace the four hardcoded-domain sites (D-19) with a user-supplied field-set model and a runtime-built structured-output schema, and wire it end-to-end so `--fields <path>` drives a real `propose_mapping()` call. This is the phase's core vertical slice: a field set on disk → runtime `Literal` schema → `MappingProposal` rendered by `cli.py`, with zero domain knowledge compiled into the mapper.

Purpose: FIELD-01/02/03/04 and MAP-01/02 all depend on the field-set model existing and the mapper being schema-driven. This plan is the load-bearing one — if time runs out after it, a working domain-independent mapper has shipped.
Output: `fields/` package (model + safe loader), `build_wire_models()` runtime schema, a domain-free `mapper.py`, an emptied `domain/models.py`, `reference.py` reborn as `presets/assay-potency.yaml`, a `--fields` CLI flag, exit code 5 (D-23), and a green test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-user-defined-fields-dynamic-mapper/02-CONTEXT.md
@.planning/phases/02-user-defined-fields-dynamic-mapper/02-RESEARCH.md
@.planning/phases/02-user-defined-fields-dynamic-mapper/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_produced>
## Artifacts this plan produces (every new/changed symbol)

New package `src/assayingest/fields/`:
- `__init__.py` — empty (layer marker; absolute imports only if any).
- `models.py`:
  - `FIELD_TYPES: tuple[str, ...]` = `("number", "integer", "date", "text")` (D-06).
  - `@dataclass(frozen=True) class Field` — attributes: `name: str`; `description: str | None = None`; `type: str | None = None`; `allowed_values: tuple[str, ...] | None = None`; `unit: str | None = None`; `required: bool = True`; `min: float | None = None`; `max: float | None = None`; `date_format: str | None = None`. Method `to_dict() -> dict`.
  - `@dataclass(frozen=True) class FieldSet` — attributes: `fields: tuple[Field, ...]`; `name: str | None = None`. Properties: `field_names -> list[str]`; `signature -> str` (order-independent sha256 over normalised per-field descriptors — Phase 3's stable identity hook). Method `to_dict() -> dict`.
- `loader.py`:
  - `MAX_FIELDS: int = 50` (D-04).
  - `load(path: str | Path) -> FieldSet` — dispatch on `.yaml`/`.yml` (yaml.safe_load) vs `.json` (json.loads); FileNotFoundError for missing path, ValueError for unsupported extension / malformed shape / >50 fields / unknown `type`.
  - private helpers: `_build_field_set(raw: dict) -> FieldSet`, `_build_field(raw: dict) -> Field`.

Changed `src/assayingest/mapping/schema.py`:
- `WireCandidate` (static BaseModel) — unchanged.
- `build_wire_models(field_names: list[str]) -> type[BaseModel]` — returns a `create_model`-built `WireMappingProposal` class whose `target_field` is `Literal[tuple(field_names)]` (D-16).
- REMOVED: `TargetFieldName` literal alias, static `WireFieldMapping`, static `WireMappingProposal` (D-19).

Changed `src/assayingest/mapping/mapper.py`:
- `_MAX_TOKENS = 16000` (was 4096).
- `propose_mapping(table: RawTable, field_set: FieldSet, client: anthropic.Anthropic | None = None) -> MappingProposal`.
- `_render_system_prompt(field_set: FieldSet) -> str` (replaces module constant `_SYSTEM_PROMPT`).
- `_render_request(table: RawTable, field_set: FieldSet) -> str`.
- `_render_table(table: RawTable) -> str` — now also emits per-column locale when `len(column_locales) == len(headers)` (D-22).
- `_to_domain_field(item) -> FieldMapping` — `target_field=item.target_field` (plain str, no enum coercion).
- REMOVED imports: `TargetField`, `ALLOWED_UNITS`, `ASSAY_TYPES`, `UNIT_VALUE_RANGES`.

Changed `src/assayingest/domain/models.py`:
- `TargetField` enum REMOVED (D-19).
- `FieldMapping.target_field: str` (was `TargetField`).

New data file `presets/assay-potency.yaml` — the migrated content of the deleted `reference.py` as a loadable field set.

Deleted: `src/assayingest/domain/reference.py`, `tests/test_reference.py`.

Changed `src/assayingest/cli.py`:
- `--fields <path>` argument (required) parsed via `fields.loader.load` (D-01/FIELD-03).
- `run(path, sheet, hint, field_set)` threads `field_set` to `_map_and_report -> _map_one -> propose_mapping`.
- `_field_to_dict`, `_render_field`, `_render_gate` — `.target_field.value` → `.target_field`.
- `_map_one` returns `0 if proposal.is_ready else 5` (D-23).
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify PyYAML legitimacy, then add the dependency</name>
  <what-built>Nothing yet — this gate precedes the only package install in the phase. RESEARCH.md's Package Legitimacy Audit marked `pyyaml` `SUS`, solely because the legitimacy seam's PyPI data source reports `weeklyDownloads: null` (an `unknown-downloads` data-availability gap in the checker), not a real supply-chain signal. It must still be confirmed by a human before `uv add`.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/PyYAML/ — confirm: 20-year publication history, project homepage pyyaml.org, latest release 6.0.x (2025), no deprecation banner, no postinstall script.
    2. Confirm the package name is exactly `pyyaml` (not a typo/slopsquat like `pyyaml-utils`, `py-yaml`, `pyaml`).
    3. On approval, the executor runs `uv add pyyaml` (resolves to 6.0.3) — this writes `pyproject.toml` + `uv.lock`.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `uv add pyyaml`, or describe a concern.</resume-signal>
  <action>Pause for the blocking-human legitimacy check above. On "approved", run `uv add pyyaml` (resolves to 6.0.3), which writes `pyproject.toml` and `uv.lock`. Do not proceed to Task 2 until the package is installed. This gate is never auto-approvable (T-02-SC).</action>
  <verify>uv run python -c "import yaml; print(yaml.__version__)"</verify>
  <done>`pyyaml` is verified legitimate on pypi.org and added as a dependency; `import yaml` succeeds.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Field-set model + safe loader (fields/models.py, fields/loader.py)</name>
  <files>src/assayingest/fields/__init__.py, src/assayingest/fields/models.py, src/assayingest/fields/loader.py, tests/test_field_set.py</files>
  <read_first>
    - src/assayingest/parsing/hint.py — the frozen-dataclass-with-optional-fields + `to_dict()` + `_jsonable` analog (02-PATTERNS.md lines 26-59).
    - src/assayingest/parsing/table.py lines 57-100 — the `sheet_names`/`parse_file` raise-on-bad-input, dispatch-on-suffix boundary shape to mirror (02-PATTERNS.md lines 63-92).
    - tests/test_hint_and_locale.py and tests/test_mapper_boundary.py — the "pure dataclass + loader, no API key" test idiom (02-PATTERNS.md lines 399-425).
    - 02-RESEARCH.md Security Domain (lines 514-532) and D-03/D-04 in 02-CONTEXT.md — the safe_load and 50-field-cap rationale.
  </read_first>
  <behavior>
    - `Field(name="value", type="number", min=0.0, max=1000.0)` constructs; all attributes but `name` default to `None`/`True` per D-05..D-11.
    - `FieldSet((Field("a"), Field("b"))).field_names == ["a", "b"]`.
    - Two FieldSets with the same fields in different order have the same `.signature` (order-independent); a FieldSet with a renamed field has a different `.signature`.
    - `load()` on a `.yaml` file returns a FieldSet with the declared fields; `load()` on the equivalent `.json` file returns an equal FieldSet (D-02 dual format, one internal model).
    - `load()` on a field set declaring 51 fields raises ValueError whose message names both the count (51) and the limit (50) (D-04).
    - `load()` on a `.txt` path raises ValueError naming the unsupported extension; a missing path raises FileNotFoundError.
    - `load()` on a field declaring `type: colour` raises ValueError naming the bad type and the four allowed types (D-06).
    - The source of `fields/loader.py` contains `yaml.safe_load` and does NOT contain the literal call `yaml.load(` (D-03) — assert by reading the module source in the test.
  </behavior>
  <action>
    Create `fields/models.py` with `FIELD_TYPES`, frozen `Field` and `FieldSet` dataclasses exactly as listed in `<artifacts_produced>`. Use `tuple[str, ...] | None` for `allowed_values` and `tuple[Field, ...]` for `FieldSet.fields` so both stay frozen/hashable (do NOT use `list` defaults on a frozen dataclass). Reuse `hint.py`'s `_jsonable` recursion approach for `to_dict()`. `FieldSet.signature` = `hashlib.sha256` over `json.dumps(sorted([...normalised field dict...]), sort_keys=True)` where each normalised dict lowercases `name` and includes type/unit/allowed_values/required/min/max/date_format — this is Phase 3's `(field set, column signature)` identity hook (02-CONTEXT.md integration note); keep it order-independent by sorting.
    Create `fields/loader.py` mirroring `parsing/table.py`'s `Path`-normalise → `.exists()` → dispatch-on-suffix → raise-named-error shape. `.yaml`/`.yml` → `yaml.safe_load` (NEVER `yaml.load` — D-03, the phase's highest-priority control); `.json` → `json.loads`; else raise ValueError describing the consequence ("Cannot load field set ... expected a .yaml or .json file, got '{suffix}'"). `_build_field_set` enforces `MAX_FIELDS` before building any `Field` (fail fast per RESEARCH Security Domain), with a message naming count and limit. `_build_field` requires `name`, validates `type` against `FIELD_TYPES`. Error messages describe the consequence, not the symptom (CONVENTIONS.md). Relative imports internally, `from __future__ import annotations` first.
    Write `tests/test_field_set.py` covering every behavior above, including the source-grep for `yaml.safe_load` present / `yaml.load(` absent. Use `tmp_path` to write the fixture YAML/JSON files. RED first (import-error/attribute failures), then implement to GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_field_set.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_field_set.py` passes.
    - `grep -rn "yaml.load(" src/` returns no matches (only `yaml.safe_load` present in `fields/loader.py`).
    - `load()` of a 51-field YAML raises ValueError; the message contains both `51` and `50`.
  </acceptance_criteria>
  <done>`FieldSet` loads from YAML and JSON into one model, rejects >50 fields and unknown types with named errors, exposes an order-independent `signature`, and is loaded exclusively via `yaml.safe_load`.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Runtime structured-output schema (mapping/schema.py build_wire_models)</name>
  <files>src/assayingest/mapping/schema.py, tests/test_schema_dynamic.py</files>
  <read_first>
    - src/assayingest/mapping/schema.py — the static `TargetFieldName`/`WireFieldMapping`/`WireMappingProposal` being replaced (02-PATTERNS.md lines 96-130).
    - 02-RESEARCH.md Pattern 1 + Pattern 2 (lines 235-309) — the verified `create_model` + `Literal[tuple(field_names)]` code to copy directly; and Anti-Patterns (lines 335-338) on not sanitising field names and rebuilding per field set.
    - src/assayingest/parsing/structure_schema.py — the sibling wire-model docstring convention (02-PATTERNS.md lines 118-127).
  </read_first>
  <behavior>
    - `build_wire_models(["compound id", "µM value", "1st replicate"])` returns a Pydantic class; its JSON schema's `target_field` enum equals exactly those three strings (spaces, µ, leading digit preserved unmodified — RESEARCH Pattern 1).
    - The returned model validates a payload with `field_mappings` each carrying `source_column`, `confidence`, `reasoning`, `needs_confirmation`, `inferred_value`, `alternatives` (D-17 shape preserved).
    - A payload whose `target_field` is a name NOT in the supplied list fails validation (the schema-level constraint of D-16, not a post-hoc check).
    - `WireCandidate` remains importable and static.
  </behavior>
  <action>
    In `mapping/schema.py`, delete `TargetFieldName`, the static `WireFieldMapping`, and the static `WireMappingProposal` (D-19). Keep `WireCandidate` static. Add `build_wire_models(field_names: list[str]) -> type[BaseModel]` implemented exactly as 02-RESEARCH.md Pattern 2 (lines 272-293): inner `create_model` for `WireFieldMapping` with `target_field=(Literal[tuple(field_names)], ...)` and every other field's `(type, Field(description=...))` identical to today's static descriptions; outer `create_model` for `WireMappingProposal` with `field_mappings=(list[WireFieldMapping], ...)`; return the outer class. Do NOT sanitise field names (RESEARCH Anti-Pattern). Preserve the module docstring's wire-boundary framing. `from __future__ import annotations`, `from typing import Literal`, `from pydantic import BaseModel, Field, create_model`.
    Write `tests/test_schema_dynamic.py`: build a model from a field-name list, assert `model_json_schema()` (resolving `$defs`) exposes the exact enum, assert a valid payload parses and an out-of-set `target_field` raises `pydantic.ValidationError`. No API key, no network. RED first.
  </action>
  <verify>
    <automated>uv run pytest tests/test_schema_dynamic.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_schema_dynamic.py` passes.
    - The dynamic model's schema `target_field.enum` equals the exact list passed in, including a name containing `µ` and a leading digit.
    - `grep -n "TargetFieldName" src/assayingest/mapping/schema.py` returns no matches.
  </acceptance_criteria>
  <done>`build_wire_models` produces a per-request Pydantic class whose `target_field` is a runtime `Literal` over the user's field names, structurally constraining Claude's output (FIELD-04, D-16/D-17).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Empty the four domain sites and wire the field set end-to-end</name>
  <files>src/assayingest/mapping/mapper.py, src/assayingest/domain/models.py, src/assayingest/domain/reference.py, src/assayingest/cli.py, presets/assay-potency.yaml, tests/test_mapper_boundary.py, tests/test_domain.py, tests/test_cli.py, tests/test_cli_run.py, tests/test_reference.py</files>
  <read_first>
    - src/assayingest/mapping/mapper.py (whole file) — the `_SYSTEM_PROMPT`, `_render_request`, `_render_table`, `_to_domain_field`, optional-client seam, `_MAX_TOKENS` (02-PATTERNS.md lines 134-215).
    - src/assayingest/domain/models.py (whole file) — `TargetField` enum to delete, `FieldMapping.target_field` to retype (02-PATTERNS.md lines 219-242).
    - src/assayingest/domain/reference.py (whole file) — the vocabulary to migrate into `presets/assay-potency.yaml` (02-PATTERNS.md lines 246-268).
    - src/assayingest/cli.py — `_field_to_dict` (line 42 `.value`), `_render_field` (line 68 `.value`), `_render_gate` (line 100 `.value`), `_map_one` (return 0), `run()`, `main()`/`--hint` arg parsing as the `--fields` analog (02-PATTERNS.md lines 326-395).
    - 02-RESEARCH.md Code Examples (lines 442-462, post-D-19 `_to_domain_field`) and Pitfall 2/3 (lines 358-368, `_MAX_TOKENS`, column_locales guard).
    - tests/test_mapper_boundary.py, tests/test_domain.py, tests/test_cli.py, tests/test_cli_run.py — the four suites that import `TargetField` and must be migrated to plain strings + a `field_set`.
  </read_first>
  <action>
    This is one atomic lockstep — the four D-19 sites must change together or the import graph breaks. Do it in this order:
    1. `domain/models.py`: delete the `TargetField` enum; change `FieldMapping.target_field` annotation to `str`. Keep `ColumnCandidate`, `MappingProposal`, `is_clear`, `is_ready`, `unclear_fields` verbatim.
    2. `mapping/mapper.py`: drop the `TargetField` and `..domain.reference` imports; import `FieldSet` from `..fields.models` and `build_wire_models` from `.schema`. Set `_MAX_TOKENS = 16000`. Replace the `_SYSTEM_PROMPT` constant with `_render_system_prompt(field_set)` that keeps the three non-negotiable rules verbatim (propose-never-write / never-guess-silently / needs_confirmation) but renders the "Target fields" section from `field_set.fields` (name + optional description + declared type/unit/allowed_values/min/max) — no assay/unit literals survive (D-18). Change `propose_mapping` to `(table, field_set, client=None)`, passing `system=_render_system_prompt(field_set)`, `messages=[{"role":"user","content":_render_request(table, field_set)}]`, `output_format=build_wire_models(field_set.field_names)`, `max_tokens=_MAX_TOKENS`; keep the `client = client or anthropic.Anthropic()` seam and the `parsed_output is None` guard verbatim. `_render_request` builds the field descriptions from `field_set` (no `ASSAY_TYPES`/`UNIT_VALUE_RANGES`). `_render_table` additionally emits each column's locale when `len(table.column_locales) == len(table.headers)` (Pitfall 3 guard) so Claude stops re-asking a resolved `decimal_comma` column (D-22) — but phrase the locale line as evidence ("parser read column X as decimal_comma"), not as "trust the parser": an `ambiguous` column never reaches here, and Claude should still flag anything the annotation does not cover. `_to_domain_field` uses `target_field=item.target_field` (plain str, per RESEARCH lines 450-461).
    3. Create `presets/assay-potency.yaml`: migrate `reference.py`'s content — one field per target (`compound_id`, `assay_type`, `value`, `unit`, `target`, `n_replicates`, `assay_date`); `assay_type` gets `allowed_values: [IC50, EC50, Ki, Kd, "%inhibition"]` (D-07); `value` gets `type: number` and `min`/`max` from `UNIT_VALUE_RANGES`' widest bound (note in a YAML comment that per-unit ranges collapsed to one range — the per-unit granularity of `UNIT_VALUE_RANGES` is not preserved by D-10's one-range-per-field model); `unit` gets `allowed_values: [µM, nM, "%"]`; `assay_date` gets `type: date`; `n_replicates` gets `type: integer`. Reuse reference.py's "ground truth ... plain data on purpose" framing as the file header comment.
    4. Delete `src/assayingest/domain/reference.py` and `tests/test_reference.py`.
    5. `cli.py`: add a required `--fields` argument in `main()` mirroring the `--hint` parsing shape, load it via `fields.loader.load(args.fields)` inside the same `try/except ValueError → stderr → sys.exit(2)` block, and pass the resulting `FieldSet` into `run(...)`. Thread `field_set` through `run -> _map_and_report -> _map_one` and into `propose_mapping(table, field_set)`. Change the three `.target_field.value` reads (`_field_to_dict`, `_render_field`, `_render_gate`) to plain `.target_field`. Change `_map_one`'s success `return 0` to `return 0 if proposal.is_ready else 5` (D-23); leave codes 1/2/3/4 untouched and keep the `worst = max(...)` reducer.
    6. Migrate the four test suites: `test_domain.py` and `test_cli.py` construct `FieldMapping(target_field="value", ...)` with plain strings instead of `TargetField.*`; `test_mapper_boundary.py` builds its wire objects via `build_wire_models([...])(...)` and drops the `TargetField` import/assertion (assert on the plain string); `test_cli_run.py`'s live test loads `presets/assay-potency.yaml`, passes the `FieldSet` to `propose_mapping`, and compares `{m.target_field for m in ...}` to the preset's `set(field_set.field_names)`, and updates `run(...)` calls to pass `--fields`/a field_set and to expect exit 5 on a blocked proposal.
    Do NOT place fenced code in this file; follow the exact signatures in `<artifacts_produced>`.
  </action>
  <verify>
    <automated>uv run pytest -x && ! grep -rqn "TargetField\|ASSAY_TYPES\|UNIT_VALUE_RANGES\|domain.reference" src/ && ! grep -rqn "yaml.load\|FullLoader" src/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` — the full suite is green (the pre-existing 32+ tests plus the new ones), no import of `TargetField` remains.
    - `grep -rn "TargetField" src/` returns no matches; `src/assayingest/domain/reference.py` does not exist.
    - A test builds `_render_system_prompt(load("presets/assay-potency.yaml"))` and asserts it contains `assay_type`, `compound_id`, and `IC50` (because the assay preset declares them) — then builds `_render_system_prompt` from a non-assay field set (fields `catalogue_number`, `quantity`, `shelf`) and asserts the prompt contains those names and contains none of `IC50`, `EC50`, `Ki`, `Kd`, `%inhibition`, `nM`, `µM` (proves no biology is compiled in — SC3 mechanism, deep_work_rules success criterion 3).
    - `assayingest --fields presets/assay-potency.yaml data/synthetic/pinnacle_labs_export.csv` (with credentials) exits 0 if fully clear or 5 if any field is yellow — never 0-on-blocked.
    - **[D-22 regression guard]** `tests/test_mapper_boundary.py` asserts, with no network call, that `_render_table()` on a `RawTable` whose `column_locales` mark one column `decimal_comma` emits a locale-evidence line naming that column and `decimal_comma`; that a column marked `ambiguous` produces **no** such line (Claude must still flag it); and that the rendered text contains no phrase asserting the numbers are already validated or trustworthy (grep the output for `trust`, `validated`, `verified` — all absent). The prompt states what the parser read, never what Claude should conclude.
    - **[D-22 guard, degenerate input]** `_render_table()` on a `RawTable` with an empty `column_locales` (the legacy `parse_file()` path) emits no locale lines and does not raise.
    - **[MAP-02 / SC4 observable]** `tests/test_mapper_boundary.py` asserts that a wire payload whose field carries two ranked `alternatives` maps to a domain `FieldMapping` with those two `ColumnCandidate`s in order, and that a field with `source_column=None` and a non-null `inferred_value` arrives with `needs_confirmation=true` — an inferred value is never silently trusted.
  </acceptance_criteria>
  <done>The mapper carries zero hardcoded domain: a loaded field set builds the schema and the prompt, `TargetField`/`reference.py` are gone, the CLI takes `--fields` and returns exit 5 on a blocked gate, and the whole suite is green.</done>
</task>

<task type="execute">
  <name>Task 5: Confirm _MAX_TOKENS against a real 50-field call (closes RESEARCH Open Question 1)</name>
  <files>tests/test_max_tokens_live.py</files>
  <read_first>
    - .planning/phases/02-user-defined-fields-dynamic-mapper/02-RESEARCH.md §"Open Questions" Q1 and the token-budget analysis — the estimate is ~6.7k worst-case response against `_MAX_TOKENS = 16000`, but adaptive-thinking spend at `effort: "high"` was never measured.
    - src/assayingest/mapping/mapper.py — `_MAX_TOKENS`, `propose_mapping`, and the `parsed_output is None` guard which is what a truncated response actually trips.
    - tests/test_cli_run.py — the existing live-API test idiom, including how it is skipped without credentials.
  </read_first>
  <action>
    Add `tests/test_max_tokens_live.py`, marked so it skips without `ANTHROPIC_API_KEY` exactly as the existing live test does. Build a synthetic 50-field `FieldSet` in code (names `field_01`..`field_50`, each with a short description; no file needed), call `propose_mapping` against a small real `RawTable`, and assert `response.stop_reason != "max_tokens"` and that `parsed_output` is not None. This is the ONLY task in the phase that may issue a billed API call, and it issues exactly one. Its purpose is to turn RESEARCH's analytical estimate into a measurement — a 50-field set is allowed by D-04, so it must be known to work, not assumed to.
    Then mark `02-RESEARCH.md`'s `## Open Questions` Q1 as RESOLVED, recording the observed `stop_reason` and, if the SDK exposes it, `usage.output_tokens`. If `stop_reason == "max_tokens"`, do NOT silently raise the constant further — report the measured value and stop, so a human decides whether 50 fields is the right cap (D-04) or the budget is.
  </action>
  <verify>
    <automated>uv run pytest tests/test_max_tokens_live.py -v</automated>
  </verify>
  <acceptance_criteria>
    - With no `ANTHROPIC_API_KEY` the test skips, and the full suite stays green offline.
    - With credentials, a 50-field `propose_mapping` returns `stop_reason != "max_tokens"` and a non-None `parsed_output`.
    - `02-RESEARCH.md`'s Open Question 1 is marked RESOLVED with the observed `stop_reason` recorded.
  </acceptance_criteria>
  <done>The 50-field cap (D-04) and the raised token budget are known to hold against a real response, not only against an estimate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| field-set file → loader | An untrusted YAML/JSON field-set file (a stranger's shared template, or a Phase-4 browser POST) crosses into the process. |
| package index → build env | `uv add pyyaml` pulls a third-party package into the toolchain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation of Privilege | `fields/loader.py` YAML parse | high | mitigate | Load exclusively via `yaml.safe_load`; a test asserts the literal `yaml.load(` appears nowhere in `src/` (D-03). Blocks arbitrary Python object construction (`!!python/object/apply`), the CVE-2020-1747 / CVE-2020-14343 class. |
| T-02-02 | Denial of Service | `loader._build_field_set` → mapper schema | medium | mitigate | `MAX_FIELDS = 50` enforced with a named error *before* any `create_model`/API call (D-04, RESEARCH Security Domain "fail fast on the loader"). |
| T-02-03 | Tampering | `date_format`/`unit`/`allowed_values` strings rendered into the system prompt | low | accept | Single-user CLI; the field-set author is the reviewer. `date_format` only ever reaches `datetime.strptime`, never `eval`/`exec` (RESEARCH Known Threat Patterns row 3). No extra control needed this phase. |
| T-02-SC | Tampering (supply chain) | `uv add pyyaml` | high | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifies `pyyaml` on pypi.org before install; never auto-approved. |
</threat_model>

<verification>
- `uv run pytest` — full suite green, including `tests/test_field_set.py` and `tests/test_schema_dynamic.py`.
- `grep -rn "yaml.load(" src/` → no matches; `grep -rn "TargetField" src/` → no matches.
- `python -c "from assayingest.fields.loader import load; fs=load('presets/assay-potency.yaml'); print(fs.field_names)"` prints the seven field names.
- No assay term (`IC50`, `EC50`, `Ki`, `Kd`, `%inhibition`, `nM`, `µM`) appears in the mapper's system prompt for a non-assay field set (asserted in Task 4).
</verification>

<success_criteria>
- A YAML or JSON field set loads into one `FieldSet` model; >50 fields and unknown types are named errors; loading is `yaml.safe_load`-only.
- `build_wire_models` yields a per-request schema whose `target_field` enum is exactly the user's field names.
- `propose_mapping(table, field_set)` drives the Claude call with a prompt and schema built entirely from the field set — no domain literals remain in `src/`.
- The CLI accepts `--fields <path>` and distinguishes clear (0) from blocked (5).
- The full test suite is green.
</success_criteria>

<output>
Create `.planning/phases/02-user-defined-fields-dynamic-mapper/02-01-SUMMARY.md` when done.
</output>
