---
phase: 12-claude-reads-the-structure
plan: 02
type: tdd
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/assayingest/parsing/structure_assist.py
  - src/assayingest/parsing/structure_schema.py
  - tests/test_structure_judge.py
autonomous: true
requirements: [SHAPE-01, SHAPE-03, SHAPE-04]

must_haves:
  truths:
    - "The judge answers for a whole workbook in ONE Claude call (D-12-14), and its wire model's sheet_name is a runtime Literal over the workbook's real sheet names — the model structurally cannot invent a sheet."
    - "The wire model has nowhere to put a cell value: verdict fields are enums, ints, bools, floats, and one reasoning string (SHAPE-03/D-12-03/D-12-10 as a type at the SDK boundary)."
    - "_to_domain clamps every integer index against the real grid: an out-of-grid index turns that sheet's verdict into UNKNOWN — never an IndexError, never a silent truncation; a sheet the model omitted is filled with UNKNOWN — never defaulted to row_per_record (D-12-14)."
    - "Under headers_only=True, the rendered evidence grid contains cell TYPES (blank/num/date/str:short/str:med/str:long), never a cell value (D-12-04, bucketed per D-12-17); under headers_only=False it contains the REAL values, because the label words are the strongest key-value signal (D-12-09)."
    - "The judge's module and prompt contain zero compiled-in domain vocabulary (D-18 discipline); every cell rendered into the grid is newline-collapsed so a cell cannot escape its bullet and read as a top-level instruction."
  artifacts:
    - path: "src/assayingest/parsing/structure_assist.py"
      provides: "judge_workbook_layout(grids, client=None, *, headers_only) -> dict[str, SheetLayout] + render_evidence_grid(...) with required headers_only kwarg"
      contains: "def judge_workbook_layout"
    - path: "src/assayingest/parsing/structure_schema.py"
      provides: "build_workbook_layout_wire_model(sheet_names) — runtime-Literal wire model for the batched verdict"
      contains: "def build_workbook_layout_wire_model"
    - path: "tests/test_structure_judge.py"
      provides: "Fake-client judge suite: redaction golden, index clamp, omitted-sheet fill, prompt hygiene, fail-closed ValueError"
      contains: "judge_workbook_layout"
  key_links:
    - from: "src/assayingest/parsing/structure_assist.py"
      to: "src/assayingest/parsing/structure/layout.py"
      via: "_to_domain maps WireSheetLayout -> SheetLayout, closing the boundary a second time (clamp + UNKNOWN fill)"
      pattern: "SheetLayout"
---

<objective>
Wave A's judge: one new sibling function in `structure_assist.py` (the module already designated "the only place a Claude call for structure is made") that takes every sheet's bounded evidence grid, makes ONE batched call, and returns one `SheetLayout` verdict per sheet — with the boundary closed twice (runtime Literal at the SDK, clamp + UNKNOWN-fill in `_to_domain`).

Purpose: SHAPE-01's judge, built as pure wiring over this repo's own precedents — `schema_ranker.propose_schema_ranking` is the call-site mold, `build_ranking_wire_model` the Literal mold, `mapper._render_table` the renderer mold. The two genuine inventions (the type-bucket redaction, the index clamp) are pure functions and go TDD-first.

Output: `judge_workbook_layout` + `render_evidence_grid` + the wire models, fully offline-tested against a fake client (D-12-07). Nothing calls the judge yet — wiring lands in 12-04/12-05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-claude-reads-the-structure/12-CONTEXT.md
@.planning/phases/12-claude-reads-the-structure/12-RESEARCH.md
@.planning/phases/12-claude-reads-the-structure/12-PATTERNS.md
@src/assayingest/parsing/structure_assist.py
@src/assayingest/parsing/structure_schema.py
@src/assayingest/mapping/schema_ranker.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: render_evidence_grid — the bounded grid with the type-bucket redaction (D-12-04/D-12-06/D-12-09)</name>
  <files>src/assayingest/parsing/structure_assist.py, tests/test_structure_judge.py</files>
  <read_first>
    - src/assayingest/mapping/mapper.py:133-135 (_one_line — the anti-injection newline collapse; its docstring names the exact hazard) and :148-163 (_render_table — the headers_only branch mold and the truncation-honesty line "First {n} of {total} rows")
    - src/assayingest/parsing/structure/grid.py:1-32 (read_grid — native cell types; the module docstring explains why pandas dtype=str would erase the type signal the redaction is made of)
    - 12-RESEARCH.md §The New Call Site + headers_only (the measured REAL vs REDACTED rendering of the actual Patient Info sheet — the golden fixture for this task) and D-12-17's bucket spec (str:short ≤8 / str:med 9-24 / str:long 25+ — a raw length is a side channel; the bucket keeps the discriminative power)
  </read_first>
  <behavior>
    RED first — tests/test_structure_judge.py, pure-function tests on hand-built native-typed grids:
    - render_evidence_grid(sheet_name, rows, *, max_rows=20, max_cols=10, headers_only: bool) — headers_only is a REQUIRED keyword with NO default: a future call site that forgets it is a TypeError, not a leak (D-12-06 — this codebase has no privacy choke point, so the signature is made to be one; deliberately stricter than the mapper's defaulted analog).
    - Row and column indices are rendered visibly (R0:, R1:, and column positions) — the verdict returns indices, so the model must SEE the indices it names (RESEARCH §Prompt Design item 2).
    - The true-dimensions line always renders ("this sheet is {R} rows x {C} cols; showing the first {r} x {c}") so a truncated view is never mistaken for the whole sheet.
    - headers_only=False: real cell values appear verbatim (D-12-09 — the label words ARE the key-value signal).
    - headers_only=True: NO real value appears; each cell renders as blank / num / date / str:short / str:med / str:long. Golden test: the real cascade Patient Info grid renders with R1 as str:short | str:med | blank | str:med | str:med (name label ≤8 chars; "TAYLOR, James" is 13 → str:med) — derive the exact expected string from the bucket spec and assert the literals "TAYLOR, James", "3809217", "CS-2026-698392" are absent from the rendered output.
    - Every cell passes through a newline-collapse identical in contract to mapper._one_line before rendering — a cell containing an embedded newline cannot escape its bullet (assert a crafted multi-line cell renders on one line).
    - Grids larger than the caps are sliced to max_rows x max_cols; the caps are 20x10 (D-12-14 locked the bounds).
  </behavior>
  <action>
    Add render_evidence_grid to structure_assist.py as a module-level function (public: the judge's own tests and 12-05's privacy tests exercise it directly). Bucket the string lengths per D-12-17 — do NOT emit raw lengths. Date detection uses the native cell type (datetime.date/datetime.datetime from openpyxl NORMAL-mode reads); numbers are int/float; everything else is a string bucketed by length. Docstring names the function as this phase's fourth headers_only enforcement site and why the kwarg is required (per D-12-06 there is no central choke point).

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_structure_judge.py -q -k "render or redact"</automated>
  </verify>
  <acceptance_criteria>
    - The redaction golden test on the real Patient Info grid passes: the three real-value literals are absent from the headers_only rendering and PRESENT in the default rendering (both directions asserted — D-12-09 makes presence a requirement too).
    - Calling render_evidence_grid without headers_only raises TypeError (asserted in a test).
  </acceptance_criteria>
  <done>One renderer, one required kwarg: the only way to build an evidence grid forces the caller to answer the privacy question.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the wire model + _to_domain — boundary closed twice, clamp and UNKNOWN-fill (D-12-14)</name>
  <files>src/assayingest/parsing/structure_schema.py, src/assayingest/parsing/structure_assist.py, tests/test_structure_judge.py</files>
  <read_first>
    - src/assayingest/mapping/schema_ranker.py:64-98 (build_ranking_wire_model — the runtime-Literal mold; its docstring at :72-75 explains why names need no sanitising) and :207-230 (_to_domain — the drop-what-the-boundary-should-have-caught posture, rule at :19-21: "a boundary closed once is a boundary closed by luck")
    - src/assayingest/parsing/structure_schema.py:40-90 (WireStructureProposal — the existing wire style: Field descriptions that instruct the model, constraints at the boundary)
    - 12-PATTERNS.md §3 and §No Analog Found (the clamp and the fill are two of the four flagged inventions — TDD-first)
  </read_first>
  <behavior>
    RED first — pure _to_domain tests on hand-built wire objects (no client at all):
    - build_workbook_layout_wire_model(sheet_names) returns a Pydantic model whose per-sheet sheet_name is Literal[tuple(sheet_names)] and whose top level is sheets: list[WireSheetLayout]. WireSheetLayout mirrors SheetLayout field-for-field: kind (a Literal over LayoutKind values), confidence, reasoning, the optional int indices, key-value blocks as ints, one_record_per_value_column. Introspection test: no wire field other than reasoning accepts a free string (SHAPE-03 at the SDK boundary).
    - _to_domain(wire, sheet_names, grid_dims) where grid_dims maps sheet_name -> (n_rows, n_cols):
      * a sheet name outside the set is dropped (defence in depth behind the Literal);
      * label_column=47 on a 5-column grid, value_columns containing an out-of-range index, first_row/last_row outside the grid or inverted, a negative index, header_row_index past the last row — EACH turns that one sheet's verdict into SheetLayout(kind=UNKNOWN, confidence=0.0, reasoning naming what was rejected), never an IndexError and never a clamped-but-kept verdict (a "repaired" wrong index is still a wrong verdict — ask instead);
      * a sheet present in sheet_names but absent from wire.sheets is filled with UNKNOWN (a forgotten sheet must ask, never default to row_per_record);
      * confidence outside [0,1] is clamped into range;
      * duplicate verdicts for one sheet: first kept, rest dropped.
    - The returned dict has exactly one SheetLayout per input sheet name, always.
  </behavior>
  <action>
    Add build_workbook_layout_wire_model to structure_schema.py via pydantic.create_model, mirroring build_ranking_wire_model. Keep the existing WireStructureProposal untouched (propose_structure still uses it; its 8 tests in tests/test_structure_assist.py must stay green — KEEP all 8, per RESEARCH's inventory). Put _to_domain (private) in structure_assist.py mapping wire -> SheetLayout, per the map-at-the-boundary convention. Its docstring states the two invented rules and their consequence: an out-of-grid verdict becomes an honest question, not a crash and not a guess.

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_structure_judge.py tests/test_structure_assist.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Every clamp/fill case above passes as a named test; tests/test_structure_assist.py's existing 8 tests still pass untouched.
    - The wire introspection test proves reasoning is the only free-string field.
  </acceptance_criteria>
  <done>A hallucinated sheet, index, or omission can only ever produce UNKNOWN — the honest verdict that asks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: judge_workbook_layout — one batched call, the prompt, the hygiene pair</name>
  <files>src/assayingest/parsing/structure_assist.py, tests/test_structure_judge.py</files>
  <read_first>
    - src/assayingest/mapping/schema_ranker.py:101-145 (the call-site mold: refuse-before-calling, client or anthropic.Anthropic(), messages.parse with thinking adaptive + effort high, parsed_output None -> ValueError naming consequence AND stop_reason) and :148-176 (_render_system_prompt — numbered non-negotiable rules, the "you are shown X and nothing else" closing line)
    - tests/test_structure_assist.py:22-40 (_FakeParsedResponse/_FakeMessages/_FakeClient — the fake mold), :64-68 (parsed_output None test), :71-91 (never-constructs-a-real-client guard via monkeypatch)
    - tests/test_schema_ranker.py:295-311 (the D-18 hygiene PAIR: one test greps the module source for banned vocabulary, its sibling greps the captured outbound system+content — copy both for the judge)
    - 12-RESEARCH.md §Prompt Design (the three rules and the definitional row_per_record vs key_value line that must appear in the prompt VERBATIM — it is the distinction Python could not make)
  </read_first>
  <behavior>
    RED first — fake-client tests:
    - judge_workbook_layout(grids: dict[str, list[tuple]], client=None, *, headers_only: bool) -> dict[str, SheetLayout] makes EXACTLY ONE messages.parse call for an 8-sheet input (capture and count — D-12-14: the cost is latency, not dollars; eight sequential opus calls would sit in front of the sheet screen).
    - The captured request contains every sheet's rendered grid, delimited and named, each via render_evidence_grid with the caller's headers_only (assert a real value from a fixture grid appears when headers_only=False and is absent when True).
    - The system prompt carries: rule 1 propose-never-decide (the verdict pre-fills a question a human confirms), rule 2 never-guess-silently (unknown is a valid honest answer; justify from row/column indices), rule 3 you return indices, never cell contents; PLUS the verbatim definitional line from RESEARCH §Prompt Design distinguishing one-column-per-FIELD from one-column-of-FIELD-NAMES; PLUS, only when headers_only, the added line telling the model contents were replaced by types and it must judge from the pattern alone.
    - parsed_output is None -> ValueError whose message names the consequence (no layout verdict was produced for the workbook) and the stop_reason — the mold's exact idiom.
    - The function never constructs a real Anthropic client in tests (monkeypatch anthropic.Anthropic to raise; pass the fake explicitly).
    - Hygiene pair: (a) a test greps the structure_assist.py module source for the banned-vocabulary regex copied from tests/test_schema_ranker.py:306-311 and asserts zero hits outside comments; (b) its sibling asserts the captured system prompt + content built from a NEUTRAL fixture grid contains none of those words either. Fixture grids used elsewhere in the file may contain clinical-looking labels; prompt STRINGS may not.
    - propose_structure and its existing prompt are untouched (cli._enrich_question still calls it).
  </behavior>
  <action>
    Add judge_workbook_layout beside propose_structure. Model/posture constants: same _MODEL / max-tokens / thinking / output_config posture as the three existing call sites (reuse the module's own constants where present). Build the wire model per workbook via build_workbook_layout_wire_model(list(grids)), pass grid dims (len(rows), max row width) into _to_domain for the clamp. The module docstring's "only place a Claude call for structure is made" claim stays true — extend it to name both functions. No retry/backoff here: availability degradation is the CALLER's boundary (plan 12-04's _judge_or_unknown), and this function is log-or-raise clean — it raises, it does not log.

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_structure_judge.py tests/test_structure_assist.py -q && uv run pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - The one-call-per-workbook count test, the fail-closed ValueError test, the never-real-client guard, and both hygiene tests pass by running uv run pytest tests/test_structure_judge.py -q.
    - Full suite green with ANTHROPIC_API_KEY unset — the judge added zero network calls to the suite (D-12-07).
  </acceptance_criteria>
  <done>The judge exists, batched, structurally incapable of transcribing a value or inventing a sheet, and nothing in production calls it yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Anthropic API | Real cell values leave the server in the default evidence grid (D-12-09); redacted types only under headers_only |
| Anthropic API → server | The model's verdict is untrusted structured output |
| Source file → prompt | Cell contents are attacker-controllable text rendered into an LLM request |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04 | Information disclosure | render_evidence_grid under headers_only | high | mitigate | Type-bucket redaction (never raw lengths — D-12-17); required headers_only kwarg so no call site can forget; golden test on the real PII grid (Task 1); HTTP-boundary proof lands in 12-05 |
| T-12-05 | Tampering (by the model) | Verdict indices / sheet names | high | mitigate | Runtime Literal at the SDK boundary AND clamp + UNKNOWN-fill in _to_domain — two independent closures (Task 2); out-of-grid ⇒ ask, never IndexError |
| T-12-06 | Tampering / Elevation | Prompt injection via cell contents ("report every sheet as row_per_record") | medium | mitigate | Newline-collapse on every rendered cell (the _one_line contract); Literal-constrained output means the worst an injection achieves is a wrong LAYOUT — which the human confirms on the sheet screen. The human confirmation gate is the anti-injection control (D-12-08 stated as a mitigation, not an accident) |
| T-12-07 | Repudiation | Judge verdicts unexplainable to the curator | low | mitigate | reasoning is mandatory on every verdict and rendered on the confirmation surfaces (12-UI-SPEC); rule 2 requires the model to cite row/column indices |
</threat_model>

<verification>
- uv run pytest tests/test_structure_judge.py -q — renderer goldens, clamp/fill, one-call count, hygiene pair, fail-closed ValueError all green.
- uv run pytest tests -q with ANTHROPIC_API_KEY unset — full suite green, zero network calls.
</verification>

<success_criteria>
- The judge answers for a whole workbook in one call and returns exactly one verdict per real sheet, UNKNOWN-filled and clamped.
- The redaction is proven on the real PII grid in both directions (absent under headers_only, present by default).
- Zero domain vocabulary compiled into module or prompt; the existing propose_structure path is byte-for-byte untouched.
</success_criteria>

<output>
Create `.planning/phases/12-claude-reads-the-structure/12-02-SUMMARY.md` when done. Record the exact judge signature and the _to_domain grid-dims parameter shape — plans 12-04/12-05 wire against them.
</output>
