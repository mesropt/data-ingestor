---
phase: 12-claude-reads-the-structure
plan: 08
type: execute
wave: 7
depends_on: ["12-02", "12-07"]
files_modified:
  - data/synthetic/lab_corpus/layout_truth.json
  - tests/test_live_layout_eval.py
autonomous: true
requirements: [SHAPE-01, SHAPE-04]

must_haves:
  truths:
    - "Per-sheet layout ground truth exists for ~50 sheets (the 12 multi-sheet corpus workbooks + every data/synthetic shape fixture), declared IN-FILE as a sample, not a census, with kind values using LayoutKind strings verbatim so the eval needs no mapping table (D-12-17)."
    - "The live eval measures and REPORTS: key_value recall on the default grid against a 100% bar, row_per_record precision against a >=95% bar, confidently-wrong verdicts (confidence >=0.9 and wrong) against a ZERO bar — and, for the redacted headers_only grid, recall is MEASURED AND REPORTED WITH NO BAR, because setting a bar before the number exists would be theatre (D-12-17)."
    - "If redacted recall lands low, the recorded remedy is to ask the human more often in headers_only — NEVER to weaken the redaction (D-12-11)."
    - "Every live test is gated on ASSAYINGEST_LIVE_TESTS=1; the offline suite's zero-network-calls property is untouched (D-12-07)."
    - "The eval's numbers — confusion matrices, the redacted-recall figure, and the measured per-workbook judge latency (RESEARCH A2: nobody has timed it; do not ship an untimed number) — are written up for VERIFICATION.md. This is the single most important number the phase produces."
  artifacts:
    - path: "data/synthetic/lab_corpus/layout_truth.json"
      provides: "Per-sheet ground truth: description + legend + records of {file, sheet, kind}, sample-not-census declaration"
      contains: "sample"
    - path: "tests/test_live_layout_eval.py"
      provides: "The four live tests: driving case, corpus accuracy, redacted-grid accuracy, out-of-grid-index probe"
      contains: "ASSAYINGEST_LIVE_TESTS"
  key_links:
    - from: "tests/test_live_layout_eval.py"
      to: "data/synthetic/lab_corpus/layout_truth.json"
      via: "the eval grades judge_workbook_layout's real verdicts against the hand-authored truth"
      pattern: "layout_truth"
---

<objective>
The eval is a deliverable, not an afterthought (D-12-17): author the per-sheet ground truth that does not exist (manifest.json is per-file, covers 20 of 30 workbooks, and omits the driving file), build the gated live eval, run it, and report the numbers — including the one nobody can know without measuring: what `headers_only`'s redaction actually costs in judge accuracy.

Purpose: D-12-08 removed the second opinion; D-12-07 removed the offline path. The only honest substitute for both is a measured accuracy number against hand-labelled truth. A missed key-value sheet is the exact silent failure this phase exists to kill — hence the 100% recall bar; a false key_value is loud and human-correctable — hence the softer precision bar.

Output: `layout_truth.json`, `tests/test_live_layout_eval.py`, and the measured results written up for VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-claude-reads-the-structure/12-CONTEXT.md
@.planning/phases/12-claude-reads-the-structure/12-RESEARCH.md
@.planning/phases/12-claude-reads-the-structure/12-02-SUMMARY.md
@data/synthetic/lab_corpus/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: author layout_truth.json — ~50 sheets of hand-verified per-sheet ground truth</name>
  <files>data/synthetic/lab_corpus/layout_truth.json</files>
  <read_first>
    - data/synthetic/lab_corpus/manifest.json:1-30 (the top-level shape to copy: a description string, a legend object explaining every label, then the data — and the limitation being fixed: its layout label is per-FILE and omits the driving file)
    - 12-RESEARCH.md §How to evaluate it (the scope ruling: the ~50 sheets of the 12 multi-sheet corpus workbooks plus every data/synthetic shape fixture covers every layout kind in about an hour; labelling all 141 worksheets is not the ask)
  </read_first>
  <action>
    Write a small throwaway grid-dump script (scratch directory, not committed) that prints each candidate sheet's first 20x10 via grid.read_grid, and label each sheet by INSPECTION — the truth file is hand-verified, never generated by the judge under evaluation (that would grade the model with its own answers). Scope: all sheets of the 12 multi-sheet lab_corpus workbooks (including every sheet of cascade_allergy_CS-2026-698392.xlsx — Summary and Patient Info are key_value per the phase's own measured evidence) plus the data/synthetic shape fixtures (apex_labs_wide_matrix, bionexus_transposed, triton_screening_two_tables, quantex_scanned_report, nimbus_labs_chartsheet, and the ordinary row-per-record files). Format (the validator below pins it, so keep the two consistent): a top-level "description" string carrying the sample-not-a-census declaration, a "legend" object defining each kind in one sentence, a "not_covered" note naming what is excluded (the single-sheet corpus workbooks, CSVs per D-12-18), and a top-level "sheets" ARRAY whose records are {file, sheet, kind} — kind values matching LayoutKind strings verbatim. Where a sheet is genuinely arguable, label it with the kind a careful curator would pick and add a note field — an arguable label with a note beats a silent omission.
  </action>
  <verify>
    <automated>uv run python -c "import json; d=json.load(open('data/synthetic/lab_corpus/layout_truth.json')); ks={'row_per_record','key_value','wide_matrix','multiple_tables','not_a_table','unknown'}; recs=d['sheets']; assert len(recs)>=45, len(recs); assert all(r['kind'] in ks for r in recs); assert 'sample' in json.dumps(d).lower(); print(len(recs), 'sheets labelled')"</automated>
  </verify>
  <acceptance_criteria>
    - The validation one-liner passes: >=45 per-sheet records, every kind a verbatim LayoutKind value, the sample declaration present.
    - Every sheet of the driving cascade workbook is labelled, with Summary and Patient Info as key_value.
  </acceptance_criteria>
  <done>The ground truth the phase can be graded against exists, hand-verified, honestly scoped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the live eval suite — four gated tests, bars asserted where D-12-17 sets them</name>
  <files>tests/test_live_layout_eval.py</files>
  <read_first>
    - tests/test_cross_domain.py:78-99 (the ASSAYINGEST_LIVE_TESTS=1 skipif gate and the live-test shape — same gate at tests/test_max_tokens_live.py:46-47)
    - 12-RESEARCH.md §How to evaluate it (the metric/bar table, verbatim) and §The live tests (the four-test list) and Assumption A2 (latency is estimated, not timed — this suite times it)
  </read_first>
  <behavior>
    Structure-first (the test logic — truth loading, confusion-matrix computation, metric math — is a pure function over (truth, verdicts) pairs and gets an OFFLINE unit test with canned verdicts before any live call; only the four live tests wear the gate):
    - test_live_judge_reads_cascade_patient_info_as_key_value: the driving case end to end against the real model — both Summary AND Patient Info come back key_value with plausible blocks (the second is precisely the sheet the deleted heuristic called a normal table).
    - test_live_judge_layout_accuracy_against_the_corpus: judge every truth-labelled workbook on the DEFAULT grid; print the full confusion matrix and per-kind recall/precision; assert key_value recall == 100%, row_per_record precision >= 0.95, and ZERO verdicts with confidence >= 0.9 that disagree with truth.
    - test_live_judge_redacted_grid_accuracy: the same corpus under headers_only=True; print the same matrix and the key_value recall figure; assert NOTHING about the number (measure and report — no bar before the number exists). The docstring states D-12-11's remedy rule: if this lands low, the fix is asking the human more often in headers_only, never weakening the redaction.
    - test_live_judge_never_returns_an_out_of_grid_index: across all corpus calls, count how often the _to_domain clamp actually fired (expose a counter or inspect raw wire vs domain); report the count — the clamp is defence, and this measures whether it is ever exercised.
    - Each live test times its judge calls and prints per-workbook wall-clock latency (A2: the plan must measure the real latency and reconsider effort/model for the judge if it is unacceptable — do not ship a number nobody timed).
  </behavior>
  <action>
    Build the suite with the metric math offline-tested first (RED on hand-built truth/verdict pairs), then the four gated live tests. Keep the live set exactly these four — small and load-bearing. Print results in a machine-greppable block (one metric per line) so Task 3 can lift them verbatim.

    Commit RED (offline metric tests) then GREEN, then the live tests.
  </action>
  <verify>
    <automated>uv run pytest tests/test_live_layout_eval.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Without ASSAYINGEST_LIVE_TESTS=1, the file's live tests all SKIP and its offline metric tests PASS — the suite stays zero-network.
    - uv run pytest tests -q remains green offline.
  </acceptance_criteria>
  <done>The eval exists, its math is offline-proven, and its live half runs only when explicitly paid for.</done>
</task>

<task type="auto">
  <name>Task 3: run it and report — the number the phase produces</name>
  <files>tests/test_live_layout_eval.py</files>
  <read_first>
    - .planning/phases/12-claude-reads-the-structure/12-CONTEXT.md D-12-17 (measured and reported in VERIFICATION.md, not asserted) and D-12-11 (the remedy rule)
  </read_first>
  <action>
    If Anthropic credentials are available in the environment: run ASSAYINGEST_LIVE_TESTS=1 uv run pytest tests/test_live_layout_eval.py -q -s and capture the full output. Record in the SUMMARY (for the verifier to lift into VERIFICATION.md): both confusion matrices, key_value recall (default and redacted), row_per_record precision, the confidently-wrong count, the clamp-fired count, and the measured per-workbook latency with a judgment call on whether the effort/model posture needs revisiting. If any asserted bar FAILS, do not tune the truth file to pass — record the failing verdicts verbatim; a prompt fix is in scope (12-02's prompt, re-run the eval after), a bar reduction is not. If credentials are NOT available: record honestly that the live eval is authored but unrun, as the phase's open verification item — never fabricate a number.
  </action>
  <verify>
    <automated>uv run pytest tests/test_live_layout_eval.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains either the full measured results block or the explicit unrun-for-credentials record — one of the two, never silence.
    - If run: the three D-12-17 bars pass, and the redacted recall figure is reported with no bar applied.
  </acceptance_criteria>
  <done>The single most important number the phase produces exists on the record — or its absence is on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Anthropic API (live eval) | Real synthetic-corpus values leave the machine during the default-grid eval runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-25 | Information disclosure | Corpus values sent to the API during eval | low | accept | The corpus is wholly synthetic (the project's standing hard rule: synthetic data only, no real files); the redacted-grid eval additionally proves the headers_only path sends none |
| T-12-26 | Repudiation | Accuracy claims without evidence | medium | mitigate | Bars are asserted in a runnable test; results are captured verbatim into the SUMMARY/VERIFICATION.md; the unrun state is recorded explicitly if credentials are absent |
| T-12-27 | Tampering | Grading the judge against its own output | medium | mitigate | layout_truth.json is authored by human inspection of grid dumps, never generated by the judge (Task 1's explicit rule) |
</threat_model>

<verification>
- uv run pytest tests/test_live_layout_eval.py -q — offline metric tests pass, live tests skip without the gate.
- ASSAYINGEST_LIVE_TESTS=1 run (credentials permitting) — bars asserted, numbers captured for VERIFICATION.md.
</verification>

<success_criteria>
- Hand-verified per-sheet truth for ~50 sheets committed, sample-scoped, LayoutKind-keyed.
- The four live tests exist, gated, with the D-12-17 bars asserted where set and measurement-only where not.
- The measured results (or the honest unrun record) are in the SUMMARY for VERIFICATION.md.
</success_criteria>

<output>
Create `.planning/phases/12-claude-reads-the-structure/12-08-SUMMARY.md` when done, carrying the full eval results block — it is the phase's headline verification evidence.
</output>
