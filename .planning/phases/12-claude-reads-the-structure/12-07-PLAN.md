---
phase: 12-claude-reads-the-structure
plan: 07
type: tdd
wave: 6
depends_on: ["12-04", "12-05", "12-09"]
files_modified:
  - src/assayingest/parsing/structure/shape.py
  - src/assayingest/parsing/table.py
  - src/assayingest/parsing/structure/sheets.py
  - tests/test_structure_shape.py
  - tests/test_parse_entry_shape.py
  - tests/test_hint_and_locale.py
  - tests/test_structure_describe_sheets.py
  - tests/test_parse_entry_excel_header.py
  - tests/test_parse_entry_sheets.py
  - tests/test_hint_replay_cli.py
  - tests/test_origin_sheet.py
  - tests/conftest.py
  - .planning/todos/shape-judge-csv-gap.md
autonomous: true
requirements: [SHAPE-01]

must_haves:
  truths:
    - "The heuristic shape classifier module and its two production imports are gone; importing it raises ModuleNotFoundError (asserted by a test, with stale __pycache__ purged so a stale .pyc cannot satisfy the import)."
    - "The offline collateral is ENUMERATED, not discovered mid-execution: this plan owns the parse-level files (test_parse_entry_excel_header ~6 of 7, test_parse_entry_sheets ~5, test_hint_and_locale ~4 beyond the 1 inventoried, test_hint_replay_cli ~2, test_origin_sheet 1) — roughly 2x RESEARCH's 43-test inventory once counted by function. The API-route collateral (~25 in test_sheets_route, 5 in test_group_export) is NOT here: it is owned by 12-04 (the judging_client fixture + 7 manifest tests) and 12-05 (~18 resolve tests + test_group_export), because an API test cannot reach a judge_fn seam at all — get_anthropic_client -> lambda: None short-circuits _judge_for before any seam binds. parse_file() is immune (legacy pandas path, no shape gate) and test_real_corpus.py tolerates a StructureQuestion — neither breaks."
    - "With no verdict on an .xlsx parse, parse() returns the fail-closed _shape_unknown_question with answerable_by_hint=True — it never guesses a shape and never constructs a table (D-12-16; Wave C only lands because Wave B proved the verdict path end to end)."
    - "The shape question is now answerable by a hint — tests/test_hint_and_locale.py's pin of the old dead end is INVERTED, because hint.layout finally has a reader (D-12-15)."
    - "The purity guard lives on, re-pointed at layout.py and unpivot.py (already written in 12-01); the structural guards at test_parse_entry_shape.py:62 and :71 still hold non-vacuously — :71 now proves a non-row_per_record VERDICT never yields a RawTable."
    - "The CSV gap is recorded out loud: a key-value CSV parses into garbage today and continues to — a known, documented gap with a follow-up todo, not a discovery someone makes later (D-12-18)."
  artifacts:
    - path: "tests/test_parse_entry_shape.py"
      provides: "The rewritten verdict-driven shape tests + the fail-closed no-verdict test + the ModuleNotFoundError guard"
      contains: "ModuleNotFoundError"
    - path: ".planning/todos/shape-judge-csv-gap.md"
      provides: "The D-12-18 known-gap record: judging CSVs is a follow-up, with the cost stated (a Claude call on every CSV upload, the CLI's credential-less main path)"
      contains: "D-12-18"
  key_links:
    - from: "src/assayingest/parsing/table.py"
      to: "src/assayingest/parsing/table.py _shape_unknown_question"
      via: "the hint.layout-is-None branch now fails closed to the answerable question instead of consulting the deleted heuristic"
      pattern: "_shape_unknown_question"
---

<objective>
Wave C: delete. The heuristic classifier module, its two imports, and its test file (all but the purity guard, which 12-01 already re-homed) are removed. The verdict-less branch in `_parse_excel_structurally` now returns the fail-closed, answerable `_shape_unknown_question`; the verdict-less branch in `describe_sheets` now yields LAYOUT_UNKNOWN per sheet. Only now may the fallback disappear — Wave B (12-04/12-05) proved the verdict path works end to end.

Purpose: SHAPE-01's second half ("the Python heuristic classifier is removed"). This is D-12-01's consequence executed: the heuristic is not tuned wrong, it is structurally blind — it has no key-value predicate at all, and its transposed metric is exactly 0.000 on the driving sheets — so it is removed, not repaired. The invariant holds to the last commit: at no point may a sheet whose shape is not understood report headers — after this plan, an un-judged sheet reports a question instead.

Output: the deletion, the fail-closed switch, the test-inventory closure (RESEARCH's 43-test table is the checklist), and the D-12-18 CSV-gap record.

<!-- planner-discipline-allow: classify_shape -->
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-claude-reads-the-structure/12-CONTEXT.md
@.planning/phases/12-claude-reads-the-structure/12-RESEARCH.md
@.planning/phases/12-claude-reads-the-structure/12-04-SUMMARY.md
@src/assayingest/parsing/table.py
@src/assayingest/parsing/structure/sheets.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the fail-closed switch, red first — then the deletion</name>
  <files>src/assayingest/parsing/table.py, src/assayingest/parsing/structure/sheets.py, src/assayingest/parsing/structure/shape.py, tests/test_parse_entry_shape.py, tests/test_structure_shape.py</files>
  <read_first>
    - 12-RESEARCH.md §Removing classify_shape Safely, Wave C paragraph (the exact deletion list: the module, the import at sheets.py:33, the import at table.py:338, the test file) and §Runtime State Inventory (the stale-__pycache__ hazard: a leftover .pyc can satisfy an import in some layouts — purge and assert)
    - src/assayingest/parsing/table.py (the 12-03 dispatch — the None branch to switch) and tests/test_parse_entry_shape.py:71 (the never-a-RawTable-for-a-bad-shape guard whose vacuous-pass is RESEARCH Pitfall 1's warning sign)
  </read_first>
  <behavior>
    RED first:
    - Parsing an .xlsx through parse() with NO hint and NO layout returns a StructureQuestion (the answerable unknown-shape question from 12-03), never a RawTable and never a guessed shape. This test FAILS before the switch (today the heuristic auto-applies row_per_record) — observe and record the red.
    - describe_sheets with layouts=None yields LAYOUT_UNKNOWN for every sheet, headers suppressed (the 12-04 fallback removed).
    - import assayingest.parsing.structure.shape raises ModuleNotFoundError (pytest.raises around importlib.import_module), proving both the deletion and the pycache purge.
  </behavior>
  <action>
    Write the red tests, then: delete the heuristic module and both imports; switch table.py's no-verdict branch to the fail-closed question; remove sheets.py's fallback branch; delete tests/test_structure_shape.py entirely (its seven heuristic tests die with the function; its purity guard already lives re-pointed in tests/test_structure_layout.py since 12-01 — verify that guard covers both new modules before deleting). Purge stale bytecode: find src -name __pycache__ -type d -exec rm -rf {} + . Remove the TableShape-based dead code the deletion strands (the old unsupported-shape question builder, if nothing reaches it — grep call sites first; TableShape itself STAYS: it is persisted in saved profiles, deprecated not removed, per 12-01).

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_parse_entry_shape.py tests/test_structure_describe_sheets.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The ModuleNotFoundError test and the fail-closed no-verdict test pass by running uv run pytest tests/test_parse_entry_shape.py -q.
    - grep -rn "classify_shape" src/assayingest --include='*.py' | grep -v '^\s*#' | wc -l returns 0 — AND the full-suite run (Task 3) proves the removal broke nothing, because the grep alone proves only absence of the name.
  </acceptance_criteria>
  <done>The heuristic is gone; an un-judged sheet asks, answerably, instead of being guessed at.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test-inventory closure — rewrite what pinned the heuristic, invert the dead end</name>
  <files>tests/test_parse_entry_shape.py, tests/test_hint_and_locale.py, tests/test_structure_describe_sheets.py</files>
  <read_first>
    - 12-RESEARCH.md §The exact test inventory (the 43-test table with dispositions — this task's checklist) and 12-PATTERNS.md §Test Patterns T3/T7
    - tests/test_parse_entry_shape.py:20,:28,:36 (heuristic-driven shape questions → become verdict-driven: a WIDE_MATRIX/MULTIPLE_TABLES verdict yields the shape question), :44,:53 (the zephyr/meridian RawTable controls — they must KEEP PASSING, now via an explicit hint carrying a ROW_PER_RECORD SheetLayout with the header row), :62 (KEEP VERBATIM), :71 (rewrite so it cannot pass vacuously: a non-row_per_record VERDICT never yields a RawTable)
    - tests/test_hint_and_locale.py:153 (test_unsupported_shape_question_is_not_answerable_by_a_hint — INVERT: the shape question IS answerable now; rename it so the name tells the new truth)
  </read_first>
  <behavior>
    - Every test in the inventory's REWRITE column for these three files is rewritten per its stated disposition; every KEEP control passes.
    - The :153 inversion asserts answerable_by_hint is True on the shape question AND that answering it (a layout-carrying hint) actually changes the parse outcome — the round trip, not just the flag.
    - The remedy for a test that now receives a question: supply an explicit ROW_PER_RECORD layout hint (or, per 12-09's rule, an explicit header_row_index, which IS a row_per_record confirmation). NEVER weaken the fail-closed branch; never special-case test mode in production code.
  </behavior>
  <action>
    Work these three files, running uv run pytest tests/test_parse_entry_shape.py tests/test_hint_and_locale.py tests/test_structure_describe_sheets.py -q between them. Add the shared row_per_record_hint(header_row) factory to tests/conftest.py here — Task 3 and every future test consume it rather than copy-pasting a hint literal.

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_parse_entry_shape.py tests/test_hint_and_locale.py tests/test_structure_describe_sheets.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The inverted :153 test and the non-vacuous :71 rewrite pass by name; every KEEP control in these three files passes.
    - git diff for src/ in this task shows no gate changes (no production code was weakened to make a test pass).
  </acceptance_criteria>
  <done>The three inventory files tell the new truth: verdicts drive shape, and the shape question is answerable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the enumerated parse-level collateral — the pre-counted ~18 tests across four files</name>
  <files>tests/test_parse_entry_excel_header.py, tests/test_parse_entry_sheets.py, tests/test_hint_replay_cli.py, tests/test_origin_sheet.py</files>
  <read_first>
    - .planning/phases/12-claude-reads-the-structure/12-09-SUMMARY.md (the header_row_index-IS-a-row_per_record-confirmation rule — most of this collateral is fixed by the hint these tests ALREADY pass, once that rule lands; check each before adding a layout)
    - tests/test_parse_entry_excel_header.py (6 of 7 parse .xlsx with no verdict), tests/test_parse_entry_sheets.py (5), tests/test_hint_replay_cli.py (~2 — mostly covered by 12-09 Task 1), tests/test_origin_sheet.py (1)
  </read_first>
  <behavior>
    - Each of these ~18 tests parses an .xlsx and, post-deletion, would receive a layout question instead of its RawTable. For each: if it already passes an explicit header_row_index, 12-09's promotion rule fixes it with NO test change (verify, and note the count — a test that needed no change is evidence the rule was right). Otherwise, supply a ROW_PER_RECORD layout hint via the shared factory.
    - The FULL suite is green offline at the end of this task. This is the plan's closing gate, and it is achievable with the files this plan lists, because the API collateral belongs to 12-04/12-05 and landed there.
    - The context guard STAYS, now as a real guard rather than a scheduled escalation: if a file outside this plan's enumerated list turns up needing changes, STOP and record it in the SUMMARY before continuing — an unenumerated file means the count was wrong, and that is worth knowing loudly.
  </behavior>
  <action>
    Work the four files, running uv run pytest tests -q after each so an unenumerated surprise surfaces immediately. Record in the SUMMARY: how many of the ~18 needed NO change thanks to 12-09's rule, and the final collateral count against RESEARCH's 43-test estimate and the checker's ~50 — the retrospective wants that delta.

    Commit as one reviewable unit.
  </action>
  <verify>
    <automated>uv run pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - Full suite green with ANTHROPIC_API_KEY unset; zero network calls.
    - No file outside this plan's files_modified (plus 12-04's and 12-05's, already landed) required a change — or, if one did, it is recorded in the SUMMARY as a miscount, not silently fixed.
  </acceptance_criteria>
  <done>Every offline test that needed a verdict has one, from the enumerated list, without a single gate weakened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: the honest edges — CSV gap recorded, CLI no-credentials pin held (D-12-18)</name>
  <files>tests/test_parse_entry_shape.py, .planning/todos/shape-judge-csv-gap.md</files>
  <read_first>
    - .planning/phases/12-claude-reads-the-structure/12-09-SUMMARY.md (the CLI now HAS a judge — plan 12-09 wired it; what remains here is only the no-credentials fail-closed pin, and confirming 12-09's CLI tests still pass after the deletion)
    - 12-RESEARCH.md §CSV: an honest, documented gap (the wording and the cost analysis to carry into the todo)
  </read_first>
  <behavior>
    - CLI no-credentials pin held after the deletion: running the CLI against the cascade fixture with no credentials exits with the structural-question code and prints the unknown-layout question — never garbage, never a guessed table. (12-09 wrote this test; this task re-runs it post-deletion, because Wave C is exactly when it could regress.)
    - The CSV path is UNCHANGED and a test says so on purpose: a key-value-shaped CSV still parses into a garbage header row today — pin the current behaviour with a test whose docstring names it a KNOWN GAP citing D-12-18, so a future fix flips a documented test rather than discovering an undocumented behaviour.
  </behavior>
  <action>
    Add both pins. Write .planning/todos/shape-judge-csv-gap.md: what the gap is (a key-value CSV parses into garbage; the judge never runs on CSVs), why it was fenced (a Claude call on every CSV upload — the CLI's main, credential-less path; multiplies the offline-test surface), what the fix looks like (extend the judge call into the CSV path behind the same seams), citing D-12-18.
  </action>
  <verify>
    <automated>uv run pytest tests/test_parse_entry_shape.py tests/test_cli_run.py tests/test_sc5_row_per_record_parity.py -q && test -f .planning/todos/shape-judge-csv-gap.md</automated>
  </verify>
  <acceptance_criteria>
    - The CLI no-credentials exit-code pin and the CSV known-gap pin pass by name.
    - 12-09's SC5 parity sweep still passes AFTER the deletion — this is the criterion that actually discharges ROADMAP SC5 ("every existing row-per-record file still ingests exactly as it does today"), and Wave C is the commit it exists to guard.
    - The todo file exists and cites D-12-18.
  </acceptance_criteria>
  <done>Every accepted limitation is pinned by a test or a todo — nothing is left to be discovered later.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Un-judged file → parse() | With the heuristic gone, a file with no verdict must never become a table |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-22 | Tampering | A stale .pyc resurrecting the deleted classifier | medium | mitigate | pycache purge + the ModuleNotFoundError test (Task 1) |
| T-12-23 | Elevation | Test pressure weakening the fail-closed branch | high | mitigate | Task 2's explicit rule: collateral is fixed with hints/fakes, never by softening the gate; git-diff acceptance criterion on src/ |
| T-12-24 | Information disclosure | Key-value CSVs still parsing values into headers | medium | accept | D-12-18: pre-existing, unchanged by this phase, pinned by a named known-gap test and a todo — accepted out loud, not silently |
</threat_model>

<verification>
- uv run pytest tests -q with ANTHROPIC_API_KEY unset — full suite green after the deletion.
- uv run python -c "import importlib; importlib.import_module('assayingest.parsing.structure.shape')" exits non-zero.
</verification>

<success_criteria>
- The heuristic and its pins are gone; the fail-closed question stands where it stood; every inventory disposition is executed.
- CLI and CSV edges are pinned honestly; nothing was weakened to get to green.
</success_criteria>

<output>
Create `.planning/phases/12-claude-reads-the-structure/12-07-SUMMARY.md` when done. Record the actual collateral count vs RESEARCH's 43-test estimate — the retrospective wants that delta.
</output>
