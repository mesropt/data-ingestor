---
phase: 12-claude-reads-the-structure
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/assayingest/parsing/structure/layout.py
  - src/assayingest/parsing/structure/unpivot.py
  - src/assayingest/parsing/hint.py
  - tests/test_structure_layout.py
  - tests/test_structure_unpivot.py
  - tests/test_structure_hint.py
  - tests/test_profile_store.py
autonomous: true
requirements: [SHAPE-02, SHAPE-03]

must_haves:
  truths:
    - "SheetLayout structurally cannot carry a cell value: every field is int, bool, float, enum, tuple-of-blocks, or free-text reasoning — there is no field a transcribed datum could occupy (SHAPE-03 as a type, D-12-13/D-12-03/D-12-10)."
    - "unpivot_key_value on the real cascade Summary sheet (blocks [(0,(1,),7,12),(4,(5,),7,12)]) yields exactly 10 headers and 1 row; on Patient Info (blocks [(0,(1,),1,10),(3,(4,),1,10)]) exactly 17 headers and 1 row — the measured golden outputs from 12-RESEARCH."
    - "A single block with several value_columns and one_record_per_value_column=True yields one row PER value column — the transposed layout falls out of the same transform for free (D-12-13)."
    - "Duplicate labels never become duplicate headers: the second occurrence is deterministically renamed, so canonical._column_index's first-occurrence lookup can never silently lose a column (RESEARCH Pitfall 6)."
    - "A StructuralHint carrying a SheetLayout round-trips through to_dict()/JSON and back losslessly, and an old stored profile with no layout key still loads (no migration)."
  artifacts:
    - path: "src/assayingest/parsing/structure/layout.py"
      provides: "LayoutKind, KeyValueBlock, SheetLayout — the pure verdict contract (D-12-13, copied verbatim from CONTEXT, not re-derived)"
      contains: "class SheetLayout"
    - path: "src/assayingest/parsing/structure/unpivot.py"
      provides: "unpivot_key_value(rows, layout) -> (headers, string_rows) — the pure transform SHAPE-02 needs"
      contains: "def unpivot_key_value"
    - path: "tests/test_structure_unpivot.py"
      provides: "Golden tests for both driving sheets, the transposed melt, duplicate disambiguation"
      contains: "unpivot_key_value"
  key_links:
    - from: "src/assayingest/parsing/hint.py"
      to: "src/assayingest/parsing/structure/layout.py"
      via: "StructuralHint.layout: SheetLayout | None = None — the field that finally makes the hint round-trip load-bearing"
      pattern: "layout"
---

<objective>
Wave A opening move (add, don't remove): the pure verdict types (`layout.py`), the pure un-pivot transform (`unpivot.py`), and the `StructuralHint.layout` field with a verified serialisation round-trip. Nothing existing is touched beyond one new optional hint field; the 1132-test suite stays green throughout.

Purpose: D-12-13 locked the verdict contract (a tuple of `KeyValueBlock`s, because both driving sheets carry TWO side-by-side blocks belonging to one record — a single `label_column: int` is wrong on the very first file). This plan makes that contract a type, and makes SHAPE-03 structural: the model will return indices into these types, and the types have nowhere to put a cell value.

Output: two new pure modules + the hint field, all TDD (red first — these are pure functions over hand-built grids, the exact case `tdd_mode` exists for).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-claude-reads-the-structure/12-CONTEXT.md
@.planning/phases/12-claude-reads-the-structure/12-RESEARCH.md
@.planning/phases/12-claude-reads-the-structure/12-PATTERNS.md
@src/assayingest/parsing/hint.py
@src/assayingest/parsing/structure/shape.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: layout.py — the verdict contract as pure frozen types (D-12-13)</name>
  <files>src/assayingest/parsing/structure/layout.py, tests/test_structure_layout.py</files>
  <read_first>
    - .planning/phases/12-claude-reads-the-structure/12-CONTEXT.md D-12-13 (the contract, verbatim — copy it, do not re-derive)
    - src/assayingest/parsing/structure/shape.py:12-16 (the purity contract to copy in spirit: no I/O, no pandas, no openpyxl, no anthropic — testable without a file on disk)
    - src/assayingest/parsing/hint.py:19-31 (TableShape — the str-Enum idiom LayoutKind succeeds) and :47-61 (the frozen-dataclass-with-optional-dimensions idiom)
    - tests/test_structure_shape.py:101-118 (the textual import-line purity guard — the mold for the NEW guard written here; the old test file is untouched until Wave C)
  </read_first>
  <behavior>
    RED first — tests/test_structure_layout.py:
    - LayoutKind is a str-Enum with exactly the six members D-12-13 names: row_per_record, key_value, wide_matrix, multiple_tables, not_a_table, unknown.
    - KeyValueBlock is frozen: label_column: int, value_columns: tuple[int, ...], first_row: int, last_row: int (0-based into the RAW grid, inclusive).
    - SheetLayout is frozen: kind, confidence: float, reasoning: str, header_row_index/first_data_row/last_data_row: int | None (ROW_PER_RECORD only), key_value_blocks: tuple[KeyValueBlock, ...] = (), one_record_per_value_column: bool = False.
    - SheetLayout.needs_confirmation property: True when confidence < 0.9 (the same 0.9 line D-12-17's "confidently wrong" bar draws) — the server sets the gate; the client only renders it (12-UI-SPEC Discretion §1).
    - SheetLayout.record_count property: for key_value with one_record_per_value_column, the total count of value columns across blocks; otherwise 1 for key_value; None for other kinds (row count is not the layout's to know).
    - Purity guard (parametrized over layout.py AND unpivot.py — the Wave-C re-point of test_structure_shape.py:101, written fresh here so it exists before the old file dies): no import line in either module source mentions anthropic, pandas, or openpyxl.
    - Type-shape guard (SHAPE-03 as a test): introspect SheetLayout's and KeyValueBlock's dataclass fields and assert the only str-typed field on either is `reasoning` — a second free-string field is a hole where a cell value could be transcribed (12-PATTERNS §1 field-naming rule).
  </behavior>
  <action>
    Create src/assayingest/parsing/structure/layout.py with the D-12-13 contract exactly as CONTEXT states it. Module docstring copies shape.py's purity contract in spirit and states the phase's rule: the verdict carries INDICES into a grid Python reads; it can never carry a value (per D-12-03/D-12-10). Do NOT carry over any of shape.py's corpus-tuned constants or homogeneity math — this module holds types only, no logic beyond the two derived properties. Docstring on LayoutKind states what each member means for the parse gate (only ROW_PER_RECORD and KEY_VALUE produce a RawTable; UNKNOWN is the honest fail-closed answer that always asks, per D-12-16).

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_structure_layout.py -q</automated>
  </verify>
  <acceptance_criteria>
    - uv run pytest tests/test_structure_layout.py -q passes, including the purity guard and the only-str-is-reasoning type-shape guard.
    - grep -v '^\s*#' src/assayingest/parsing/structure/layout.py | grep -cE 'import (pandas|openpyxl|anthropic)' returns 0 (and the guard test proves the same by running).
  </acceptance_criteria>
  <done>The verdict contract exists as pure frozen types with structurally nowhere to put a cell value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: unpivot.py — the pure key-value/transposed transform (SHAPE-02's missing piece)</name>
  <files>src/assayingest/parsing/structure/unpivot.py, tests/test_structure_unpivot.py</files>
  <read_first>
    - .planning/phases/12-claude-reads-the-structure/12-RESEARCH.md §The Un-Pivot Transform (the signature, the verified golden outputs for both driving sheets, the duplicate-label hazard)
    - src/assayingest/parsing/table.py:467-503 (_raw_table_from_header_row — the output contract unpivot feeds), :524-526 (_row_to_strings — the stringification contract to match: "" if cell is None else str(cell)), :206-215 (_clean_header — the label-cleaning contract to match)
    - src/assayingest/parsing/structure/grid.py:22-32 (read_grid — the native-typed row tuples the transform consumes)
    - 12-PATTERNS.md §2 (this is one of the four flagged inventions — TDD-first is mandatory, and the driving-sheet expected outputs are precomputed)
  </read_first>
  <behavior>
    RED first — tests/test_structure_unpivot.py, on hand-built native-typed grids (no file I/O) plus the two real driving sheets read via grid.read_grid:
    - Signature: unpivot_key_value(rows: list[tuple], layout: SheetLayout) -> tuple[list[str], list[list[str]]] — (headers, string_rows), the two things a RawTable is made of.
    - Single block, one value column → labels become headers, values become the single row.
    - Two blocks, one record (one_record_per_value_column=False) → both blocks contribute COLUMNS to ONE record, in block order.
    - GOLDEN (real file, data/synthetic/lab_corpus/cascade_allergy_CS-2026-698392.xlsx): Summary with blocks [(0,(1,),7,12),(4,(5,),7,12)] → exactly 10 headers, 1 row, first header "Patient Name", first value "TAYLOR, James"; Patient Info with blocks [(0,(1,),1,10),(3,(4,),1,10)] → exactly 17 headers, 1 row. (These are RESEARCH's measured outputs — the transform is proven against the file that forced the phase.)
    - Transposed: one block, value_columns=(1,2,3), one_record_per_value_column=True → 3 rows, one per value column, same headers (D-12-13: transposed folds into KEY_VALUE for free).
    - Duplicate labels: two rows labelled identically → second header deterministically disambiguated with a " (2)" suffix (then " (3)"...), asserted so canonical's first-occurrence header lookup can never silently strand a column (RESEARCH Pitfall 6). Zero duplicates on either driving sheet — this is defence, asserted on a hand-built grid.
    - Stringification: a native float 0.19 becomes "0.19" (no thousands separator — matching the _row_to_strings contract keeps the downstream locale gate honest); None becomes "".
    - Blank labels stay "" after cleaning (an unlabelled row is signal, not noise — the _clean_header rationale).
    - Rows outside [first_row, last_row] and columns outside the block's declared columns are never read.
  </behavior>
  <action>
    Create src/assayingest/parsing/structure/unpivot.py, pure (stdlib + layout.py imports only — it must pass Task 1's purity guard). It re-implements the two 1-line cell contracts locally with docstring cross-references to table.py:524-526 and :206-215 (importing them from table.py would drag pandas into the pure module; the duplication is two lines and deliberate — say so in the docstring). Internal imports are relative (from .layout import ...). The disambiguation policy (" (2)" suffix) gets a docstring naming the consequence it prevents: a duplicate header's second column is silently unreachable downstream (cites canonical.py:259-269 via 12-RESEARCH). Do not raise on duplicates and do not inherit first-wins — both rejected alternatives are named in RESEARCH §Friction (c).

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_structure_unpivot.py tests/test_structure_layout.py -q</automated>
  </verify>
  <acceptance_criteria>
    - uv run pytest tests/test_structure_unpivot.py -q passes, including both real-file golden tests (Summary 10/1, Patient Info 17/1) and the transposed 3-row melt.
    - The purity guard (Task 1, parametrized over unpivot.py) passes: grep -v '^\s*#' src/assayingest/parsing/structure/unpivot.py | grep -cE 'import (pandas|openpyxl|anthropic)' returns 0.
  </acceptance_criteria>
  <done>A key-value or transposed grid becomes (headers, rows) deterministically, in Python, with zero silent data loss on duplicate labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: StructuralHint.layout + the serialisation round-trip (_jsonable's missing tuple branch)</name>
  <files>src/assayingest/parsing/hint.py, tests/test_structure_hint.py, tests/test_profile_store.py</files>
  <read_first>
    - src/assayingest/parsing/hint.py:47-61 (StructuralHint), :63-65 (to_dict), :108-116 (_jsonable — VERIFIED this session: it handles Enum/dict/list only; asdict() preserves a tuple of KeyValueBlock as a tuple of dicts, which falls through _jsonable's final return with any nested Enum UN-coerced — this is a real latent bug the round-trip test must catch red)
    - tests/test_structure_hint.py:17 (full-population round trip) and :36 (enum→string serialisation) — the two tests to EXTEND per RESEARCH's inventory (T7)
    - tests/test_profile_store.py:52-… (test_structural_hint_round_trips — hint persisted into saved learning profiles; the loader must tolerate a missing layout key in old rows)
  </read_first>
  <behavior>
    RED first:
    - A StructuralHint(layout=SheetLayout(kind=LayoutKind.KEY_VALUE, confidence=0.92, reasoning="...", key_value_blocks=(KeyValueBlock(0,(1,),1,10),))) .to_dict() is fully JSON-safe: json.dumps succeeds, kind serialises to its string value, blocks serialise to plain dicts/lists. (Expected to FAIL on the current _jsonable — the tuple branch is missing.)
    - The dict round-trips back to an equal StructuralHint via whatever from-dict path the profile store uses (extend the existing reconstruction to rebuild SheetLayout/KeyValueBlock from dicts).
    - tests/test_profile_store.py: a layout-carrying hint survives a full store save/load cycle; a stored profile dict WITHOUT a layout key loads as layout=None (no migration — old rows deserialise unchanged, per RESEARCH §Runtime State Inventory).
    - table_shape still round-trips (it stays: already serialised into saved profiles; removing it is a migration this phase refuses).
  </behavior>
  <action>
    Add layout: SheetLayout | None = None to StructuralHint (import from .structure.layout). Mark table_shape deprecated in the docstring (superseded by layout.kind; kept because it is persisted in saved profiles). Extend _jsonable with a tuple branch (coerce to list, recurse) so nested enums inside block tuples serialise. Extend the hint reconstruction path (wherever the profile store rebuilds a StructuralHint from JSON — grep for the call sites of StructuralHint( in learning/ and follow test_profile_store.py:52's existing round-trip) to rebuild layout when the key is present and default None when absent.

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_structure_hint.py tests/test_profile_store.py -q && uv run pytest tests -q -x</automated>
  </verify>
  <acceptance_criteria>
    - uv run pytest tests/test_structure_hint.py tests/test_profile_store.py -q passes, including the new layout round-trip and the missing-key tolerance test.
    - The FULL suite still passes (uv run pytest tests -q): Wave A adds, it does not break — 1132+ tests green, zero network calls (run with ANTHROPIC_API_KEY unset).
  </acceptance_criteria>
  <done>The hint can carry a verdict, the verdict survives the learning store's JSON round-trip, and old profiles still load.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored profile JSON → StructuralHint loader | Persisted data (SQLite/JSON) re-enters the domain on load |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | SheetLayout / KeyValueBlock field types | high | mitigate | The only-str-is-reasoning type-shape guard test: a future field that could carry a transcribed cell value fails CI (SHAPE-03 enforced structurally, Task 1) |
| T-12-02 | Information disclosure | unpivot output feeding downstream headers | medium | mitigate | Duplicate-label disambiguation prevents silent column loss that would mask a value under the wrong header (Task 2); indices outside declared blocks are never read |
| T-12-03 | Denial of service | Profile loader on malformed stored layout dict | low | accept | Loader defaults to layout=None on a missing key; malformed stored blocks surface at load in this developer-local store — index-range validation of CLIENT-posted layouts lands in plan 12-05 where the untrusted boundary is |
</threat_model>

<verification>
- uv run pytest tests/test_structure_layout.py tests/test_structure_unpivot.py tests/test_structure_hint.py tests/test_profile_store.py -q — all new/extended tests green.
- uv run pytest tests -q with ANTHROPIC_API_KEY unset — full suite green, zero network calls (D-12-07's offline property preserved).
</verification>

<success_criteria>
- The D-12-13 verdict contract exists verbatim as pure types; the purity and type-shape guards run in CI.
- unpivot_key_value reproduces RESEARCH's measured golden outputs on both driving sheets and handles the transposed melt and duplicate labels.
- StructuralHint.layout round-trips through the profile store; nothing existing broke.
</success_criteria>

<output>
Create `.planning/phases/12-claude-reads-the-structure/12-01-SUMMARY.md` when done. Record the actual _jsonable fix shape (the tuple branch) — plan 12-05's wire mapping reads it.
</output>
