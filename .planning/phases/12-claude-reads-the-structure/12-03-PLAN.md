---
phase: 12-claude-reads-the-structure
plan: 03
type: tdd
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/assayingest/parsing/table.py
  - tests/test_parse_entry_shape.py
autonomous: true
requirements: [SHAPE-02]

must_haves:
  truths:
    - "When a hint carries a KEY_VALUE verdict, parse() returns a real RawTable built by unpivot_key_value — a key-value sheet is READ, not refused (SHAPE-02)."
    - "When a hint carries a ROW_PER_RECORD verdict, parse() uses the verdict's header_row_index and first/last_data_row — trailing prose below a real table (the Quality Control case) no longer poisons the read."
    - "When a hint carries any other verdict kind (wide_matrix, multiple_tables, not_a_table, unknown), parse() returns a StructureQuestion with answerable_by_hint=True whose proposal carries the layout — the question is finally answerable (D-12-15)."
    - "When hint.layout is None, behaviour is byte-for-byte today's: the heuristic classifier still runs — Wave A adds a branch, it removes nothing, and the full suite stays green."
    - "The shape gate stays at its single choke point inside parse(); the un-pivoted table goes through the identical locale/date gates with no one-row special case (RESEARCH Pitfalls 1 and 5)."
  artifacts:
    - path: "src/assayingest/parsing/table.py"
      provides: "_table_from_layout dispatch + _raw_table_from_key_value + _shape_unknown_question"
      contains: "def _raw_table_from_key_value"
  key_links:
    - from: "src/assayingest/parsing/table.py"
      to: "src/assayingest/parsing/structure/unpivot.py"
      via: "_raw_table_from_key_value calls unpivot_key_value then the SAME _resolve_locales_or_ask gate as the header-row path"
      pattern: "unpivot_key_value"
---

<objective>
Wave A's parse-path dispatch: `_parse_excel_structurally` gains a branch — BEFORE the existing gate — that dispatches on `hint.layout` when a verdict is present. Key-value verdicts un-pivot into a legal `RawTable`; row-per-record verdicts use the verdict's row indices; everything else asks, answerably. When no verdict is present, nothing changes: the old classifier still runs, and all 1132 tests stay green.

Purpose: this is the half of "Claude judges; Python reads" (D-12-02) where Python reads. The verdict arrives as indices; every value in the output is read from the file by this code (SHAPE-03). The gate's location is deliberately untouched — `table.py:326-331` documents it as the single choke point covering both the auto-detected and explicit-hint paths (RESEARCH Pitfall 1: replace what FILLS it, never where it sits).

Output: the dispatch, the key-value assembly, the fail-closed unknown question — all TDD, exercised through `parse()` with hand-built hints, no Claude anywhere.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-claude-reads-the-structure/12-CONTEXT.md
@.planning/phases/12-claude-reads-the-structure/12-RESEARCH.md
@.planning/phases/12-claude-reads-the-structure/12-PATTERNS.md
@src/assayingest/parsing/table.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the layout dispatch — _table_from_layout, gate unmoved</name>
  <files>src/assayingest/parsing/table.py, tests/test_parse_entry_shape.py</files>
  <read_first>
    - src/assayingest/parsing/table.py:312-371 (_parse_excel_structurally — the function gaining the branch; its docstring at :326-331 states the single-choke-point rule this task must preserve; the dispatch site is :362-365), :467-503 (_raw_table_from_header_row — the row-per-record path the verdict re-parameterises)
    - tests/test_parse_entry_shape.py:44 and :53 (the zephyr/meridian RawTable controls — they stay green untouched in this wave because they pass no layout) and :62 (test_no_raw_table_construction_path_attaches_a_shape_field — KEEP VERBATIM; it must still hold once the un-pivot construction path exists)
    - .planning/phases/12-claude-reads-the-structure/12-CONTEXT.md D-12-15 (row_per_record reads the ordinary way and changes no value's meaning; every other verdict changes what a value IS and must be confirmed)
  </read_first>
  <behavior>
    RED first — extend tests/test_parse_entry_shape.py, driving parse() with hand-built StructuralHint(layout=...) hints on tmp_path workbooks and on the real cascade fixture:
    - hint.layout.kind == KEY_VALUE with blocks → parse() returns a RawTable whose headers are the labels (via Task 2's assembly). Golden: the real cascade "Patient Info" sheet with the D-12-13 blocks parses to a 17-header, 1-row RawTable.
    - hint.layout.kind == ROW_PER_RECORD with header_row_index → parse() returns a RawTable via the existing header-row path, header taken from the verdict, not re-detected. With first_data_row/last_data_row set, rows outside the range are excluded — build a tmp_path workbook shaped like the Quality Control case (a 3-row table, a blank row, then prose rows) and assert the prose never appears in the RawTable.
    - hint.layout.kind in {WIDE_MATRIX, MULTIPLE_TABLES, NOT_A_TABLE, UNKNOWN} → parse() returns a StructureQuestion with answerable_by_hint=True and proposal carrying the layout (Task 2's question builder).
    - hint.layout is None → the pre-existing behaviour, unchanged: assert by running the FULL existing test file untouched (the wave-A invariant is that no existing test moves).
    - A ROW_PER_RECORD verdict whose needs_confirmation is True still parses when explicitly passed as a hint — the hint path IS the confirmation; deciding when to ask lives in service (plan 12-05), not here.
  </behavior>
  <action>
    In _parse_excel_structurally, immediately before the existing header-detection/classifier flow, add: if hint is not None and hint.layout is not None, delegate to a new private _table_from_layout(path, rows, hint.layout, sheet_tag, hint, origin_sheet=...) — a named helper, not a fourth inline branch (single-level-of-abstraction: the function is already at its limit per 12-PATTERNS §4). _table_from_layout dispatches on layout.kind exactly as the behavior block states. The existing gate at :362-365 and _shape_unsupported_question stay byte-for-byte (they die in Wave C, not here).

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_parse_entry_shape.py -q && uv run pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - The new dispatch tests pass; tests/test_parse_entry_shape.py's pre-existing tests pass UNMODIFIED (git diff shows only additions to the test file in this task).
    - Full suite green (uv run pytest tests -q) — Wave A adds, nothing is removed or un-suppressed.
  </acceptance_criteria>
  <done>A verdict-carrying hint steers parse() deterministically; a verdict-less parse is exactly yesterday's parse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _raw_table_from_key_value + _shape_unknown_question — the assembly and the honest question</name>
  <files>src/assayingest/parsing/table.py, tests/test_parse_entry_shape.py</files>
  <read_first>
    - src/assayingest/parsing/table.py:467-503 (_raw_table_from_header_row — mirror it line for line: the sheet_tag/origin_sheet two-argument distinction explained at :483-486, and the _resolve_locales_or_ask gate at :493), :274-304 (why NEITHER branch may skip the locale gate — the 1000x corruption consequence at :298-304), :440-464 (_shape_unsupported_question — the question template: consequence-first reason, evidence_rows from the first 8 rows, confidence 0.0)
    - 12-RESEARCH.md §Friction (a) and (b) (the one-row locale/date bounce is CORRECT behaviour, not a bug — measured: neither driving sheet triggers it) and the _shape_unknown_question sketch in §The failure mode
  </read_first>
  <behavior>
    RED first:
    - _raw_table_from_key_value(path, rows, layout, sheet_tag, hint, *, origin_sheet) calls unpivot_key_value(rows, layout), then the SAME _resolve_locales_or_ask gate, and assembles a RawTable with the same field discipline as the header-row sibling (source_name, sheet_name, column_locales, origin_sheet).
    - The one-row locale bounce is NOT suppressed: a hand-built key-value grid whose single value is a text-stored "150,000" produces a StructureQuestion from the locale gate, not a guessed table (RESEARCH Pitfall 5 — the gate is the product; a lone ambiguous number genuinely cannot prove its own locale).
    - _shape_unknown_question(path, sheet_name, rows, proposal) returns a StructureQuestion with: a consequence-first reason (reading a labels-down-the-side sheet as a table would produce a clean-looking table with every field wrong — the RESEARCH sketch's wording), confidence 0.0, evidence_rows from the first 8 rows via _row_to_strings, answerable_by_hint=True (the inversion of today's dead end: hint.layout finally has a reader), and proposal carrying the judge's layout when one exists or None honestly when none does.
    - tests/test_parse_entry_shape.py:62's structural guard still passes: no RawTable construction path — including the new one — attaches a shape field to the table.
  </behavior>
  <action>
    Implement both helpers in table.py per the mirrors above. _raw_table_from_key_value's docstring cross-references its sibling and states why the locale gate is shared (cite the :274-277 rationale). _shape_unknown_question's docstring states how it differs from the unsupported-shape sibling: this one is answerable, because the layout hint now does something (D-12-15). Wire _table_from_layout's non-readable-kind arm and its KEY_VALUE arm to these helpers (completing Task 1's dispatch).

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_parse_entry_shape.py tests/test_hint_and_locale.py -q && uv run pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - The key-value golden (real Patient Info → 17-header RawTable through parse()), the locale-bounce test, and the answerable-question test all pass by name in uv run pytest tests/test_parse_entry_shape.py -q.
    - tests/test_hint_and_locale.py passes UNCHANGED in this wave (its :153 inversion belongs to Wave C when the old question dies — plan 12-07).
    - Full suite green.
  </acceptance_criteria>
  <done>A confirmed key-value verdict yields a real, locale-gated RawTable; an unknown shape yields a question a human can finally answer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hint (layout verdict) → parse() | The layout arrives from the judge OR from a client-posted hint; parse() must treat its indices as untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-08 | Tampering / DoS | _raw_table_from_key_value indexing rows with verdict indices | high | mitigate | unpivot reads only within declared block ranges (12-01); parse-side: guard block/row indices against len(rows)/row width before indexing — out-of-grid ⇒ _shape_unknown_question, never IndexError (the wire-boundary 422 for client posts lands in 12-05; this is the second, inner closure) |
| T-12-09 | Tampering | A layout hint bypassing the locale/date gates | high | mitigate | The un-pivoted table goes through the identical _resolve_locales_or_ask gate — asserted by the one-row bounce test; no confidence value exempts a value from a gate |
| T-12-10 | Elevation | A future caller reaching _raw_table_from_key_value without the choke point | medium | mitigate | The gate stays at its documented single choke point inside parse(); test_parse_entry_shape.py:62's structural guard kept verbatim |
</threat_model>

<verification>
- uv run pytest tests/test_parse_entry_shape.py -q — dispatch, key-value golden, row-range trim, answerable question, locale bounce all green.
- uv run pytest tests -q with ANTHROPIC_API_KEY unset — full suite green; git diff confirms no pre-existing test was modified in this plan.
</verification>

<success_criteria>
- parse() reads a key-value sheet into a real RawTable per a verdict, trims row ranges per a row-per-record verdict, and asks answerably otherwise.
- The verdict-less path is byte-for-byte unchanged; the shape gate never moved.
</success_criteria>

<output>
Create `.planning/phases/12-claude-reads-the-structure/12-03-SUMMARY.md` when done. Record the _table_from_layout dispatch signature — plans 12-04/12-05 route verdicts into it via the hint.
</output>
