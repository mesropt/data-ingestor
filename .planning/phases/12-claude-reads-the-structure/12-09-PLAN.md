---
phase: 12-claude-reads-the-structure
plan: 09
type: tdd
wave: 5
depends_on: ["12-03", "12-05"]
files_modified:
  - src/assayingest/cli.py
  - src/assayingest/parsing/table.py
  - src/assayingest/service.py
  - tests/test_cli_run.py
  - tests/test_hint_replay_cli.py
  - tests/test_parse_entry_shape.py
  - tests/test_sc5_row_per_record_parity.py
autonomous: true
requirements: [SHAPE-01, SHAPE-02]

must_haves:
  truths:
    - "The CLI HAS a judge. RESEARCH's four-path table promised 'CLI: the judge, or nothing' and only the 'nothing' half was planned — so after Wave C every .xlsx through the CLI, credentials or not, would die with a layout question, including every ordinary row-per-record file in data/synthetic/. With credentials the CLI now gets a real verdict; without, it fails closed to the answerable question."
    - "An explicit human header_row_index hint COUNTS AS a ROW_PER_RECORD confirmation. One rule in the parse dispatch. Without it the hint-replay loop never terminates (a replayed hint carries header_row_index but layout=None, and Wave C would fail it closed forever) — and it is what makes 12-05's 'One row per record' answer actually resolve."
    - "ROADMAP SC5 has a RUNNABLE test: a parametrized sweep over EVERY row-per-record file in data/synthetic/ asserts ingestion parity with today under a confident row_per_record judge. SC5 is a claim about every existing file; a single-workbook manifest assertion does not discharge it."
    - "The CLI's degradation ladder is the existing one: _enrich_question already calls Claude and already degrades on AuthenticationError / APIError / ValueError — the judge rides the same shape and the same three excepts, not a new one."
  artifacts:
    - path: "src/assayingest/cli.py"
      provides: "The CLI's judge call — credentials-gated, degrading to the answerable question exactly as _enrich_question already does"
      contains: "judge"
    - path: "tests/test_sc5_row_per_record_parity.py"
      provides: "The SC5 sweep: every row-per-record file in data/synthetic/ ingests exactly as it does today"
      contains: "parametrize"
  key_links:
    - from: "src/assayingest/parsing/table.py"
      to: "src/assayingest/parsing/structure/layout.py"
      via: "an explicit header_row_index hint is promoted to a ROW_PER_RECORD SheetLayout in the dispatch — the rule that terminates the hint-replay loop"
      pattern: "header_row_index"
---

<objective>
Give the CLI a verdict source, and give an explicit human header-row answer the standing it needs to terminate a parse. Then prove ROADMAP SC5 — "every existing row-per-record file in `data/synthetic/` still ingests exactly as it does today" — with a test that actually sweeps them, rather than a claim.

Purpose: the phase correctly decided every verdict-less `.xlsx` parse must fail closed (12-07), and then under-planned who supplies the verdict where one is needed. The API paths are covered by 12-04/12-05. The CLI is covered here, together with the one dispatch rule (`header_row_index` implies row-per-record) on which both the CLI's hint-replay loop and 12-05's "One row per record" answer depend.

Output: `cli.py`'s judge, the dispatch rule in `table.py`, and the SC5 parity sweep — all landing BEFORE Wave C removes the fallback, so the deletion lands on a codebase where every path already has an answer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-claude-reads-the-structure/12-CONTEXT.md
@.planning/phases/12-claude-reads-the-structure/12-RESEARCH.md
@.planning/phases/12-claude-reads-the-structure/12-03-SUMMARY.md
@.planning/phases/12-claude-reads-the-structure/12-05-SUMMARY.md
@src/assayingest/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: an explicit header_row_index IS a row_per_record confirmation (the rule that closes two loops)</name>
  <files>src/assayingest/parsing/table.py, tests/test_hint_replay_cli.py, tests/test_parse_entry_shape.py</files>
  <read_first>
    - .planning/phases/12-claude-reads-the-structure/12-03-SUMMARY.md (the _table_from_layout dispatch this rule extends)
    - tests/test_hint_replay_cli.py (the replay loop: a saved hint carries header_row_index and layout=None — post-Wave-C, without this rule, it would fail closed FOREVER and the replay feature would be dead)
    - src/assayingest/parsing/table.py (the dispatch from 12-03; the hint-override path the gate's docstring at :326-331 says the single choke point deliberately covers)
  </read_first>
  <behavior>
    RED first:
    - In the parse dispatch: when hint.layout is None AND hint.header_row_index is not None, the hint is promoted to SheetLayout(kind=ROW_PER_RECORD, header_row_index=hint.header_row_index, confidence=1.0, reasoning naming the source as the human's own answer) and parsed the ordinary way. A HUMAN naming the header row IS a human saying "this is an ordinary table, and here is where it starts" — the rule is a promotion of an explicit human answer, never an inference from absence.
    - It fires ONLY on an explicit human/replayed hint. A hint with neither layout nor header_row_index still fails closed (Wave C) — assert both branches so the rule cannot widen into "when in doubt, assume row_per_record", which would resurrect exactly the silent guess this phase deletes.
    - tests/test_hint_replay_cli.py: a replayed hint carrying only header_row_index terminates in a RawTable. This is the test that proves the loop closes.
  </behavior>
  <action>
    Implement the promotion inside the existing _table_from_layout dispatch (no new branch in _parse_excel_structurally — single level of abstraction). Docstring states the rule and its boundary: an explicit answer is a confirmation; an absent answer is not.

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_hint_replay_cli.py tests/test_parse_entry_shape.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The replayed-hint-terminates test and the still-fails-closed-without-either test both pass by name (the rule closes the loop without widening into a guess).
  </acceptance_criteria>
  <done>A human who names the header row has answered the layout question — and both the CLI replay loop and the UI's "One row per record" answer now terminate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the CLI's judge — the missing half of RESEARCH's four-path table</name>
  <files>src/assayingest/cli.py, src/assayingest/service.py, tests/test_cli_run.py</files>
  <read_first>
    - src/assayingest/cli.py:376-410 (_enrich_question and its call site — it ALREADY calls Claude for a structural proposal and ALREADY degrades to the un-enriched question on AuthenticationError / APIError / ValueError at :401-404; the judge rides the same shape and the same three excepts), :383-384 (the headers-only skip of the ENRICHMENT call — untouched; D-12-05's redacted call lives on the judge path, and tests/test_headers_only.py:204 keeps passing)
    - src/assayingest/service.py resolve_table_mapping (the CLI's entry into the resolve path — the judge_fn seam from 12-05 threads through here)
    - src/assayingest/cli.py exit-code contract (exit 4 = structural question) — the fail-closed outcome when there are no credentials
  </read_first>
  <behavior>
    RED first — offline, with an injected judge:
    - With a judge available (credentials, or an injected fake), the CLI judges an .xlsx and proceeds: an ordinary row-per-record file ingests with no question and identical output to today. This is the test that would have caught B2 — without it, every CLI .xlsx run dies at Wave C.
    - Without credentials, the CLI fails closed: the answerable layout question is printed and the structural-question exit code is returned — never a guessed table, never a crash. (The credential-less path is what most of the suite runs on; pin it explicitly.)
    - Judge failure (auth error, API error, malformed response) degrades identically — the same three excepts as _enrich_question, no new failure vocabulary. Pin with a raising fake.
    - A key-value .xlsx through the CLI with a judge yields the answerable layout question with Claude's read pre-filled (the CLI's half of the propose-never-decide contract).
  </behavior>
  <action>
    Thread the judge into the CLI's resolve path via 12-05's judge_fn seam, gated on has_credentials() exactly as the mapper call already is. Keep the CLI's degradation ladder as-is and reuse it — do not invent a second one. Where the judge and _enrich_question would both call Claude for the same file, the judge's verdict is what pre-fills the question's proposal (the enrichment path stays for the non-layout structural questions it already serves).

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli_run.py tests/test_cli.py tests/test_headers_only.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The with-judge CLI ingest test, the no-credentials fail-closed exit-code test, and the judge-failure degradation test all pass by name.
    - tests/test_headers_only.py passes unchanged (the CLI's headers-only enrichment skip is untouched).
  </acceptance_criteria>
  <done>The CLI has a verdict source with credentials and an honest question without one — the half of the four-path table that was promised and unplanned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the SC5 sweep — every row-per-record file in data/synthetic/, or the criterion is a claim</name>
  <files>tests/test_sc5_row_per_record_parity.py</files>
  <read_first>
    - .planning/ROADMAP.md Phase 12 Success Criterion 5 ("Every existing row-per-record file in data/synthetic/ still ingests exactly as it does today — removing the Python classifier regresses nothing")
    - tests/test_real_corpus.py (the corpus-sweep idiom to copy — note it tolerates a StructureQuestion, which is why it does NOT discharge SC5 on its own)
    - data/synthetic/ (the fixture list: the ordinary row-per-record .xlsx files, excluding the known shape fixtures apex_labs_wide_matrix / bionexus_transposed / triton_screening_two_tables / quantex_scanned_report / nimbus_labs_chartsheet, and excluding CSVs, which never had a shape gate — D-12-18)
  </read_first>
  <behavior>
    - A parametrized sweep over every row-per-record .xlsx in data/synthetic/: under a confident row_per_record judge fake, each parses to a RawTable whose headers and row_count are IDENTICAL to today's (capture today's values first, on the pre-change code, and pin them as the expected data — a parity test written after the change can only prove self-consistency).
    - The excluded shape fixtures are excluded BY NAME with a one-line reason each, so the exclusion list is a stated scope, not a silent omission — and each excluded file gets its expected non-row_per_record verdict asserted instead (it is refused or un-pivoted, never silently ingested).
    - The sweep runs offline (judge fake) and is part of the default suite — SC5 is a standing regression guard, not a one-off check.
  </behavior>
  <action>
    Capture the baseline BEFORE the Wave-C deletion lands (run the sweep against current main to record headers/row_count per file), commit the baseline as the test's expected data with a comment naming when and how it was captured, then make the sweep green under the judge fake. If any file's post-change output differs from the baseline, that is a REGRESSION and the plan stops — not a baseline to update.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sc5_row_per_record_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The sweep passes with one parametrized case per row-per-record file in data/synthetic/ (count asserted, so a newly added fixture cannot silently escape the sweep).
    - The excluded-fixtures assertions pass: each is refused or un-pivoted per its verdict, never silently ingested.
  </acceptance_criteria>
  <done>SC5 is a runnable, standing guard over every file it claims to be about.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human hint (header_row_index) → parse() | An explicit human answer is promoted to a layout confirmation; absence must never be |
| CLI → Anthropic API | The CLI's judge call — a credential-gated availability boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-28 | Elevation | The header_row_index promotion widening into "when in doubt, row_per_record" | high | mitigate | The rule fires ONLY on an explicit hint; the fails-closed-without-either test pins the boundary (Task 1) — the silent guess this phase deletes must not re-enter through a convenience rule |
| T-12-29 | Denial of service | The CLI unusable on every .xlsx after Wave C | high | mitigate | Task 2 gives the CLI a judge; Task 3's SC5 sweep is the standing proof that no existing file regressed |
| T-12-30 | Information disclosure | The CLI's judge call under headers_only | medium | mitigate | The judge uses render_evidence_grid's required headers_only kwarg (12-02); the CLI's existing enrichment skip is untouched and tests/test_headers_only.py:204 still passes |
</threat_model>

<verification>
- uv run pytest tests/test_cli_run.py tests/test_hint_replay_cli.py tests/test_sc5_row_per_record_parity.py -q — CLI judge, replay-loop termination, SC5 parity all green.
- uv run pytest tests -q with ANTHROPIC_API_KEY unset — full suite green, zero network calls.
</verification>

<success_criteria>
- Every one of RESEARCH's four production paths now has a named verdict source: multi-sheet (12-04), single-sheet API (12-05), hint resolve (12-05), CLI (here).
- The hint-replay loop and the "One row per record" answer both terminate.
- ROADMAP SC5 is discharged by a parametrized sweep with a pre-change baseline.
</success_criteria>

<output>
Create `.planning/phases/12-claude-reads-the-structure/12-09-SUMMARY.md` when done. Record the SC5 baseline capture method — 12-07's deletion runs against it.
</output>
