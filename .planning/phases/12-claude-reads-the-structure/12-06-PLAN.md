---
phase: 12-claude-reads-the-structure
plan: 06
type: tdd
wave: 5
depends_on: ["12-05"]
files_modified:
  - frontend/src/lib/types.ts
  - frontend/src/state/sheets.ts
  - frontend/src/state/sheets.test.ts
  - frontend/src/components/SheetQuestionPanel.tsx
  - frontend/src/components/StructuralHintPanel.tsx
autonomous: true
requirements: [SHAPE-01, SHAPE-02]

must_haves:
  truths:
    - "isUnreadableShape is true for layout.kind in {not_a_table, wide_matrix, multiple_tables} — NEVER for key_value (the phase's point) and never for unknown (answerable, not unreadable — collapsing them would rebuild the dead end D-12-15 just killed)."
    - "A key_value sheet renders readable and tickable: labels as mono chips under the caption 'Detected labels', a Schema select, a secondary 'labels down the side' badge — and a cell value never renders as a column header (D-12-12's UI half)."
    - "Every judged sheet's layout line shows Claude's reasoning and confidence — the tool never presents a claim the human cannot check — and carries the 'Not right? Answer the layout yourself' disagree action routing to the ONE answer surface (12-UI-SPEC Discretion §2)."
    - "The StructuralHintPanel offers the key-value option ONLY when the proposal carries blocks (a kind without indices is un-actionable), pre-selected when it does; 'One row per record' is always offered and reveals the existing header-row control."
    - "Amber renders ONLY off the server-sent needs_confirmation flag or the unknown/all-unknown states — the client renders gates, it does not set them."
  artifacts:
    - path: "frontend/src/state/sheets.ts"
      provides: "Verdict-aware derivations: isUnreadableShape re-keyed, layout-line copy builder, all-unknown detector, ask-me-instead flag — all in the pure module"
      contains: "layout"
    - path: "frontend/src/state/sheets.test.ts"
      provides: "Gate tests for every LayoutKind x status combination, written red-first against the pure module"
      contains: "key_value"
  key_links:
    - from: "frontend/src/lib/types.ts SheetOut"
      to: "src/assayingest/api/wire.py SheetOut"
      via: "layout: SheetLayoutOut | null and the widened status union — mirrored verbatim from 12-05's recorded wire shape"
      pattern: "SheetLayoutOut"
---

<objective>
The two — and only two — UI surfaces this phase touches, per the binding 12-UI-SPEC: the sheet-selection panel gains the per-sheet layout verdict (line, badges, labels caption, disagree action, all-unknown notice), and the structural-hint panel becomes actually answerable for the first time (verdict block + two layout options). No new screens, no new primitives, no new installs, zero new hex values or icons.

Purpose: D-12-08 makes the human the ONLY check on the judge — so the checking material (reasoning, confidence, the labels the verdict produces) must be on the screen where confirmation happens. Ticking IS confirming (D-12-02).

Output: types mirrored from the wire, the pure state module TDD'd first, both panels extended per the Copywriting Contract verbatim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-claude-reads-the-structure/12-UI-SPEC.md
@.planning/phases/12-claude-reads-the-structure/12-CONTEXT.md
@.planning/phases/12-claude-reads-the-structure/12-05-SUMMARY.md
@frontend/src/state/sheets.ts
@frontend/src/components/SheetQuestionPanel.tsx
@frontend/src/components/StructuralHintPanel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: types.ts + state/sheets.ts — the pure derivations, red first</name>
  <files>frontend/src/lib/types.ts, frontend/src/state/sheets.ts, frontend/src/state/sheets.test.ts</files>
  <read_first>
    - .planning/phases/12-claude-reads-the-structure/12-05-SUMMARY.md (the recorded wire field names — mirror them verbatim; do not invent)
    - frontend/src/state/sheets.ts:1-26 (the module's own render-only contract), :53-55 (_isPreTicked), :67-69 (isUnreadableShape), :79-91 (UNREADABLE_SHAPE_LINE + showsDetectedHeaders — both doc comments must be REWRITTEN to the verdict story, not deleted or stranded: :79-80's "PARSE-V2-01, deliberately not v1" sentence and :82-88's TAYLOR-James second-lock story are now history), :97-99 (showsSchemaSelect), :136-146 (submitBlockedReason — unchanged)
    - frontend/src/state/sheets.test.ts (the sheet({...}) factory style near :132/:187 — keep it)
    - 12-UI-SPEC.md Discretion §1 (the binding derivation table) and the Copywriting Contract (every layout-line and badge string, verbatim)
  </read_first>
  <behavior>
    RED first — sheets.test.ts with the factory:
    - types.ts: SheetOut gains layout: SheetLayoutOut | null ({ kind, confidence, reasoning, record_count, needs_confirmation }); status union gains "layout_unknown". Doc comment cites the backing wire.py class by name (the file's existing habit).
    - isUnreadableShape(sheet): true iff layout.kind is one of not_a_table / wide_matrix / multiple_tables; false for key_value, unknown, row_per_record, and null layout.
    - showsDetectedHeaders: true for key_value (chips = labels, caption "Detected labels"); follows the unreadable predicate otherwise as today.
    - showsSchemaSelect: true for key_value AND for layout unknown (an unknown sheet the human ticks still needs its Schema chosen now — mapping runs after its layout question resolves); false for unreadable kinds.
    - _isPreTicked rule unchanged (status ok + proposals exist): a key_value sheet with matched labels arrives ticked; an unknown sheet arrives unticked.
    - layoutLine(sheet): the exact Copywriting Contract strings per kind, including the N-records variant off record_count and the unknown line with no confidence figure (printing zero-percent confidence would be theatre); amber keyed ONLY off the server-sent needs_confirmation or kind unknown.
    - allUnknown(sheets): true only when EVERY sheet's layout kind is unknown — drives the single workbook-level amber notice.
    - Per-kind refusal lines replace UNREADABLE_SHAPE_LINE per the Copywriting Contract (not_a_table vs matrix/multiple-tables wording).
    - askLayout choice flag: a pure state addition mirroring the existing per-sheet choice fields, carried into the resolve payload under 12-05's recorded field name.
  </behavior>
  <action>
    Extend types.ts and the pure module; every new derivation lives in sheets.ts with the panel staying render-only (the module's own stated contract). Rewrite — do not delete — the two doc comments whose stories this phase closes. Copy strings from the Copywriting Contract character-for-character; they are contract, not suggestion.

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/state/sheets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every LayoutKind renders through the derivations with the contracted copy; the key_value-is-never-unreadable and unknown-is-not-unreadable tests pass by name.
    - cd frontend && npx tsc --noEmit exits 0 (or the project's build check if no tsc script — verify with npm run).
  </acceptance_criteria>
  <done>The gates and copy are pure, tested, and mirror the wire — before a pixel changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SheetQuestionPanel — the layout line, badges, labels, disagree action, all-unknown notice</name>
  <files>frontend/src/components/SheetQuestionPanel.tsx, frontend/src/state/sheets.test.ts</files>
  <read_first>
    - frontend/src/components/SheetQuestionPanel.tsx:43-66 (StatusBadge — the switch to extend; its doc comment's muted-fact vs amber-act-on-this distinction is the rule for the new badges), :95-146 (the card anatomy — the layout line inserts as item 2, per 12-UI-SPEC §Screens 1), :127-138 (the header-chip idiom the label chips reuse)
    - 12-UI-SPEC.md §Screens 1 (the six card states) and §Color / §Typography (verdict kind phrase .text-label-strong muted; reasoning .text-label; chips/confidence mono tabular-nums; NOTHING new gets accent — a verdict is a proposal to check, not an achievement to celebrate)
    - 12-UI-SPEC.md Design System row "Component library": this project uses @base-ui/react, NOT Radix — the mount-persistence prop is keepMounted; writing forceMount means the wrong mental model was imported (the Phase-11 incident this phase's plans must not repeat)
  </read_first>
  <behavior>
    Extend the pure-module tests where logic lives; the panel work is rendering:
    - Card anatomy order per the spec: selection row → layout line (NEW) → detected headers/labels chips → proposal + coverage → Schema select.
    - Badges per contract: "labels down the side" (secondary), "layout unknown" (amber — the header-unclear badge's exact classes), "not a table" / "can't read this layout yet" (muted outline).
    - The disagree text button ("Not right? Answer the layout yourself") on EVERY judged sheet's layout line — including a confident row_per_record (under headers_only the judge is provably weakest exactly where a wrong row_per_record re-opens the leak; the human is the only check, D-12-08). Engaged state swaps the line for the engaged copy with the undo action ("Use Claude's read instead"). A real button element with visible text, 32px hit area, never icon-only.
    - The all-unknown state: one workbook-level amber notice, eight quiet cards — no per-card repetition of the judge-failure story.
    - Unreadable kinds: refusal line replaces the middle blocks (today's unsupported-shape rendering re-pointed).
  </behavior>
  <action>
    Extend SheetCard/StatusBadge per the spec. All copy comes from Task 1's builders — no strings composed in the component. No new icons, no new spacing tokens, accent untouched (existing CTA + checked Checkbox only).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Full frontend suite green; typecheck clean.
    - grep -rn "forceMount" frontend/src/components/SheetQuestionPanel.tsx frontend/src/components/StructuralHintPanel.tsx returns nothing — AND the vitest suite exercising the panels passes (the grep alone proved nothing in Phase 11; the running suite is the gate).
  </acceptance_criteria>
  <done>The sheet screen shows every verdict as a checkable, disagreeable proposal, in the contract's exact words.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: StructuralHintPanel — the one answer surface, upgraded once (12-UI-SPEC Discretion §3)</name>
  <files>frontend/src/components/StructuralHintPanel.tsx, frontend/src/state/sheets.test.ts</files>
  <read_first>
    - frontend/src/components/StructuralHintPanel.tsx:36-64 (the derive-controls-from-non-null-proposal-fields pattern — the layout control follows it exactly, never keyed off unsure_about's free text), :48-53 (the headers-only evidence suppression — keep working), :99-155 (the evidence grid + header-row control the "one row per record" answer reveals), :134-138 (the !answerable_by_hint dead-end branch — becomes legacy; KEEP it so a stale response cannot crash, write no new copy for it), :211-214 (the CTA)
    - 12-UI-SPEC.md Copywriting Contract (hint-panel rows) and Discretion §3-4 (verdict block, the two options, the honest limit line, labels shown under headers-only WITH the acknowledged residual)
  </read_first>
  <behavior>
    - Verdict block first: Claude's read per contract, "Labels found:" mono chips when key-value — the chips ARE the checking material (labels reading as field names is the verdict being right; a name among them is it being wrong; this is how the curator checks without opening Excel).
    - "How is this sheet laid out?" — the DateFormatQuestionPanel two-option idiom: "Labels down the side — {n} record(s)" ONLY when the proposal carries blocks, pre-selected when present; "One row per record" always offered, revealing the existing header-row number input and clickable grid rows pre-filled from layout.header_row_index.
    - Selecting labels-down-the-side submits confirmation of Claude's blocks via the hint's layout field (12-05's wire shape) — never new indices; the grid row-click still sets ONLY header_row_index.
    - No key-value proposal (unknown/unreachable): only "One row per record" plus the honest limit line from the contract.
    - Submit disabled until an option is chosen, with the blocked line; low-confidence row_per_record arrives with "One row per record" pre-selected and the header row pre-filled (the off-by-one fix path).
    - Headers-only: evidence grid stays value-suppressed as today; verdict + labels still show (Discretion §4's ruling, with its residual acknowledged in the spec, not re-litigated here).
  </behavior>
  <action>
    Extend the panel per Discretion §3, md rhythm, selected-option treatment mirroring the existing bg-primary/15 idiom. A confident row_per_record never reaches this panel (D-12-15) — do not add a rendering branch for it.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run && npm run build</automated>
    <human-check>Upload data/synthetic/lab_corpus/cascade_allergy_CS-2026-698392.xlsx in the running app: Summary and Patient Info show "labels down the side" with label chips (no patient name as a header anywhere); tick Patient Info and confirm in Review via the hint panel; the sheet maps as a 17-column dataset. Then re-upload with headers only ON and confirm the panel still shows the verdict and labels but no cell values in the evidence grid. (end-of-phase check)</human-check>
  </verify>
  <acceptance_criteria>
    - Frontend suite + production build green.
    - The pure-module tests for option availability (key-value option present iff blocks exist; pre-selection rules) pass by name.
  </acceptance_criteria>
  <done>One answer surface, reached from both paths, that lets the human confirm, correct to ordinary-table, or honestly stop — never silently guess.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → browser | Verdict metadata (kind, reasoning, confidence, labels-as-headers) renders to the curator |
| Curator → server | The confirmation click and the ask_layout disagree flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-19 | Information disclosure | A cell value rendered as a header/label chip under a wrong verdict | medium | mitigate | Backend suppression is the primary control (12-04); UI: the disagree affordance on EVERY judged sheet + reasoning/confidence rendered so a wrong verdict is detectable at a glance (the UI-SPEC's acknowledged residual, with its rationale) |
| T-12-20 | Spoofing | Client-side confidence thresholds overriding server gates | low | mitigate | Amber keys ONLY off server-sent needs_confirmation; pinned by a pure-module test (the client renders gates, it does not set them) |
| T-12-21 | Tampering | Rejecting a verdict falling back to a silent guess | medium | mitigate | Rejection routes to the hint panel or a skip — no rejection path constructs a table; pinned by the option-availability tests (a kind without indices is never offered) |
</threat_model>

<verification>
- cd frontend && npx vitest run — full frontend suite green.
- cd frontend && npx tsc --noEmit && npm run build — types and production build clean.
- Human check (end-of-phase): the Task 3 human-check walkthrough on the running app.
</verification>

<success_criteria>
- Both surfaces render every verdict per the Copywriting Contract verbatim; key_value sheets are first-class readable citizens; unknown is a designed state, not an error toast.
- Disagreement always lands on the one answer surface; nothing new got accent, no new installs, keepMounted throughout.
</success_criteria>

<output>
Create `.planning/phases/12-claude-reads-the-structure/12-06-SUMMARY.md` when done.
</output>
