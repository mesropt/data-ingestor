---
phase: 12-claude-reads-the-structure
plan: 05
type: tdd
wave: 4
depends_on: ["12-04"]
files_modified:
  - src/assayingest/service.py
  - src/assayingest/api/wire.py
  - src/assayingest/api/routes/upload.py
  - src/assayingest/api/routes/sheets.py
  - src/assayingest/api/routes/structural_hint.py
  - tests/api/test_upload.py
  - tests/api/test_sheets_route.py
  - tests/api/test_group_export.py
  - tests/api/test_structural_hint_context.py
autonomous: true
requirements: [SHAPE-02, SHAPE-04]

must_haves:
  truths:
    - "UNKNOWN ATTACHES (the decision with the largest blast radius in this phase): _resolve_one_sheet attaches the retained UNKNOWN layout to the hint — it does NOT drop it to None. Post-Wave-C both routes end at the same question, but the attached verdict carries the REASON the human is being asked, whereas a None that silently means 'ask' is precisely the write-only dead end this phase exists to kill."
    - "A confident row_per_record verdict proceeds with NO question — exactly today's behaviour; EVERY other verdict (key_value, wide_matrix, multiple_tables, not_a_table, unknown, low-confidence row_per_record) returns a StructureQuestion with answerable_by_hint=True. The principle: row_per_record changes no value's meaning; every other verdict changes what a value IS, and must be confirmed by a human (D-12-15)."
    - "BOTH human answers round-trip, not just one: selecting 'Labels down the side' posts layout={kind: key_value, blocks: ...} (confirmation of Claude's blocks); selecting 'One row per record' posts layout={kind: row_per_record, header_row_index: N}. Each has its own end-to-end test ending in a mapped dataset — an unanswerable loop on the surface D-12-15's whole principle rests on would be the worst possible bug here."
    - "The privacy proof exists at the HTTP boundary: a fake client at the get_anthropic_client DI seam captures the ACTUAL outbound request through the real /api/upload → describe_workbook → judge_workbook_layout chain; with headers_only=true the real cell-value literals are ABSENT — and the MIRROR test with headers_only=false asserts they ARE present (D-12-09 makes presence a requirement, not an accident)."
    - "A client-posted layout with an out-of-grid index is a 422 naming the consequence at the wire boundary — never an IndexError dressed as a 500 (ASVS V5; the WR-02 bug class, fixed once, not reintroduced)."
    - "The verdict rides the retained SheetManifestEntry to resolve time: ticking a sheet on the sheet screen IS the confirmation, and resolving it costs ZERO extra Claude calls (D-12-02/D-12-14)."
    - "The sheet screen's disagree path exists on the wire: a per-selection ask_layout flag routes that member to the layout question in Review instead of applying the verdict (12-UI-SPEC Discretion §2 — the planner-named field)."
  artifacts:
    - path: "src/assayingest/api/wire.py"
      provides: "SheetOut.layout (SheetLayoutOut: kind/confidence/reasoning/record_count/needs_confirmation — no indices), status Literal + layout_unknown; StructuralHintIn.layout with index validation"
      contains: "class SheetLayoutOut"
    - path: "tests/api/test_upload.py"
      provides: "The SHAPE-04 captured-outbound pair (absent under headers_only, present by default)"
      contains: "headers_only"
  key_links:
    - from: "src/assayingest/api/routes/sheets.py _resolve_one_sheet"
      to: "src/assayingest/service.py resolve_or_map"
      via: "hint=StructuralHint(layout=entry.layout) from the server-retained manifest — never from the client"
      pattern: "layout"
    - from: "src/assayingest/api/routes/structural_hint.py _to_domain_hint"
      to: "src/assayingest/parsing/structure/layout.py"
      via: "the human's confirmed layout re-enters as a validated hint — the round trip that makes answerable_by_hint=True true"
      pattern: "SheetLayout"
---

<objective>
Wire the verdict through the API: the single-sheet path judges in `resolve_or_map` (row_per_record as the null hypothesis, D-12-15), the multi-sheet path rides the retained manifest, the human's answer round-trips through `StructuralHintIn.layout` — and SHAPE-04 is proven where it counts, at the HTTP boundary, in both directions.

Purpose: after 12-04 the verdict exists server-side; this plan makes it reach parse time on every production path and makes the privacy guarantee a captured-outbound fact rather than a promise. `_asks_which_sheets` stays `> 1` — D-12-15 explicitly rejected forcing a sheet screen on single-sheet uploads.

Output: `resolve_or_map(judge_fn=...)`, the wire models, the 422 boundary for client-posted indices, the `ask_layout` disagree flag, and the two privacy tests copied from the strongest existing precedent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-claude-reads-the-structure/12-CONTEXT.md
@.planning/phases/12-claude-reads-the-structure/12-RESEARCH.md
@.planning/phases/12-claude-reads-the-structure/12-PATTERNS.md
@.planning/phases/12-claude-reads-the-structure/12-04-SUMMARY.md
@src/assayingest/api/wire.py
@src/assayingest/api/routes/structural_hint.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolve_or_map judges the single-sheet path — row_per_record is the null hypothesis (D-12-15)</name>
  <files>src/assayingest/service.py, src/assayingest/api/routes/upload.py, src/assayingest/api/routes/sheets.py, tests/api/test_upload.py, tests/api/test_sheets_route.py, tests/api/test_group_export.py</files>
  <read_first>
    - src/assayingest/service.py:296-364 (resolve_or_map — it already has headers_only and client; the propose_mapping_fn seam idiom at :251-258/:306 is the mold for judge_fn)
    - src/assayingest/api/routes/upload.py:247-272 (_asks_which_sheets — UNCHANGED: D-12-15 rejects >= 1; tests/api/test_sheets_route.py:554 test_an_explicit_sheet_bypasses_the_question_entirely must stay green) and :275-315 (_sheet_question — the describe_workbook call at :305-307 gains headers_only, already in scope at :279 and currently unforwarded)
    - src/assayingest/api/routes/sheets.py:175-219 (_resolve_one_sheet — where the retained entry's layout becomes the hint; the exception ladder at :201-211 is the route-level error idiom)
    - .planning/phases/12-claude-reads-the-structure/12-04-SUMMARY.md (the judging_client fixture — THE lever for every API test here: get_anthropic_client -> lambda: None short-circuits _judge_for before any seam, so an HTTP test cannot reach judge_fn; consume the shared fixture, do not improvise a second fake)
    - .planning/phases/12-claude-reads-the-structure/12-CONTEXT.md D-12-15 (the full ruling, including why answerable_by_hint=True finally means something)
  </read_first>
  <behavior>
    RED first — offline; judge_fn fakes at the service level, the shared judging_client fixture at the HTTP level:
    - resolve_or_map gains judge_fn=None (keyword-only, defaulted — every existing call site unchanged). When the path is .xlsx AND hint (or hint.layout) is None AND a judge is available (_judge_for(client, judge_fn)): judge the workbook's target sheet once. A ROW_PER_RECORD verdict with needs_confirmation False → proceed silently, the verdict riding the hint into parse (identical UX to today, where the classifier auto-applied its own row_per_record with no human in the loop). ANY other verdict, and a low-confidence ROW_PER_RECORD → return the StructureQuestion from parse-time dispatch with proposal carrying the layout, answerable_by_hint=True.
    - Judge unavailable (no client, no judge_fn) → hint.layout stays None → parse falls through to the classifier fallback, exactly today's behaviour in this wave (the fail-closed switch is Wave C).
    - UNKNOWN ATTACHES: _resolve_one_sheet builds hint=StructuralHint(layout=entry.layout, ...) even when entry.layout.kind is UNKNOWN — it never drops the verdict to None. A named test asserts the resulting question carries the verdict's reasoning (the human is told WHY they are being asked). Rationale in the code comment: post-Wave-C a None and an UNKNOWN both end at the same question, but only one of them can say why — and a None that silently means "ask" is the write-only dead end this phase exists to kill.
    - Zero-extra-calls proof: on the multi-sheet resolve path, a judge_fn that raises AssertionError if touched proves resolving ticked sheets makes NO judge call — the verdict rides the retained SheetManifestEntry.layout into resolve_or_map's hint (copy the explode-fake idiom from tests/test_describe_workbook.py:200-211).
    - upload.py forwards headers_only into describe_workbook; a recorder judge_fn on the real route asserts it arrives.
    - ask_layout: a per-selection boolean (default false) on the sheets-resolve wire model. When true, that member does NOT apply the retained verdict — it returns the layout StructureQuestion for that member (proposal = the retained verdict, so the panel can render Claude's read), surfacing in its Review tab (the Phase 11 recursive-arm machinery, reused).
    - COLLATERAL MIGRATION (enumerated, not discovered): the ~18 resolve/ingestion-subject tests in tests/api/test_sheets_route.py and the 5 in tests/api/test_group_export.py now need a verdict to produce a dataset — migrate each onto 12-04's judging_client fixture. Tests whose subject IS the no-judge path keep the lambda: None override. Neither file may be left to Wave C: this is the plan that makes them need a verdict.
  </behavior>
  <action>
    Implement the seam, the null-hypothesis rule, and the UNKNOWN-attaches rule in resolve_or_map / _resolve_one_sheet, each with the stated rationale in a comment. Add ask_layout to the selection wire model and thread it. Rewrite tests/api/test_sheets_route.py:335-352 and :356-381 (retarget the hand-built unsupported-shape entries to a NOT_A_TABLE-backed entry) and ADD the third: a key_value sheet KEEPS its Schema control and its label headers. Then migrate the enumerated collateral onto judging_client, running uv run pytest tests/api -q between files so a surprise surfaces immediately rather than at the end.

    Commit RED then GREEN, then the collateral migration as its own reviewable commit.
  </action>
  <verify>
    <automated>uv run pytest tests/api/test_sheets_route.py tests/api/test_upload.py tests/api/test_group_export.py -q && uv run pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - The explode-fake zero-extra-calls test, the confident-row_per_record-proceeds test, the every-other-verdict-asks test, the UNKNOWN-attaches-with-reasoning test, and the ask_layout round-trip test pass by name.
    - tests/api/test_sheets_route.py:554 (explicit sheet bypasses the question) passes UNCHANGED.
    - Full suite green offline with the collateral in tests/api/test_sheets_route.py and tests/api/test_group_export.py migrated onto judging_client — both files are listed in this plan's files_modified, so this criterion is achievable with the files this plan actually owns.
  </acceptance_criteria>
  <done>Every production path gets its verdict from the right place, the 90% case stays friction-free, and disagreement routes to the one answer surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the wire — SheetOut.layout out, StructuralHintIn.layout in, 422 on bad indices (ASVS V5)</name>
  <files>src/assayingest/api/wire.py, src/assayingest/api/routes/structural_hint.py, tests/api/test_structural_hint_context.py, tests/api/test_sheets_route.py</files>
  <read_first>
    - src/assayingest/api/wire.py:578-611 (SheetOut — the status Literal at :608 to widen with "layout_unknown"; the docstring habit at :582-587 of stating the redaction posture out loud), :371-381 (StructuralHintIn — "mirroring the domain dataclass exactly" per its own docstring), :426-427 and :497-499 (the 422-at-the-boundary Literal idiom: "never a silent mis-apply")
    - src/assayingest/api/routes/structural_hint.py:180-188 (_to_domain_hint — the wire→domain mapping to extend) and :79-95 (the route exception ladder with the temp-file-cleanup rationale)
    - src/assayingest/api/routes/sheets.py:121-162 (_validated_selections — the all-or-nothing posture and the consequence-first "Nothing was ingested:" detail shape to copy)
    - 12-UI-SPEC.md Discretion §1 (SheetLayoutOut carries kind/confidence/reasoning/record_count/needs_confirmation — indices stay server-side: the browser never needs them, which is also the smallest untrusted-input surface)
  </read_first>
  <behavior>
    RED first:
    - SheetOut gains layout: SheetLayoutOut | None (kind, confidence, reasoning, record_count, needs_confirmation — NO index fields on the outbound model). status Literal gains "layout_unknown" and keeps "unsupported_shape" (gate semantics; the layout kind travels in layout.kind — RESEARCH Pitfall 7).
    - StructuralHintIn gains layout: a nested wire model mirroring SheetLayout INCLUDING indices (the human's confirmation posts Claude's blocks back — 12-UI-SPEC Discretion §3: selecting labels-down-the-side submits confirmation of the blocks, not new indices; the row-click still sets only header_row_index).
    - Validation, each as a named test: kind outside the LayoutKind values → 422 (Literal at the boundary); label_column/value_columns/first_row/last_row/header_row_index negative or beyond the grid of the re-parsed file → 422 whose detail names the consequence ("Nothing was ingested: ..." shape), never an IndexError→500; a huge last_row must not materialise a giant list (the un-pivot slices within the real grid — assert response time-safety by bounding to actual row count before any allocation).
    - Round trip A (key-value): POST the layout question's confirmed key-value answer to the structural-hint resolve route with the cascade fixture → the response is a mapped 17-column dataset (the full SHAPE-02 loop: judge → question → human confirms → un-pivot → mapper), with the mapper behind the existing propose_mapping_fn/DI fake.
    - Round trip B (ordinary table) — EQUALLY LOAD-BEARING, and the one that closes D-12-15's loop: selecting "One row per record" posts layout={kind: "row_per_record", header_row_index: N}. Test: an unknown-layout question → the human answers row-per-record with a header row → a real RawTable → a mapped dataset. Without this, the resolve re-parse would receive layout=None, Wave C would return the SAME layout question, and the human would be stuck in an unanswerable loop on the exact surface D-12-15's whole principle rests on. (The parse-dispatch rule that makes an explicit header_row_index count as a row_per_record confirmation lands in plan 12-09; this test is its pin at the HTTP boundary — assert BOTH the explicit-layout form and the bare-header_row_index form terminate in a dataset.)
    - needs_confirmation on SheetLayoutOut is the SERVER-computed flag (12-01's property) — a test pins that the client never receives raw confidence without the gate flag beside it (the client renders gates, it does not set them).
  </behavior>
  <action>
    Extend wire.py and _to_domain_hint per the molds. Index validation lives at the wire→domain boundary (Pydantic validators for shape/sign; grid-bounds check where the grid is known, in the route's resolve flow before parse) — cite the WR-02 precedent in the docstring: a client error dressed as a server error is the bug class this prevents. SheetLayoutOut's docstring states why indices are withheld from the browser. Grep for the resolve-route test file location first (tests/api/test_structural_hint_context.py vs tests/api/test_hint_and_export.py) and extend the one that exercises POST /api/structural-hint/resolve.

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/api/test_structural_hint_context.py tests/api/test_hint_and_export.py tests/api/test_sheets_route.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Every 422 case passes as a named test; no test observes a 500 from a malformed layout.
    - Round trip A passes: cascade Patient Info → question → posted key-value confirmation → 17-column mapped dataset.
    - Round trip B passes: unknown-layout question → posted row_per_record + header_row_index → RawTable → mapped dataset. Both the explicit-layout and bare-header_row_index forms terminate; NEITHER re-returns the layout question (the loop is closed, asserted).
  </acceptance_criteria>
  <done>BOTH human answers round-trip validated; the browser sees verdicts, never indices; bad input is a named 422; no answer leads back to the same question.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the SHAPE-04 proof — captured outbound at the HTTP boundary, both directions (D-12-06/D-12-09)</name>
  <files>tests/api/test_upload.py</files>
  <read_first>
    - tests/api/test_upload.py:268-314 (the strongest existing privacy test — its docstring insists the production chain is NOT monkeypatched; the fake client lands at app.dependency_overrides[get_anthropic_client] at :300 and captures kwargs["messages"][0]["content"])
    - tests/test_headers_only.py:64-116 (the unit-level capture mold AND the mirror precedent: test_propose_mapping_default_still_sends_sample_rows)
    - .planning/phases/12-claude-reads-the-structure/12-CONTEXT.md D-12-06 (per-site enforcement — this phase added a site, so this phase proves it at the outermost boundary), D-12-09 (default-path realness is a requirement), D-12-11 (headers_only is the curator's promise)
  </read_first>
  <behavior>
    RED first — two tests, same fake, opposite assertions:
    - test_upload_headers_only_judge_sends_no_cell_value: fake client at the get_anthropic_client DI seam; POST data/synthetic/lab_corpus/cascade_allergy_CS-2026-698392.xlsx to the real /api/upload with headers_only=true; the fake's parse(**kwargs) captures every outbound content and returns parsed_output=None (the judge degrades to all-UNKNOWN — the route still answers). Assert "TAYLOR, James", "CS-2026-698392", and "3809217" are each absent from ALL captured content.
    - test_upload_default_judge_sends_the_real_grid: same chain, headers_only=false → assert "TAYLOR, James" IS present in the captured content. Without this mirror, a future over-zealous redaction silently degrades default-path accuracy with no test to catch it — D-12-09 makes presence a requirement.
    - Both run through the REAL judge_workbook_layout and REAL render_evidence_grid — no production monkeypatching; only the SDK client is faked.
  </behavior>
  <action>
    Copy the :268-314 shape exactly, adapted to the judge chain. Name both tests so the pair is obviously a pair. If the upload route's existing tests already override get_anthropic_client in a fixture, extend rather than duplicate the fixture. The cli.py:383-384 headers-only skip of the ENRICHMENT call is untouched by this phase (tests/test_headers_only.py:204 keeps passing — the redacted call this phase adds lives on the API judge path, per D-12-05).
  </action>
  <verify>
    <automated>uv run pytest tests/api/test_upload.py tests/test_headers_only.py -q && uv run pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - Both privacy tests pass by name; tests/test_headers_only.py passes unchanged.
    - Full suite green with ANTHROPIC_API_KEY unset (the DI fake carries the whole judge chain offline).
  </acceptance_criteria>
  <done>SHAPE-04 is a captured-outbound fact in both directions, proven through the untouched production chain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/sheets/resolve, /api/structural-hint/resolve | Client-posted sheet selections, ask_layout flags, and layout hints are untrusted |
| Server → Anthropic API (upload path) | The new production judge call site — the fourth headers_only enforcement site |
| Server → browser (SheetOut) | Verdict metadata leaves the server; indices deliberately do not |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-15 | Tampering / DoS | Client-posted layout indices (label_column 999999, first_row -1, huge last_row) | high | mitigate | 422 at the wire boundary naming the consequence; grid-bounds check before any allocation; never IndexError→500 (Task 2, ASVS V5) |
| T-12-16 | Information disclosure | Real cell values in the judge's outbound request under headers_only | critical | mitigate | Captured-outbound HTTP test through the real chain (Task 3); render_evidence_grid's required kwarg (12-02) is the inner layer |
| T-12-17 | Spoofing / Elevation | A client claiming a layout for a sheet the server never judged | medium | mitigate | Resolve path takes the layout from the SERVER-retained manifest keyed by validated sheet_name (_validated_selections posture); the hint route re-validates indices against the re-parsed grid |
| T-12-18 | Information disclosure | StructureQuestion.evidence_rows reaching the browser unredacted under headers_only | medium | transfer | Pre-existing leak, explicitly out of scope (CONTEXT §deferred, its own follow-up task) — do not fix here, do NOT make it worse: the new layout question reuses the existing wire path unchanged |
</threat_model>

<verification>
- uv run pytest tests/api -q — routes, wire validation, privacy pair all green.
- uv run pytest tests -q with ANTHROPIC_API_KEY unset — full suite green, zero network calls.
</verification>

<success_criteria>
- Single-sheet: confident row_per_record silent, everything else asks answerably; multi-sheet: verdict rides the manifest with zero extra calls; disagree routes via ask_layout.
- The confirmed key-value round trip produces a mapped dataset end to end.
- SHAPE-04 proven at the HTTP boundary in both directions.
</success_criteria>

<output>
Create `.planning/phases/12-claude-reads-the-structure/12-05-SUMMARY.md` when done. Record the exact wire field names (SheetLayoutOut fields, ask_layout, StructuralHintIn.layout shape) — plan 12-06's types.ts mirrors them verbatim.
</output>
