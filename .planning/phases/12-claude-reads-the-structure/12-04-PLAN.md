---
phase: 12-claude-reads-the-structure
plan: 04
type: tdd
wave: 3
depends_on: ["12-02", "12-03"]
files_modified:
  - src/assayingest/service.py
  - src/assayingest/parsing/structure/sheets.py
  - tests/api/conftest.py
  - tests/test_describe_workbook.py
  - tests/test_structure_describe_sheets.py
  - tests/api/test_sheets_route.py
autonomous: true
requirements: [SHAPE-01]

must_haves:
  truths:
    - "describe_workbook calls the judge exactly ONCE per workbook, BEFORE the per-sheet scoring loop (ordering is load-bearing: a key-value sheet's headers only exist after the verdict), through a judge_fn=None seam mirroring rank_fn (D-12-14)."
    - "A shared judging fake CLIENT (not a judge_fn seam) exists in tests/api/conftest.py, because HTTP tests override get_anthropic_client -> lambda: None and _judge_for(client=None, judge_fn=None) short-circuits to None BEFORE any seam or monkeypatch can bind a judge: an API test literally cannot reach the judge_fn kwarg. The fake is a duck-typed structured-output client returning confident row_per_record verdicts for the workbook's real sheet names. Tests that are genuinely about the no-judge path keep the None override — no client really does mean no judge, and the fixture mirrors production honestly."
    - "THE regression test (D-12-12): the patient-name literal from the cascade fixture's Patient Info sheet is ABSENT from that sheet's manifest headers — and this test FAILED before the switch (it leaks on main today via the header_uncertain exemption). A test that was already green proves nothing."
    - "Header suppression is only ever WIDENED: NOT_A_TABLE / MULTIPLE_TABLES / WIDE_MATRIX / UNKNOWN report no headers; a KEY_VALUE sheet reports its LABELS (read from the grid by Python via the un-pivot — safe and genuinely useful: they are what the crosswalk and the learning-store signature want)."
    - "Every judge failure — no client, auth error, rate limit, malformed response — degrades every sheet to UNKNOWN and the manifest still builds: a judge failure costs a QUESTION (the human is asked about the layout), where a ranker failure costs a SUGGESTION — the difference is stated in the code because the two boundaries look identical and are not (D-12-16)."
    - "describe_workbook's docstring no longer claims to be pure Python with no headers_only parameter — the prose and the tests that enforce it are rewritten in the same commit as the behaviour (RESEARCH Pitfall 3)."
  artifacts:
    - path: "src/assayingest/service.py"
      provides: "_judge_for / _judge_or_unknown seams; describe_workbook(judge_fn=None, headers_only=False); SheetManifestEntry.layout"
      contains: "def _judge_or_unknown"
    - path: "src/assayingest/parsing/structure/sheets.py"
      provides: "describe_sheets/_sheet_status/_reportable_headers keyed off LayoutKind when verdicts are supplied"
      contains: "LayoutKind"
    - path: "tests/test_structure_describe_sheets.py"
      provides: "The D-12-12 regression test that failed before the switch, plus the widened signature-suppression test"
      contains: "cascade_allergy"
    - path: "tests/api/conftest.py"
      provides: "judging_client fixture — the shared duck-typed structured-output fake every API test that expects a dataset uses from Wave B onward"
      contains: "judging_client"
  key_links:
    - from: "src/assayingest/service.py describe_workbook"
      to: "src/assayingest/parsing/structure_assist.py judge_workbook_layout"
      via: "_judge_for(client, judge_fn) resolved seam, called once per workbook"
      pattern: "judge_fn"
    - from: "src/assayingest/parsing/structure/sheets.py _reportable_headers"
      to: "src/assayingest/parsing/structure/unpivot.py"
      via: "a KEY_VALUE sheet's reportable headers are the un-pivot's label headers"
      pattern: "unpivot_key_value"
---

<objective>
Wave B: switch the manifest onto the verdict. `describe_workbook` judges the workbook once and threads one `SheetLayout` per sheet into `describe_sheets`; status and header suppression key off `layout.kind`. This is the wave that lands THE regression test — the one that fails on today's code, because today `Patient Info` ships a patient's name as a column header and hashes it into the learning-store signature (D-12-12: the leak is live, not hypothetical).

Purpose: SHAPE-01's manifest half, sequenced so the invariant holds — at no commit may a sheet whose shape is not understood report headers. Suppression is only ever widened here; the heuristic classifier still exists as the verdict-less fallback and dies only in Wave C (12-07), after this wave proves the verdict path end to end.

Output: the seams, the switch, the regression tests, and the rewritten describe_workbook contract (prose + the tests that enforced the old prose, in the same commit).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-claude-reads-the-structure/12-CONTEXT.md
@.planning/phases/12-claude-reads-the-structure/12-RESEARCH.md
@.planning/phases/12-claude-reads-the-structure/12-PATTERNS.md
@.planning/phases/12-claude-reads-the-structure/12-02-SUMMARY.md
@src/assayingest/service.py
@src/assayingest/parsing/structure/sheets.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _judge_for + _judge_or_unknown — the availability boundary, with the difference stated (D-12-16)</name>
  <files>src/assayingest/service.py, tests/test_describe_workbook.py</files>
  <read_first>
    - src/assayingest/service.py:1956-1972 (_ranker_for — the seam mold: injected fn, else client-bound real fn, else None; "None is a legitimate answer, not an error") and :1975-1994 (_rank_or_none — the broad-except availability boundary; its docstring rationale is the one D-12-16 requires the judge's sibling to carry AND differ from)
    - src/assayingest/service.py:99-107 (has_credentials) and :251-258 (the propose_mapping_fn resolution idiom)
    - tests/test_describe_workbook.py:266-273 (the recorder-fake idiom), :293-303 (the failing-fake test), :306-312 (the no-client test) — the molds for the judge's equivalents
  </read_first>
  <behavior>
    RED first — tests/test_describe_workbook.py additions, all offline:
    - _judge_for(client, judge_fn): injected fn wins; client=None → None; else a closure binding judge_workbook_layout to the client. Same contract prose as _ranker_for.
    - _judge_or_unknown(judge, grids, *, headers_only, sheet_names) -> dict[str, SheetLayout]: judge is None → every sheet UNKNOWN; judge raises ANY exception (RuntimeError fake) → every sheet UNKNOWN, logged once with exc_info, never logged AND raised; judge returns normally → its verdicts, with any missing sheet UNKNOWN-filled (defence behind 12-02's own fill).
    - The warning message names the consequence, not the symptom: every sheet will ask about its layout before anything is mapped.
  </behavior>
  <action>
    Add both helpers beside their ranker siblings. _judge_or_unknown's docstring MUST state, verbatim in intent (D-12-16 makes this a requirement, not style): this looks identical to _rank_or_none and is not — a ranker failure costs a SUGGESTION (the human picks a Schema); a judge failure costs a QUESTION (the human is asked about the layout). Neither guesses. Copy the AVAILABILITY-boundary-not-logic-boundary rationale for the broad except.

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_describe_workbook.py -q -k "judge"</automated>
  </verify>
  <acceptance_criteria>
    - The no-judge, raising-judge, and recorder-judge tests pass by running uv run pytest tests/test_describe_workbook.py -q.
    - grep -n "costs a question" src/assayingest/service.py (case-insensitive) finds the stated difference in _judge_or_unknown's docstring — AND the raising-judge test proves the behaviour it describes.
  </acceptance_criteria>
  <done>Judge unavailability is a safe, honest, logged-once degradation to UNKNOWN — never a guess, never a 500.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: describe_workbook onto the verdict — seams in, stale contract out (RESEARCH Pitfall 3)</name>
  <files>src/assayingest/service.py, tests/test_describe_workbook.py</files>
  <read_first>
    - src/assayingest/service.py:2115-2167 (describe_workbook — the docstring at :2157-2161 that becomes false: "There is still no headers_only parameter, and still nothing for one to do") and :2170-2195 (_manifest_entry — scores description.headers and hashes column_signature at :2184; this is WHY the judge must run before the loop)
    - src/assayingest/service.py:2086-2112 (SheetManifestEntry — the wire-discipline note at :2095-2098: store values, never enum members, because this dataclass is what the wire serialises; the new layout field follows the same rule)
    - tests/test_describe_workbook.py:45-53 (the autouse _no_claude fixture asserting "propose_mapping must NOT be called: describe_workbook is pure Python"), :188-197 (the no-headers_only-parameter signature inspection — INVERT it), :214-224 (the seams-exist signature test — EXTEND with judge_fn)
  </read_first>
  <behavior>
    RED first:
    - describe_workbook gains judge_fn=None and headers_only: bool = False, keyword-only, defaulted — every existing call site unchanged. It resolves _judge_for(client, judge_fn), reads every sheet's native grid once, calls _judge_or_unknown ONCE for the whole workbook BEFORE the per-sheet _manifest_entry loop, and passes the verdicts into describe_sheets (Task 3's parameter).
    - A recorder judge_fn on the cascade fixture is called exactly once, receives all 8 sheet grids, and receives the caller's headers_only.
    - SheetManifestEntry gains layout (stored wire-safe per the :2095-2098 discipline — the kind as its string value plus confidence/reasoning/record_count/needs_confirmation; block indices stay server-side per 12-UI-SPEC Discretion §1).
    - With a judge_fn supplying ROW_PER_RECORD verdicts for a normal workbook, the manifest is identical to today's (the null hypothesis changes nothing — D-12-15).
    - Existing keep-tests are re-fitted with a row_per_record judge_fn fake where they need non-UNKNOWN statuses; the autouse _no_claude fixture STAYS (the mapper is still never called) but its prose and the module docstring drop the "pure Python" claim; :188-197 is inverted (the parameter now exists, and the test says why); :214-224 extends to assert the judge_fn seam.
  </behavior>
  <action>
    Implement the switch. Rewrite describe_workbook's docstring: it now takes headers_only and forwards it to the judge's evidence rendering; the manifest is no longer a pure function of the file — it is a pure function of the file PLUS one proposed (never auto-applied) layout verdict per sheet, and with no judge every sheet honestly reports layout unknown. The stale prose and the stale tests change in this same commit — a docstring a test enforces must never outlive the behaviour (RESEARCH Pitfall 3).

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_describe_workbook.py -q</automated>
  </verify>
  <acceptance_criteria>
    - uv run pytest tests/test_describe_workbook.py -q passes: one-call recorder test, headers_only forwarding test, inverted signature test, extended seams test.
    - uv run python -c "import inspect, assayingest.service as s; sig=inspect.signature(s.describe_workbook); assert 'judge_fn' in sig.parameters and 'headers_only' in sig.parameters" exits 0.
  </acceptance_criteria>
  <done>The manifest is verdict-driven, one judge call per workbook, with its contract prose telling the truth again.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: sheets.py re-keyed + THE regression test that fails first (D-12-12)</name>
  <files>src/assayingest/parsing/structure/sheets.py, tests/test_structure_describe_sheets.py, src/assayingest/service.py</files>
  <read_first>
    - src/assayingest/parsing/structure/sheets.py:242-269 (_reportable_headers — suppresses on UNSUPPORTED_SHAPE ONLY, deliberately not on HEADER_UNCERTAIN per the meridian-LEGEND rationale at :255-265; its docstring names the consequence chain screen → _manifest_entry → column_signature → learning store, which the rewrite must keep naming) and :272-284 (_sheet_status — the shape gate first, then header confidence)
    - tests/test_structure_describe_sheets.py:191-264 (the six UNSUPPORTED_SHAPE assertions to rewrite, incl. the two _key_value_workbook tmp_path tests at :226/:237 which now assert READABLE) and :268 (the signature-never-fabricated test to WIDEN to the key-value case)
    - 12-RESEARCH.md §Removing classify_shape Safely (the measured 8-sheet status table — the before/after truth this task pins)
  </read_first>
  <behavior>
    RED FIRST, AND OBSERVED RED — the D-12-12 regression test in tests/test_structure_describe_sheets.py:
    - test_a_key_value_sheets_manifest_never_reports_a_cell_value_as_a_header: describe_workbook on data/synthetic/lab_corpus/cascade_allergy_CS-2026-698392.xlsx with NO client and NO judge_fn → the Patient Info entry's headers do not contain the patient-name literal "TAYLOR, James" and the Methodology & Notes entry's headers do not contain the lab-address value. RUN IT BEFORE IMPLEMENTING and record the failure output in the commit/summary: today Patient Info is header_uncertain, exempt from suppression, and the name IS in its headers (measured in RESEARCH). This test failing first is the plan's proof of work; committed only once green, with the observed-red noted in the test's docstring.
    - describe_sheets gains layouts: Mapping[str, SheetLayout] | None = None. With verdicts supplied: KEY_VALUE → status OK, headers = the un-pivot's label headers (Python reads them from the grid per the verdict — never from the verdict itself); ROW_PER_RECORD → the existing header-confidence gate (OK / HEADER_UNCERTAIN), header row from the verdict; WIDE_MATRIX / MULTIPLE_TABLES / NOT_A_TABLE → UNSUPPORTED_SHAPE, headers suppressed; UNKNOWN → a NEW SheetStatus.LAYOUT_UNKNOWN ("layout_unknown"), headers suppressed. Status stays a GATE outcome; the layout kind travels separately on the manifest entry (RESEARCH Pitfall 7: never conflate).
    - layouts=None → today's classifier behaviour, byte-for-byte (direct callers and the CLI keep working until Wave C removes the fallback).
    - The :268 signature test widens: a key-value sheet's column_signature is computed over its LABELS and never over a fabricated header list containing a cell value; an UNKNOWN sheet's signature is never computed at all.
    - drawing_only handling unchanged; with a NOT_A_TABLE verdict for the chart sheet Result Visualization, it is no longer offered as ingestible (rewrite the relevant assertion from the measured table).
    - Judge-failure UX pinned: describe_workbook with a raising judge_fn on cascade → all 8 entries LAYOUT_UNKNOWN, all headers suppressed, manifest still builds (degraded-but-honest — the accepted D-12-16 cost, stated in the test name).
  </behavior>
  <action>
    Rewrite _sheet_status and _reportable_headers to key off LayoutKind when a layout is supplied, keeping the classifier fallback for layouts=None (Wave C deletes it — this wave only WIDENS suppression, never narrows: assert in a test that every sheet suppressed before is still suppressed after). _reportable_headers' rewritten docstring keeps naming the downstream chain. Rewrite the six inventory assertions; the two _key_value_workbook tmp_path tests now assert the sheet is readable with label headers when a verdict is supplied. In service.py, _manifest_entry only computes column_signature when headers are non-empty (it already suppresses via empty headers — verify, and pin the key-value-labels signature case). Update the SheetStatus enum with LAYOUT_UNKNOWN and its docstring (which gate failed: the layout could not be judged — answerable, not unreadable).

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_structure_describe_sheets.py tests/test_describe_workbook.py -q && uv run pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - The D-12-12 regression test passes AND its red-first run is recorded (failure output quoted in the SUMMARY): the patient-name literal is gone from the manifest.
    - The widened :268 signature test and the all-UNKNOWN degradation test pass by name.
    - Full suite green with ANTHROPIC_API_KEY unset AFTER Task 4's fixture lands — describe_sheets' verdict-less callers are unaffected in this wave (the classifier fallback is intact until 12-07), and the ~7 Wave-B-affected API sheet-route tests are migrated onto the judging fixture in Task 4.
  </acceptance_criteria>
  <done>No sheet whose shape is not understood reports headers; a key-value sheet reports its labels; the live PII leak is dead and pinned by a test that failed first.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: the shared judging fake CLIENT — the fixture the whole phase's API tests depend on</name>
  <files>tests/api/conftest.py, tests/api/test_sheets_route.py</files>
  <read_first>
    - tests/api/test_sheets_route.py:104 and :120 (the get_anthropic_client -> lambda: None overrides — THE constraint: with client None, _judge_for(client=None, judge_fn=None) short-circuits to None BEFORE any seam or monkeypatch can bind a judge, so an HTTP test cannot reach the judge_fn kwarg at all. A judge_fn fake is mechanically impossible here; a fake CLIENT is the only lever)
    - tests/api/conftest.py (the existing fixture surface — where the shared fake lands)
    - tests/test_structure_assist.py:22-40 (_FakeParsedResponse/_FakeMessages/_FakeClient — the duck-typed structured-output fake to generalise) and .planning/phases/12-claude-reads-the-structure/12-02-SUMMARY.md (the judge's exact wire model, so the fake returns a parsed_output the REAL _to_domain accepts — the fake stops at the SDK boundary; production mapping code is never bypassed)
  </read_first>
  <behavior>
    RED first — a self-test of the fixture itself, then the migration:
    - A judging_client fixture in tests/api/conftest.py: a duck-typed object with .messages.parse(**kwargs) returning a parsed_output that is a REAL instance of the judge's wire model, carrying a confident (confidence 1.0) row_per_record verdict for EVERY sheet name the request mentions — derived from the wire model's runtime Literal, so the fake cannot drift from the schema. Any other Claude call the same client receives (the mapper, the ranker) keeps whatever the test already stubbed — the fixture composes with, and never replaces, existing per-test DI overrides.
    - Self-test: overriding get_anthropic_client with judging_client through the REAL /api/upload chain yields a manifest whose sheets are row_per_record with real headers — proving the fake's output survives the real _to_domain (a fake that only satisfies a fake proves nothing).
    - The "no client really does mean no judge" contract is preserved: a test that KEEPS the lambda: None override gets all-LAYOUT_UNKNOWN sheets. Both fixtures coexist by design, and each has a named test asserting its own outcome, so a future reader can tell which behaviour a test is actually about.
    - Migration, this wave only: the ~7 tests in tests/api/test_sheets_route.py whose subject is the MANIFEST (statuses, headers, badges) switch to judging_client. The ~18 whose subject is RESOLVE/ingestion move in 12-05 (they need that plan's resolve wiring first). Tests whose subject IS the no-judge path stay on the None override, renamed if their name implies otherwise.
  </behavior>
  <action>
    Build the fixture and migrate this wave's 7. Its docstring states the mechanical fact that forced it (the client=None short-circuit precedes every seam — an API test cannot reach judge_fn) and the honesty rule (no client means no judge; that is production behaviour, not a test artifact). Export it so 12-05, 12-07 and any future API test consume ONE fake, not five improvised ones.

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/api/test_sheets_route.py tests/api/test_upload.py -q && uv run pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - The fixture self-test passes: judging_client through the real chain yields row_per_record manifests with real headers (the fake's output survives the real _to_domain).
    - The paired no-judge test passes: the lambda: None override yields all-LAYOUT_UNKNOWN.
    - Full suite green with ANTHROPIC_API_KEY unset.
  </acceptance_criteria>
  <done>Every API test that needs a verdict has one shared, schema-bound way to get it — designed on paper in Wave B, not improvised under pressure at Wave C.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Judge verdict → manifest/signature | Model output steers which headers are shown and hashed |
| Anthropic API availability → manifest | A remote failure must not corrupt or block the manifest |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-11 | Information disclosure | PII in manifest headers / learning-store column_signature | critical | mitigate | Suppression re-keyed on LayoutKind and only ever widened; the D-12-12 regression test (red first) pins the fix; key-value headers are Python-read labels, never verdict text |
| T-12-12 | Denial of service | Judge outage blocking uploads | medium | mitigate | _judge_or_unknown broad-except availability boundary: manifest always builds, all sheets LAYOUT_UNKNOWN, human can still answer (D-12-16 accepted degradation, pinned by test) |
| T-12-13 | Tampering (by the model) | A wrong verdict marking a real table unreadable or vice versa | medium | accept | D-12-08: no second opinion by design — the human is the check on the sheet screen; a wrong verdict is loud (badge + reasoning rendered) and human-correctable; the live eval (12-08) measures the rate |
| T-12-14 | Repudiation | Old learning-store signatures hashed over fabricated headers | low | accept | No migration: an old garbage-keyed signature simply never matches again, which is correct — noted so nobody "fixes" the miss (RESEARCH §Runtime State Inventory) |
</threat_model>

<verification>
- uv run pytest tests/test_describe_workbook.py tests/test_structure_describe_sheets.py -q — seams, switch, regression, degradation all green.
- uv run pytest tests -q with ANTHROPIC_API_KEY unset — full suite green, zero network calls.
- The SUMMARY quotes the regression test's observed-red output from before the switch (the D-12-12 proof).
</verification>

<success_criteria>
- One judge call per workbook feeds the manifest; ordering (judge before scoring) is pinned by the recorder test.
- The patient-name-as-header leak is fixed and regression-locked by a test that demonstrably failed first.
- Suppression only widened; verdict-less fallback intact for Wave C to remove.
</success_criteria>

<output>
Create `.planning/phases/12-claude-reads-the-structure/12-04-SUMMARY.md` when done. Record: the observed-red output of the regression test, the SheetManifestEntry.layout stored shape, and the LAYOUT_UNKNOWN status value — 12-05 (wire) and 12-06 (frontend) consume both.
</output>
