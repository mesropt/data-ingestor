---
phase: 11-multi-sheet-ingest
plan: 09
type: execute
wave: 4
depends_on: ["11-07"]
files_modified:
  - frontend/src/lib/types.ts
  - frontend/src/lib/api.ts
  - frontend/src/state/upload.ts
  - frontend/src/state/upload.test.ts
  - frontend/src/state/sheets.ts
  - frontend/src/state/sheets.test.ts
  - frontend/src/components/SheetQuestionPanel.tsx
  - frontend/src/components/ui/checkbox.tsx
  - frontend/src/screens/Upload.tsx
autonomous: true
requirements: [SHEET-01, SHEET-05]

must_haves:
  truths:
    - "A multi-sheet workbook shows a sheet-selection panel below the dropzone listing every worksheet with its headers, row count, status, and its ranked Schema proposal WITH the coverage that produced it (which field matched which header, and what remains uncovered)."
    - "A proposal pre-selects; it never auto-applies. A zero-coverage sheet is pre-unticked with a skip line; a tie is shown as a tie with an empty Schema select; a gate-failed sheet is present, marked, and still tickable."
    - "Submit is blocked with the specific reason shown: at 0 ticked, and while any ticked sheet has no Schema chosen."
    - "The 5th and 6th response kinds are handled exhaustively — assertNever still compiles, and the new phases lock the dropzone."
  artifacts:
    - path: "frontend/src/components/SheetQuestionPanel.tsx"
      provides: "The sheet manifest panel — checkbox + per-sheet Schema select + visible coverage"
      contains: "SheetQuestionPanel"
    - path: "frontend/src/state/sheets.ts"
      provides: "All panel logic as pure functions — pre-selection derivation, submit gating, resolve payload"
      contains: "toResolvePayload"
    - path: "frontend/src/lib/types.ts"
      provides: "SheetQuestionResponse + SheetGroupResponse in the UploadResponse union"
      contains: "sheet_question"
  key_links:
    - from: "frontend/src/state/upload.ts"
      to: "frontend/src/lib/types.ts"
      via: "fromResponse gains the two new kinds; assertNever stays exhaustive"
      pattern: "sheet_question"
    - from: "frontend/src/components/SheetQuestionPanel.tsx"
      to: "frontend/src/state/sheets.ts"
      via: "the panel renders; sheets.ts decides (gating + payload), so vitest covers the logic with no DOM"
      pattern: "from \"../state/sheets\""
---

<objective>
Build the sheet-selection surface (UI-SPEC §Screens 1, Discretion §1 — APPROVED and BINDING).

Purpose: this is the screen where "the tool proposes, the human disposes" becomes visible for the sheet AND the Schema choice. The coverage must be **shown**, not implied — the human is being asked to check the tool's reasoning and cannot check what is not shown (D-11-03, the builder's own words).

Output: the two new response kinds in the client union, the `sheetQuestion` / `resolvingSheets` / `sheetGroup` phases, `state/sheets.ts` (all logic, pure), and `SheetQuestionPanel` (rendering only).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-multi-sheet-ingest/11-CONTEXT.md
@.planning/phases/11-multi-sheet-ingest/11-UI-SPEC.md
@.planning/phases/11-multi-sheet-ingest/11-PATTERNS.md
@.planning/phases/11-multi-sheet-ingest/11-07-SUMMARY.md
@frontend/src/components/DateFormatQuestionPanel.tsx
@frontend/src/state/dateFormat.ts
@frontend/src/state/upload.ts
@frontend/src/lib/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the two new kinds, the three new phases, and the pure panel logic</name>
  <files>frontend/src/lib/types.ts, frontend/src/lib/api.ts, frontend/src/state/upload.ts, frontend/src/state/upload.test.ts, frontend/src/state/sheets.ts, frontend/src/state/sheets.test.ts</files>
  <read_first>
    - frontend/src/lib/types.ts:183-209 (`DateFormatQuestionResponse`'s interface style — `kind` as a string literal, docstring citing the `api/wire.py` class it mirrors — and the `UploadResponse` union at :205)
    - frontend/src/state/upload.ts (whole file — the module docstring's "logic is tested, rendering is checker-validated" rule; the `dateQuestion`/`resolvingDateFormat` state arms at :79-85; the action triple at :99-101; `fromResponse` + `assertNever` at :111-126; `toDropzonePhase` at :137-159; the reducer's guard-then-transition triple at :203-213)
    - frontend/src/state/dateFormat.ts:50-98 (`allColumnsAnswered` / `toResolvePayload` — the pure-helper split `state/sheets.ts` mirrors)
    - frontend/src/state/upload.test.ts (the literal-fixture, one-transition-per-test style)
    - frontend/src/state/dateFormat.test.ts (the analog for `state/sheets.test.ts`)
    - frontend/src/lib/api.ts (the existing `resolveHint` / date-format resolve calls — the shape `resolveSheets` copies)
    - .planning/phases/11-multi-sheet-ingest/11-07-SUMMARY.md (the EXACT wire field names — `SheetOut`, `SheetSchemaProposalOut`, `SheetResolveRequest`, `SheetGroupResponse`)
    - .planning/phases/11-multi-sheet-ingest/11-UI-SPEC.md §Copywriting Contract and §Discretion 1 (pre-selection states, verbatim)
  </read_first>
  <behavior>
    RED first — `frontend/src/state/sheets.test.ts` and additions to `frontend/src/state/upload.test.ts` (pure functions, node env, no DOM):
    - `initialSelections(response, defaultSchema)` derives the pre-selection per UI-SPEC exactly: a proposal exists → ticked + that Schema; zero coverage (no proposal) → UNTICKED + no Schema; tie → TICKED + no Schema; gate-failed (`drawing_only` / `unsupported_shape` / `header_uncertain`) → unticked, but still selectable. When the Upload dropdown supplied a Schema and the scorer proposed none, the dropdown's Schema is the fallback pre-selection (D-11-16).
    - `submitBlockedReason(selections)` returns the UI-SPEC's exact strings: "Tick at least one sheet to ingest." at 0 ticked; "Choose a Schema for '{sheet}' — ties aren't broken automatically." when a ticked sheet has no Schema; `null` when ready.
    - `toResolvePayload(uploadToken, selections)` includes ONLY ticked sheets, each with its chosen Schema, matching `SheetResolveRequest`.
    - `upload.ts`: `fromResponse` maps `kind:"sheet_question"` → `{phase:"sheetQuestion", ...}` and `kind:"sheet_group"` → `{phase:"sheetGroup", groupId}`; `assertNever` stays exhaustive (a missing case is a compile error — `npm run build` must fail if a kind is unhandled; verify by temporarily deleting a case in the RED step, then restoring).
    - The `SUBMIT_SHEETS` / `SHEETS_SUCCESS` / `SHEETS_ERROR` triple transitions `sheetQuestion → resolvingSheets → sheetGroup`, and a wrong-phase action returns state unchanged.
    - `toDropzonePhase` maps all three new phases to `"locked"`.
  </behavior>
  <action>
    1. `lib/types.ts`: add `SheetSchemaProposal`, `SheetOut`, `SheetQuestionResponse` (`kind: "sheet_question"`), `SheetSelection`, `SheetResolveRequest`, `SheetMember`, `SheetGroupResponse` (`kind: "sheet_group"`; `members[*].response` is `UploadResponse` minus the two group kinds — the recursive arm), each mirroring the `api/wire.py` class named in its docstring. Extend the `UploadResponse` union with both kinds. This BREAKS `assertNever` in `upload.ts` and `Upload.tsx` at compile time until the cases exist — that is the safety net working; never widen a `default` to silence it.

    2. `state/upload.ts`: add the three phases, the action triple, the two `fromResponse` cases, the `toDropzonePhase` mapping, and the reducer's guard-then-transition triple — copying the `dateQuestion`/`resolvingDateFormat` recipe touch-for-touch.

    3. `state/sheets.ts` (NEW): `initialSelections`, `submitBlockedReason`, `toResolvePayload`, and a small `coverageLine(proposal)` returning the UI-SPEC's exact copy for each source (`"{matched}/{total} canonical fields matched · learned profile"` / `"· crosswalk"` / `"No crosswalk match — Claude suggests {schema}. Check it before ingesting."`). ALL panel logic lives here — the component decides nothing.

    4. `lib/api.ts`: `resolveSheets(request: SheetResolveRequest): Promise<SheetGroupResponse>` posting to `/api/sheets/resolve`, copying the existing resolve calls' error handling.

    Commit RED then GREEN.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/state/sheets.test.ts src/state/upload.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` succeeds (proves `assertNever` is satisfied — the union is handled exhaustively).
    - `npm test -- --run src/state/sheets.test.ts` passes, covering all four pre-selection states and both blocked reasons.
    - `grep -c 'sheet_question\|sheet_group' frontend/src/lib/types.ts` >= 2.
    - The panel logic is in `state/sheets.ts`, not in a component: `grep -c 'useState' frontend/src/state/sheets.ts` returns 0.
  </acceptance_criteria>
  <done>The client understands the two new arms, the reducer drives the new phases, and every decision the panel makes is a tested pure function.</done>
</task>

<task type="auto">
  <name>Task 2: SheetQuestionPanel — the coverage is on the screen</name>
  <files>frontend/src/components/SheetQuestionPanel.tsx, frontend/src/components/ui/checkbox.tsx, frontend/src/screens/Upload.tsx</files>
  <read_first>
    - frontend/src/components/DateFormatQuestionPanel.tsx (whole file — the exact shape to copy: props + local answers + gated submit at :11-48, the `Card`/`CardHeader` + lucide icon + `text-heading` title at :51-61, the per-item `rounded-lg border border-border p-3` block at :63-70, the single submit with `Loader2` at :113-116)
    - frontend/src/components/SchemaPicker.tsx (the governed-Schema `Select` idiom to reuse per sheet)
    - frontend/src/components/StructuralHintPanel.tsx (the inline-question slot below the dropzone, and the dropzone `locked` interplay)
    - frontend/src/screens/Upload.tsx (where the panel mounts; the `assertNever` there also needs the new kinds)
    - frontend/src/components/ui/ (the installed primitives — `Badge` chips, `Select`, `Button`, `Card`, `Alert`)
    - .planning/phases/11-multi-sheet-ingest/11-UI-SPEC.md — §Discretion 1 (per-sheet block anatomy, top to bottom), §Copywriting Contract (EVERY string is specified — use them verbatim), §Color (amber = tie/header-unclear; muted = skip/no-table-found/unsupported-shape; accent = submit CTA + checked box only), §Registry Safety (`checkbox` is the ONE new official install)
  </read_first>
  <action>
    1. Install the ONE approved primitive: `npx shadcn@latest add checkbox` in `frontend/`. It must add `components/ui/checkbox.tsx` and install ZERO new npm packages (`@base-ui/react` is already a dependency). **If the CLI proposes any new package, STOP and surface it in the SUMMARY — do not accept a silent dependency addition.** No other primitive may be added; if a richer one seems needed, it must come back through the UI-SPEC first.

    2. Create `frontend/src/components/SheetQuestionPanel.tsx` — the structural twin of `DateFormatQuestionPanel`: one inline `Card` below the dropzone (same `max-w-[720px]` slot, same "one panel at a time" rule), `Layers` icon + "Choose sheets to ingest" (`text-heading`) + the intro line, then one bordered block per sheet (`rounded-lg border border-border p-3`), then ONE submit for all sheets.

       Per-sheet block, top to bottom (md 16px rhythm), exactly per UI-SPEC §Discretion 1:
       1. Selection row — `Checkbox` + sheet name (mono, 600) + "{row_count} rows" (mono, tabular-nums, muted) + a status badge when not `ok`. The WHOLE row is the click target (32px min height).
       2. "Detected headers" + the sheet's headers as mono chips (the existing `Badge`/chip idiom), wrapping. Shown in full — headers are never redacted under headers-only.
       3. Proposal + coverage — the coverage line with its source suffix, then the matched pairs (`{field} ← {header}`, one mono line each) and "Not matched: {fields}". **Always visible, never behind a disclosure.** This block is the phase's reason to exist.
       4. "Schema for this sheet" + a `Select` over the governed Schemas.

       States: proposal → ticked + pre-selected; zero coverage → unticked + skip line (muted); tie → ticked + EMPTY select + amber tie notice; gate-failed → unticked + muted/amber badge + helper line, still tickable (the human may insist, and the failure then surfaces as that member's own structural question).

       Submit: disabled at 0 ticked and while any ticked sheet has no Schema, with `submitBlockedReason`'s string shown. Error → destructive `Alert` with the UI-SPEC's copy; selections preserved.

       All strings come from the UI-SPEC Copywriting Contract verbatim. All decisions come from `state/sheets.ts` — the component holds only the answers map and the submitting flag.

    3. `screens/Upload.tsx`: render the panel in the existing inline-question slot for the `sheetQuestion` phase; wire `SUBMIT_SHEETS` → `resolveSheets` → `SHEETS_SUCCESS` / `SHEETS_ERROR`; handle the two new kinds in its own `assertNever` switch. The dropzone locks (already mapped in Task 1).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ui/checkbox.tsx` exists and `git diff frontend/package.json` shows NO new dependency (or, if one was added, the SUMMARY names it explicitly and flags it).
    - `npm run build` and `npm run lint` pass; the whole vitest suite passes.
    - The coverage detail is rendered unconditionally: `grep -c 'Not matched' frontend/src/components/SheetQuestionPanel.tsx` >= 1 and the matched-pair rendering is present.
    - No new hex value and no new type role: `grep -cE '#[0-9a-fA-F]{6}' frontend/src/components/SheetQuestionPanel.tsx` returns 0.
    - The panel holds no gating logic: `grep -cE 'function (submitBlockedReason|initialSelections|toResolvePayload)' frontend/src/components/SheetQuestionPanel.tsx` returns 0.
  </acceptance_criteria>
  <done>A multi-sheet workbook shows every sheet, what the tool knows about it, and why it proposes the Schema it proposes — and nothing is ingested until the human ticks and confirms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server response → React render | Worksheet titles and header text are untrusted user content rendered in the panel |
| shadcn registry → repo | A component install writes source files into the project |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-33 | Tampering | XSS via a worksheet title or header text | medium | mitigate | React escapes by default; render titles/headers as text children only — no `dangerouslySetInnerHTML`, no `innerHTML`. Same rule `ReconcilePanel` follows for untrusted crosswalk text (T-08-11). |
| T-11-34 | Tampering | supply chain via the shadcn `checkbox` add | high | mitigate | Official shadcn registry only (pre-approved in UI-SPEC §Registry Safety). The add must install ZERO new npm packages — `@base-ui/react` is already present. Any proposed new package STOPS the task and is surfaced, never silently accepted. |
| T-11-35 | Spoofing | the client re-sending server state | medium | mitigate | `SheetResolveRequest` carries only the token and the human's choices; the manifest, the temp file and the Schema objects are server-retained (T-08-08) |
| T-11-SC | Tampering | package installs | high | mitigate | One shadcn component add, official registry, expected to add zero packages. If any package is added, the executor STOPS and reports it rather than installing it. |
</threat_model>

<verification>
- `cd frontend && npm run build && npm run lint && npm test -- --run` all green.
- `git diff frontend/package.json frontend/package-lock.json` — no new dependency (or an explicit, surfaced one).
- Manual: upload `data/synthetic/meridian_cro_codes.xlsx` — LEGEND appears, unticked, with "No canonical fields matched any Schema — proposed: skip this sheet."
</verification>

<success_criteria>
- Every sheet of a multi-sheet workbook is listed with headers, row count, status and a coverage-bearing Schema proposal.
- Pre-selection follows D-11-06 exactly: proposal → ticked; zero coverage → skip; tie → ticked with no Schema and a tie notice; gate-failed → marked but still tickable.
- Submit is blocked with the reason named; nothing is auto-applied.
- The build compiles, which proves the two new kinds are handled exhaustively.
</success_criteria>

<artifacts_this_phase_produces>
- `frontend/src/lib/types.ts`: `SheetSchemaProposal`, `SheetOut`, `SheetQuestionResponse`, `SheetSelection`, `SheetResolveRequest`, `SheetMember`, `SheetGroupResponse`; both added to the `UploadResponse` union
- `frontend/src/lib/api.ts`: `resolveSheets(request) -> Promise<SheetGroupResponse>`
- `frontend/src/state/upload.ts`: phases `sheetQuestion` / `resolvingSheets` / `sheetGroup`; actions `SUBMIT_SHEETS` / `SHEETS_SUCCESS` / `SHEETS_ERROR`; two `fromResponse` cases; three new `toDropzonePhase` → `locked` mappings
- **New file** `frontend/src/state/sheets.ts`: `initialSelections`, `submitBlockedReason`, `toResolvePayload`, `coverageLine`
- **New file** `frontend/src/components/SheetQuestionPanel.tsx` (+ its per-sheet `SheetCard` block)
- **New file** `frontend/src/components/ui/checkbox.tsx` (official shadcn primitive — the one approved install)
- New tests: `frontend/src/state/sheets.test.ts`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/11-multi-sheet-ingest/11-09-SUMMARY.md` when done.
</output>
