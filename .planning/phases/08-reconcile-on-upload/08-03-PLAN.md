---
phase: 08-reconcile-on-upload
plan: 03
type: tdd
wave: 3
depends_on:
  - 08-02
files_modified:
  - frontend/src/lib/types.ts
  - frontend/src/state/upload.ts
  - frontend/src/state/reconcile.ts
  - frontend/src/state/reconcile.test.ts
  - frontend/src/state/upload.test.ts
  - frontend/src/lib/api.ts
  - frontend/src/components/ReconcilePanel.tsx
  - frontend/src/components/MapFileControls.tsx
  - frontend/src/screens/Upload.tsx
autonomous: true
requirements:
  - RECON-01
  - RECON-02
  - RECON-03
must_haves:
  truths:
    - "On the Upload screen a curator can pick a target Schema, set a vendor label, and optionally attach a map file alongside the CSV/Excel before uploading."
    - "When the server returns a reconcile_question, an inline ReconcilePanel (mirroring StructuralHintPanel) lists each conflict with keep-master / take-map-file choices and re-submits to /api/reconcile/resolve."
    - "On no-conflict (or after resolution) the flow proceeds silently to Review with alias-pre-filled rows already green — reusing the existing review + confirm gate."
    - "The upload state machine handles kind = mapping | structural_question | reconcile_question consistently through the same reducer."
  artifacts:
    - "frontend/src/lib/types.ts (ReconcileQuestionResponse, ReconcileChoice, extended UploadResponse)"
    - "frontend/src/state/upload.ts (reconcileQuestion phase)"
    - "frontend/src/state/reconcile.ts + reconcile.test.ts (choice reducer + buildResolvePayload)"
    - "frontend/src/lib/api.ts (uploadFile map-file params + resolveReconcile)"
    - "frontend/src/components/ReconcilePanel.tsx + MapFileControls.tsx"
    - "frontend/src/screens/Upload.tsx (threading)"
  key_links:
    - "Upload.tsx -> uploadFile(file, fieldSet, headersOnly, {mapFile, schemaName, vendor}) -> reducer -> ReconcilePanel -> resolveReconcile(token, choices) -> onMapped -> Review"
    - "state/upload.ts fromResponse maps kind=reconcile_question -> phase reconcileQuestion (mirrors the structural_question branch)"
---

<objective>
Thread the reconcile flow through the browser, mirroring the existing inline structural-hint pattern (per 08-CONTEXT D-08-06): add an optional map-file attach + target-Schema/vendor selection to the Upload screen, add a `ReconcilePanel` (inline, mirroring `StructuralHintPanel`) that renders the conflict list with keep-master / take-map-file choices and re-submits to `/api/reconcile/resolve`, and on no-conflict proceed silently to the existing Review screen with alias-pre-filled (green) rows. Keep the shadcn / dark data-theme aesthetic. Do NOT build the Registry table or Docs page (Phase 09).

The testable logic (the upload/reconcile reducers, the resolve payload builder, the api wrappers' request shape) is unit-covered with vitest, tests-first; the panel/controls rendering is validated by the type-checked build and the end-of-phase human review.

Purpose: RECON-01/02/03 are visible end-to-end in the browser — the demo money shot (a known-vendor second file maps with zero yellow, and a conflicting map file surfaces a resolvable question).
Output: New types + reducers + api wrappers + two components + Upload screen threading, build green, vitest green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-reconcile-on-upload/08-CONTEXT.md
@.planning/phases/08-reconcile-on-upload/08-02-PLAN.md

# The exact code this plan extends/mirrors:
@frontend/src/lib/types.ts
@frontend/src/lib/api.ts
@frontend/src/state/upload.ts
@frontend/src/state/upload.test.ts
@frontend/src/screens/Upload.tsx
@frontend/src/components/StructuralHintPanel.tsx
@frontend/src/components/SchemaControls.tsx
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Reconcile types + upload/reconcile reducers (vitest)</name>
  <files>frontend/src/lib/types.ts, frontend/src/state/upload.ts, frontend/src/state/reconcile.ts, frontend/src/state/reconcile.test.ts, frontend/src/state/upload.test.ts</files>
  <behavior>
    - RED first (vitest):
    - state/upload.test.ts: UPLOAD_SUCCESS with a kind="reconcile_question" response transitions uploading -> reconcileQuestion carrying the response + upload_token; HINT_SUCCESS/RECONCILE_SUBMIT paths mirror the structural_question shape. A reconcile_question response from a resolve is handled identically to the fresh path.
    - state/reconcile.test.ts: initialReconcileChoices(conflicts) defaults every conflict to "keep_master"; setChoice(state, sourceColumn, vendor, "take_map_file") updates only that conflict; buildResolvePayload(uploadToken, choices) returns {upload_token, choices:[{vendor, source_column, decision}]} with one entry per conflict and no extras.
  </behavior>
  <action>
In lib/types.ts add `ReconcileConflict` ({vendor, source_column, master_field, map_file_field}), `ReconcileQuestionResponse` ({kind:"reconcile_question", upload_token, schema_name, vendor, conflicts: ReconcileConflict[]}) mirroring api/wire.py's ReconcileQuestionResponse, and `ReconcileChoice` ({vendor, source_column, decision:"keep_master"|"take_map_file"}). Extend the `UploadResponse` union to `MappingResponse | StructuralQuestionResponse | ReconcileQuestionResponse`.

In state/upload.ts extend `UploadState` with a `reconcileQuestion` phase ({phase:"reconcileQuestion"; file; response: ReconcileQuestionResponse; uploadToken}) and a `resolvingReconcile` phase ({phase:"resolvingReconcile"; file; uploadToken}); add actions SUBMIT_RECONCILE / RECONCILE_SUCCESS / RECONCILE_ERROR mirroring the SUBMIT_HINT / HINT_SUCCESS / HINT_ERROR trio. Update `fromResponse` to map kind="reconcile_question" -> the reconcileQuestion phase (the mapping and structural_question branches unchanged). Update toDropzonePhase mapping in Upload.tsx consumers by exporting the phase names (the "locked" set gains reconcileQuestion + resolvingReconcile).

Create state/reconcile.ts: a pure module (no React) with `ReconcileChoiceState` (a map/array of per-conflict decisions), `initialReconcileChoices(conflicts)` (every conflict defaults to keep_master — the safe, no-op-against-master default, P1/D-08-02), `setChoice(...)`, and `buildResolvePayload(uploadToken, choices)` returning exactly the `/api/reconcile/resolve` body shape (mirrors state/upload.ts::buildHintPayload). Keep it vitest-covered like state/reconcile.test.ts, matching the "logic tested, rendering checker-validated" split.
  </action>
  <verify>
    <automated>cd /home/mesrop_custom_user/PycharmProjectsWSL_Linux_native/data-ingestor/frontend && . ~/.nvm/nvm.sh 2>/dev/null; npx vitest run src/state/reconcile.test.ts src/state/upload.test.ts</automated>
  </verify>
  <done>Types mirror the wire models; the upload reducer handles reconcile_question -> reconcileQuestion and the resolve loop; reconcile choices default to keep_master and buildResolvePayload emits the exact resolve body; vitest green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: api.ts map-file params + resolveReconcile + ReconcilePanel + MapFileControls</name>
  <files>frontend/src/lib/api.ts, frontend/src/components/ReconcilePanel.tsx, frontend/src/components/MapFileControls.tsx</files>
  <behavior>
    - RED first (vitest, extend state/upload.test.ts's api-wrapper coverage which already mocks fetch): uploadFile appends map_file + schema_name + vendor to the FormData only when provided (a plain upload's body is byte-identical to today's — regression guard); resolveReconcile POSTs {upload_token, choices} to /api/reconcile/resolve with credentials:"include" and returns the discriminated UploadResponse.
  </behavior>
  <action>
Extend `uploadFile` in lib/api.ts with an optional trailing `options?: {mapFile?: File; schemaName?: string; vendor?: string}` (keep the existing positional signature backward compatible so the current call site and tests do not break). When options.mapFile is present, `body.append("map_file", options.mapFile)`, `body.append("schema_name", options.schemaName)`, `body.append("vendor", options.vendor)`; never set Content-Type (the browser owns the multipart boundary — existing note). Add `resolveReconcile(uploadToken, choices)` mirroring `resolveHint`: POST /api/reconcile/resolve with {upload_token, choices}, returning `UploadResponse`.

Create components/ReconcilePanel.tsx mirroring StructuralHintPanel.tsx's structure and props shape ({question: ReconcileQuestionResponse; submitting; onResolve:(choices)=>void}): render each conflict as a row showing the source column + vendor and the two canonical-field options, with a keep-master / take-map-file choice control per conflict (shadcn Select or RadioGroup; keep-master preselected). Drive the choice state via state/reconcile.ts (initialReconcileChoices/setChoice) and submit buildResolvePayload's choices via onResolve. Use the same Card/Label/Button shadcn primitives and copy tone as StructuralHintPanel. Escape-by-default React rendering of vendor/column/field text (no dangerouslySetInnerHTML) — untrusted crosswalk text.

Create components/MapFileControls.tsx: a compact Upload-screen control (mirroring the relevant parts of SchemaControls) with a target-Schema selector (populated by listSchemas), a vendor Input, and an optional map-file `<input type="file" accept="application/json,.json">` attach with a clear "optional" affordance. It surfaces the chosen {schemaName, vendor, mapFile} up to the Upload screen via callbacks. Gate the map-file attach affordance on signedIn/verified as a UX mirror (the server re-checks; do not rely on the mirror) — reuse the signedIn/verified prop pattern from SchemaControls.
  </action>
  <verify>
    <automated>cd /home/mesrop_custom_user/PycharmProjectsWSL_Linux_native/data-ingestor/frontend && . ~/.nvm/nvm.sh 2>/dev/null; npx vitest run src/state/upload.test.ts && npm run build</automated>
  </verify>
  <done>uploadFile sends map_file/schema_name/vendor only when attached (plain upload unchanged); resolveReconcile hits /api/reconcile/resolve; ReconcilePanel + MapFileControls compile and render the conflict choices / attach controls in the shadcn aesthetic; vitest + typed build green.</done>
</task>

<task type="auto">
  <name>Task 3: Thread map-file controls + ReconcilePanel into the Upload screen</name>
  <files>frontend/src/screens/Upload.tsx</files>
  <action>
Wire the reconcile flow into Upload.tsx mirroring the existing structural-hint threading (D-08-06). Add local state for the selected Schema name, vendor, and attached map file (via MapFileControls). Render MapFileControls above/near the dropzone. In handleSubmitUpload, pass the map-file options to uploadFile; branch the response: kind="mapping" -> onMapped (existing); kind="structural_question" -> existing hint panel; kind="reconcile_question" -> set the reconcile phase and keep the last reconcile question for the panel (mirror lastQuestion). Render ReconcilePanel inline (below the dropzone) when phase is reconcileQuestion|resolvingReconcile; its onResolve calls a new handleResolveReconcile that dispatches SUBMIT_RECONCILE, calls resolveReconcile(uploadToken, choices), and on kind="mapping" calls onMapped(response, fieldSet) (proceed silently to Review), else re-shows the panel (a resolve can still surface a follow-up state defensively). Keep all transitions going through the reducer; update toDropzonePhase to map the two new phases to "locked". Thread signedIn/verified into MapFileControls (pass through from Upload's props — extend UploadProps if needed, mirroring how Review receives them).

Do NOT alter the plain-upload path when no map file is attached — that flow must stay byte-identical to today.
  </action>
  <verify>
    <automated>cd /home/mesrop_custom_user/PycharmProjectsWSL_Linux_native/data-ingestor/frontend && . ~/.nvm/nvm.sh 2>/dev/null; npm run build && npx vitest run</automated>
    <human-check>Run the app (uvicorn + npm run dev), sign in as a verified user, attach a conflicting map file with a target Schema + vendor: the ReconcilePanel lists the conflict with keep-master/take-map-file choices; resolving proceeds to Review. Attach a non-conflicting map file whose aliases cover the columns: Review shows green pre-filled rows with no Claude yellow (money shot). A plain upload with no map file behaves exactly as before.</human-check>
  </verify>
  <done>The Upload screen offers optional map-file attach + Schema/vendor selection, renders the inline ReconcilePanel on conflict, proceeds silently to Review on no-conflict/after-resolution, and leaves the plain-upload path unchanged; typed build + full vitest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server reconcile_question payload -> React render | Untrusted vendor/column/canonical-field text rendered in the ReconcilePanel |
| user file picker -> uploadFile FormData | A chosen map file + schema/vendor labels sent to the governed endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-11 | Tampering | XSS via conflict/alias text in ReconcilePanel | medium | mitigate | React escapes all rendered text by default; no dangerouslySetInnerHTML is used for any crosswalk/conflict string |
| T-08-12 | Elevation of privilege | client-side signedIn/verified mirror bypass | low | mitigate | The mirror only gates UX affordances; the server re-enforces require-verified-user on the augmenting path (08-02) — the client mirror is never the authority |
| T-08-13 | Information disclosure | map-file contents echoed unexpectedly | low | accept | The map file is a user-chosen master-map JSON the user already owns; it is sent only to the governed endpoint over the existing credentialed fetch |
</threat_model>

<verification>
- `npx vitest run` — all frontend unit suites green (upload + reconcile reducers, api wrappers).
- `npm run build` — tsc type-check + vite build succeed (no type drift against the 08-02 wire models).
- End-of-phase human check confirms the inline reconcile flow and the no-conflict money shot visually.
</verification>

<success_criteria>
- Optional map-file attach + Schema/vendor selection on the Upload screen (RECON-01).
- Inline ReconcilePanel surfaces conflicts with keep-master/take-map-file choices and re-submits to /api/reconcile/resolve (RECON-02).
- No-conflict / post-resolution proceeds silently to the existing Review + confirm gate (RECON-03).
- The plain-upload path is unchanged; the reducer handles all three response kinds.
</success_criteria>

<output>
Create `.planning/phases/08-reconcile-on-upload/08-03-SUMMARY.md` when done.
</output>
