# Synthetic demo data

Fully synthetic CRO assay exports — no confidential or real data. Each "lab" formats
its file differently on purpose, so the mapper has real work to do. Target fields the
mapper must produce: `compound_id`, `assay_type`, `value`, `unit`, `target`,
`n_replicates`, `assay_date`.

| File | Lab | The mess it demonstrates |
|------|-----|--------------------------|
| `novascreen_batch01.csv` | NovaScreen | **Missing unit column** (blank header). Unit must be *inferred* from the value range (0.8–880 → nM) and flagged yellow. Terse headers: `cmpd`, `potency`, `target_gene`. |
| `helixbio_export.csv` | HelixBio | **Unit embedded in the header** `Conc (uM)` → must be extracted to `unit = µM`. Dates are `DD/MM/YYYY` (ambiguous vs MM/DD). `# Reps` → `n_replicates`. |
| `crestchem_results.csv` | Crestchem | **Mixed assay types + units in one file**: `Inhibition %` (→ `%inhibition`, unit `%`), `IC50`/`Kd` in `nM`. Assay labels need normalization. Leading space in the `ID` header to strip. |
| `novascreen_batch02.csv` | NovaScreen | **Learning-loop payload.** Identical column signature to `batch01`. First NovaScreen file = several yellow fields; this second file should auto-map at confidence 1.0 from the saved lab profile — zero yellows. |

## Extended vendor set (12 files in this table; 16 total generated by `scripts/gen_synthetic_pk.py` including the 4 drawing fixtures below)

Twelve "vendors" here (ten from Phase 1, plus two Phase 2 Plan 3 corpus-gap
fixtures below), each with a distinct structural mess, to stress the parser +
mapper harder. Regenerate deterministically with
`uv run python scripts/gen_synthetic_pk.py` (seed `20260709`).

| File | Vendor | The mess it demonstrates |
|------|--------|--------------------------|
| `zephyr_bio_ZB-2025.xlsx` | Zephyr Bio | **3 sheets** (Week 1-3), each with **4 metadata/title rows above the real header** — header is not on row 1. |
| `meridian_cro_codes.xlsx` | Meridian CRO | **Cryptic 3-4 letter column codes** (`CMP/ASY/VAL/UOM/TGT/NREP/DT`), lowercase assay labels, `DD-MM-YYYY`, plus a `LEGEND` sheet. |
| `apex_labs_wide_matrix.xlsx` | Apex Labs | **Wide format**: one row per compound, a separate IC50 column *per target* (EGFR/JAK2/…). Unit only in the sheet name. `MM/DD/YYYY`. |
| `helix_genomics_DE.xlsx` | Helix Genomics | **German headers** + **comma decimals** (`14,771`) + `DD.MM.YYYY`. Unit embedded in `Konz. (µM)`. |
| `bionexus_transposed.xlsx` | BioNexus | **Transposed layout** — fields are rows, each compound is a column. |
| `orion_pk_report.xlsx` | Orion PK | **3 sheets**: `Summary` (the real data, PK wording), `Raw timepoints`, `Notes`. `YYYY/MM/DD`. Must pick the right sheet. |
| `summit_discovery_mixed.xlsx` | Summit Discovery | **Assay + unit fused** in the readout value (`IC50 (nM)`), `%inhibition` rows mixed in, trailing space in `id `, unpadded `M/D/YYYY`. |
| `cascade_assays_nounit.xlsx` | Cascade Assays | **No unit column** (infer from range) **and** a blank-named junk column of QC flags. Compact `YYYYMMDD` dates. |
| `delta_screening_per_target.xlsx` | Delta Screening | **One sheet per target**, and the header spelling **differs on every sheet** (`IC50 (nM)` vs `IC50 nM` vs `ic50_nm`). `DD/MM/YYYY`. |
| `pinnacle_labs_export.csv` | Pinnacle Labs | **Semicolon-delimited** CSV with **comment lines above the header** and comma decimals. |
| `vertex_pk_eu_format.xlsx` | Vertex PK | **European thousands+decimal** (`3.698,10` — `.` groups thousands, `,` is the decimal) in a value column whose magnitude crosses 1000. Distinct from Pinnacle's bare decimal-comma (`11,076`), which never reaches the thousands threshold — closes a real corpus gap (Phase 2 Plan 3, 02-RESEARCH.md Pitfall). `DD.MM.YYYY`. |
| `castlebio_native_dates.xlsx` | CastleBio | **Excel-native date-typed cell** — the date column is written as a real `datetime` object (`ws.cell(...).value = date(...)`), not a formatted string like every other fixture's dates. `openpyxl` reads it back as `datetime.datetime`, not `"DD/MM/YYYY"` text — a shape no `date_format` `strptime` pattern matches (Phase 2 Plan 3, 02-RESEARCH.md Pitfall 6). |

## Drawing fixtures (Phase 1 Plan 3 — D-16..D-18, the corpus previously had zero drawings)

| File | Vendor | The mess it demonstrates |
|------|--------|--------------------------|
| `vantage_pk_with_chart.xlsx` | Vantage PK | **Embedded chart on the data sheet** (D-16) — a `BarChart` anchored at `E2` must not disqualify the sheet; `pd.read_excel`'s shape is unaffected by a floating drawing. |
| `nimbus_labs_chartsheet.xlsx` | Nimbus Labs | **Dedicated chartsheet** alongside its `Data` sheet (D-17) — must be dropped from sheet ranking and never crash the reader (`wb.worksheets` structurally excludes it). |
| `quantex_scanned_report.xlsx` | Quantex | **Image-only sheet**, no cells at all (D-18) — must surface a `StructureQuestion` explaining the unreadable drawing, never the silent "sheet has no data rows" skip. |
| `triton_screening_two_tables.xlsx` | Triton Screening | **Two independent tables on one sheet**, different headers, separated by 2 fully-blank rows (D-10's `multiple_tables` shape) — previously untested; a real corpus gap this plan closes. |

## `verity_reagents_stock.xlsx` — the domain-independence proof

A reagent stockroom export, generated by `scripts/gen_synthetic_reagents.py`. It exists to
be run against `presets/reagent-inventory.yaml`, which describes lab consumables rather than
assay results.

It is deliberately hostile to a lucky guess:

- the header sits on row 3, under a merged title row and a blank row, so the parser stops
  and asks rather than guessing (`--hint header-row=2`)
- no column name shares any vocabulary with a field name — `SKU`, `Use By`, `Location`
- it carries a `Reordered?` column the field set never asked for

Live result: all six fields map at confidence 1.00, `quantity` becomes a real number,
`expiry_date` converts to ISO because the preset declares its `date_format`, and the extra
column is ignored. Export unblocks (exit code 0). No code in `src/` knows any of these words.

Pinned by `tests/test_cross_domain.py`.

## Demo narrative (the money shot)

1. Upload `novascreen_batch01.csv` → mapper returns a draft; the **unit** field is yellow
   ("no unit column; inferred nM from value range") + waits for human confirm.
2. Curator confirms `unit = nM`. Mapping is saved as the **NovaScreen lab profile**.
3. Upload `novascreen_batch02.csv` (same lab, same columns) → **zero yellow fields**,
   auto-mapped at confidence 1.0. That contrast is the differentiator.

## Value-range heuristic (for unit inference / validation)

Typical potency magnitudes, useful for the no-LLM validator:

- `nM`: ~0.1 – 1000
- `µM`: ~0.001 – 100 (often < 10 for potent compounds)
- `%` (inhibition): 0 – 100

These overlap by design — which is exactly why a bare guess is unsafe and the field must
be flagged for human confirmation rather than silently filled.