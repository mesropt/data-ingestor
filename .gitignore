# Junk / accidental downloads
linux-x64
*:Zone.Identifier

# IDE
.idea/
.vscode/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Env / secrets
.env
.env.*
!.env.example

# Node / frontend
node_modules/
dist/
.vite/

# Data / local artifacts
*.sqlite
*.sqlite3
*.db

# Learning-loop profile store (D-01) -- directory-level, so WAL/-shm/-journal
# side files are covered too, not just the main .db file (Pitfall 4).
/.assayingest/

# OS
.DS_Store
Thumbs.db

# Local run artifacts -- exports a curator downloaded, Excel's own lock files,
# and a nested profile store under frontend/. Outputs, never sources.
exported/
.assayingest/
~$*
